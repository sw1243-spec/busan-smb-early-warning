# 문제 ①②③ 해결안

> 최종계획서_수정반영본 검토에서 지적한 문제 3건의 구체적 해결
> 2026-08-27

---

# ① SPN090 코드 체계 — Q1 대체

## 문제

기존 Q1은 SPN088과 SPN090의 코드를 **대조**한다. 그런데 행정동·법정동은 앞 5자리(시군구)가 같고 상당수가 겹쳐서, **매칭률이 높게 나와도 다른 체계일 수 있다.** 판정이 오작동한다.

## 해결 — 개수로 판별한다

부산 행정동은 약 205개, 법정동은 300개가 넘는다. **개수가 체계를 직접 증명한다.**

```sql
-- Q1-A  [최우선] 코드 개수로 체계 판별
SELECT 'SPN088_매출' AS src, COUNT(DISTINCT ADMNST_CD) AS code_n FROM SPN088
UNION ALL
SELECT 'SPN089_현금매출',    COUNT(DISTINCT ADMNST_CD)         FROM SPN089
UNION ALL
SELECT 'SPN090_매입',        COUNT(DISTINCT ADMNST_CD)         FROM SPN090
UNION ALL
SELECT 'SPN091_메뉴',        COUNT(DISTINCT ADMNST_CD)         FROM SPN091
UNION ALL
SELECT 'SPN057_행정동기준',  COUNT(DISTINCT ADMNST_INST_CD10)  FROM SPN057;
```

**판정**

| 결과 | 해석 |
|---|---|
| SPN090이 나머지와 **비슷** (200 안팎) | 표기 오타. 정상 진행 |
| SPN090만 **300 이상** | 실제로 법정동. Plan B 발동 |
| SPN057과 비교해 확정 | SPN057이 부산 행정동 기준선 |

## Q1-B — 개수가 같아도 하는 확인 (교차 검증)

개수가 비슷해도 코드값 자체가 다를 수 있다. 집합 차이를 본다.

```sql
-- Q1-B  양방향 집합 차이
SELECT
  (SELECT COUNT(*) FROM (SELECT DISTINCT ADMNST_CD FROM SPN088) a
    WHERE a.ADMNST_CD NOT IN (SELECT DISTINCT ADMNST_CD FROM SPN090)) AS only_in_sales,
  (SELECT COUNT(*) FROM (SELECT DISTINCT ADMNST_CD FROM SPN090) b
    WHERE b.ADMNST_CD NOT IN (SELECT DISTINCT ADMNST_CD FROM SPN088)) AS only_in_prch;
```

양쪽 차집합이 모두 **10 미만**이면 같은 체계로 본다.

> **성능 주의**: 원 Q1은 대용량 원테이블에 바로 조인을 건다. 반드시 `DISTINCT`를 **먼저** 뽑은 뒤 비교할 것. 위 두 쿼리는 그렇게 짜여 있다.

## Plan B (SPN090이 실제 법정동일 때)

**SPN019 「BC카드 행정동-법정동 코드 매핑」이 오픈랩에 있고 원본 반출 가능(OOOOO)하다.**

```
SPN090(법정동) ──SPN019──→ 행정동 ──→ SPN088·089·091과 조인
```

- 1군 신청 목록에 **SPN019 추가** (기존 계획에 없음)
- 매핑 실패 시에만 **구·군 단위로 상향**

즉 최악의 경우에도 주제가 붕괴하지 않는다. **"유일한 단일 실패점" 판정을 하향 조정할 수 있다.**

---

# ② 좌표→행정동 매핑 — 진단 정정 및 해결

## 먼저 — 제 앞선 진단이 과장이었다

내가 "CRD023은 좌표만 있어 행정동 경계 SHP가 없으면 설계 절반이 흔들린다"고 했다. **틀렸다.**

```
CRD023  KOR_BZPLC_ADDR  한글사업장주소  varchar(130)   ← 주소가 있다
CRD023  LOT / LAT       경도 / 위도                    ← 좌표도 있다
```

**주소 텍스트가 있으므로 좌표가 유일한 경로가 아니다.** SHP 없이도 매핑 가능하다.

## 인벤토리 확인 결과

오픈랩 240건에 **행정동 경계 SHP는 없다.** 공간 데이터는 250m 격자·용도지역·거리 데이터뿐이고 **전부 `XXXXX`(반출 전면 불가)**다. 따라서 SHP를 기대한 설계는 애초에 성립하지 않았고, 주소 경로로 가는 것이 정답이다.

대신 매핑에 쓸 수 있는 것이 세 개 있고 **모두 원본 반출 가능**하다.

| 관리번호 | 내용 | 반출 |
|---|---|:-:|
| **SPN019** | BC카드 **행정동-법정동 코드 매핑** | OOOOO |
| **SPN057** | 부산광역시 행정동·행정기관·행정구역 코드 매핑 | OOOOO |
| **POP001** | 집계구 코드 기본정보 — **집계구코드 + 도로명주소 + 지번주소 + 중심 경위도** | OOOOO |

## 해결 — 3중 경로

### 경로 A (주력) : 주소 → 법정동 → 행정동

```
CRD023.KOR_BZPLC_ADDR
   └─ "부산광역시 ○○구 ○○동 123-4" 에서 구·동 파싱
        └─ SPN019 로 법정동 → 행정동 변환
             └─ SPN057 로 자릿수 정규화 (10자리 ↔ 8자리)
                  └─ SPN088~091 / SPN059 와 조인
```

- SHP 불필요
- 파싱 대상이 `○○구` + `○○동` 두 토큰뿐이라 정확도가 높다
- 실패분만 경로 B로 보낸다

### 경로 B (보조) : 좌표 → 집계구 → 행정동

```
CRD023.LAT/LOT
   └─ POP001 집계구 중심 경위도와 최근접 매칭
        └─ POP014 (동코드 + 집계구코드 동시 보유) 브리지
             └─ 행정동
```

- 중심점 최근접이라 **경계 부근에서 오류** 발생 가능
- 경로 A의 **검증용**으로 병행: A와 B 결과가 다른 건수를 세어 오차율 보고

### 경로 C (최후) : 구·군 단위 상향

주소에서 `○○구`만 파싱하면 **16개 구군 매핑은 사실상 100%** 성립한다. 해상도를 잃지만 분석은 살아남는다.

## 검증 쿼리

```sql
-- Q2-A  CRD023 주소 형식 확인 (파싱 규칙 확정용)
SELECT KOR_BZPLC_ADDR
FROM   CRD023
WHERE  KOR_BZPLC_ADDR IS NOT NULL AND KOR_BZPLC_ADDR <> ''
LIMIT  200;
--  지번주소인지 도로명주소인지, "부산광역시"로 시작하는지,
--  구·동 토큰이 일관되게 존재하는지 눈으로 확인

-- Q2-B  주소 결측률
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR = '' THEN 1 ELSE 0 END) AS addr_null,
       SUM(CASE WHEN LAT IS NULL OR LAT = '' THEN 1 ELSE 0 END) AS coord_null
FROM   CRD023;

-- Q2-C  SPN019 매핑 구조 확인
SELECT * FROM SPN019 LIMIT 50;
--  행정동↔법정동 코드가 실제로 양쪽 다 있는지 확인
--  (데이터명은 매핑이나 컬럼 목록에 법정동이 불명확)
```

## 신청 목록 수정

| 조치 | 대상 |
|---|---|
| **1군 추가** | **SPN019** (행정동-법정동 매핑) — Plan B와 경로 A 모두의 핵심 |
| 1군 확인 | SPN057, POP001, POP014 이미 포함되어 있는지 |

## 리스크 재평가

| | 기존 | 수정 |
|---|---|---|
| 심각도 | 중 (리스크 7) | **중 유지** — 단, 사유 변경 |
| 사유 | "SHP 없으면 매핑 불가" | "SHP는 애초에 없다. **주소 파싱 정확도**가 실제 쟁점" |
| 센터 문의 | "SHP 제공되나요?" | **불필요** — 인벤토리로 확인 완료 (없음) |

문의 항목 하나를 줄이고, 그 자리에 다른 질문을 넣을 수 있다.

---

# ③ 개인사업자 ≠ 소상공인 — Q4 대체

## 문제

기존 Q4는 `CP_CD`(개인/법인)와 `ENP_SZE`(대·중견·중소)만 본다. 그런데

| 용어 | 기준 |
|---|---|
| 개인사업자 | 법인이 아니라는 뜻. **규모와 무관** |
| **소상공인** | **상시근로자 수** — 제조·건설·운수·광업 10명 미만 / **그 외 5명 미만** |

`ENP_SZE`의 "중소기업"은 소상공인보다 훨씬 넓은 범주다. 그대로 세면

> 묻는 것: **소상공인** 몇 명이 관측되는가
> 세는 것: **개인사업자·중소기업** 몇 명이 있는가

분자와 분모의 기준이 어긋나 **결론 3층의 비율 자체가 틀린다.**

## 해결 — 직원수 컬럼을 넣는다

`CRD022`에 `JOIN_CNT`(직원수, int)가 있다.

```sql
-- Q4-A  소상공인 기준 커버리지 (3축 교차)
SELECT CP_CD,
       ENP_SZE,
       CASE WHEN JOIN_CNT IS NULL         THEN '미상'
            WHEN JOIN_CNT < 5             THEN '5명미만'
            WHEN JOIN_CNT < 10            THEN '5-9명'
            ELSE                               '10명이상' END AS size_band,
       COUNT(DISTINCT IDCD) AS ent_n,
       COUNT(DISTINCT BRNO) AS brno_n
FROM   CRD022
GROUP  BY 1, 2, 3
ORDER  BY ent_n DESC;
```

## 소상공인 판정 기준

업종별로 기준이 다르므로 산업코드와 함께 본다.

```sql
-- Q4-B  업종 대분류 × 직원수 (업종별 기준 적용용)
SELECT SUBSTR(INDST_CD_ID, 1, 1) AS industry_1,
       CASE WHEN JOIN_CNT < 5 THEN '5미만'
            WHEN JOIN_CNT < 10 THEN '5-9'
            ELSE '10이상' END AS size_band,
       COUNT(DISTINCT IDCD) AS n
FROM   CRD022
GROUP  BY 1, 2;
```

**적용 규칙**

```
소상공인 = ( 제조·건설·운수·광업  AND  상시근로자 10명 미만 )
        OR ( 그 외 업종          AND  상시근로자  5명 미만 )
```

## 결론 3층 계산 수정

기존 L1/L2/L3 정의에 **소상공인 필터를 명시적으로 건다.**

```
A  = CRD022 등재 사업자 중 소상공인 기준 충족분   ← JOIN_CNT 필터 적용
B  = A 중 CRD025 거래 관측분
N  = 부산 소상공인 모집단 (KOSIS / 국세청 2기준 구간)

L1 완전 관측 = |A ∩ B|
L2 부분 관측 = |A \ B|
L3 미관측    = N − |A|
```

**JOIN_CNT 결측이 많을 경우**: `ENP_SZE`와 `CP_CD` 조합으로 근사하되, **근사했다는 사실과 결측률을 반드시 명시**한다. 결론 3층은 "구간 제시"가 원칙이므로 결측률이 곧 구간 폭이 된다.

---

# 요약 — 반영할 것

| # | 조치 | 위치 |
|:-:|---|---|
| 1 | **Q1을 Q1-A(개수) + Q1-B(집합차)로 교체** | 7장 W1 쿼리 |
| 2 | Plan B에 **SPN019 경유** 추가. 리스크 1의 "유일한 단일 실패점" 표현 완화 | 4장 P3 / 9장 리스크 1 |
| 3 | **SPN019를 1군 신청 목록에 추가** | 2장 신청 목록 |
| 4 | CRD023 설명에 **`KOR_BZPLC_ADDR` 주소 컬럼 명기** | 3장 공간 키 / 4장 P1 |
| 5 | 좌표→행정동을 **경로 A(주소) 주력 / B(좌표) 검증 / C(구군) 최후**로 재서술 | 4장, 9장 리스크 7 |
| 6 | **센터 문의에서 "행정동 경계 SHP" 항목 삭제** (인벤토리로 확인 완료 — 없음) | 10장 문의 |
| 7 | **Q4를 Q4-A + Q4-B로 교체**, `JOIN_CNT` 포함 | 7장 W1 쿼리 |
| 8 | L1/L2/L3 정의에 **소상공인 필터 명시** | 5장 문제 3 |
| 9 | Q2-A·Q2-B·Q2-C를 W1 쿼리에 신규 추가 | 7장 |
