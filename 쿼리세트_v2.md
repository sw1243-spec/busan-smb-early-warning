# 오픈랩 쿼리 세트 v2

> 코드리뷰 15건 반영 · 2026-08-28
> **반입 시 xlsx로 변환** (`.sql` 반입 불가 / xls·xlsx·pdf·이미지 **3개** 30MB)

---

## v1에서 고친 것

| # | v1 문제 | v2 조치 |
|:-:|---|---|
| 1 | **SPN019를 법정동 매핑으로 오인** — 실제 컬럼에 법정동 없음 | REL020(법정동코드 보유)로 **직접 대조**. 매핑은 **반입 시트**로 해결 |
| 2 | P1이 varchar 금액을 무가드 CAST | 정규식 가드 + 실패 건수 동시 집계 |
| 3 | BIRTH_YEAR 무가드 CAST → AGE 부풀림 | `^[0-9]{4}$` + 연도 범위 가드, **공통 CTE로 1회 정의** |
| 4 | P2가 CRD026 SE_GB 무시 | `SE_GB IN ('0','2')` + **폐업 시점 근접 조건** |
| 5 | Q4가 다중 기준연월 중복 계수 | 업체별 **최신 CRTR_YM 고정** |
| 6 | Q4-C 단일 임계(10명) | **업종별 임계**(제조·건설·운수·광업 10 / 그 외 5) |
| 7 | P3 ended/started 행 수 계수 | `COUNT(DISTINCT DLR_BRNO)` |
| 8 | NET_EXIT 분모가 당월 | **전월** 참조 (LAG + 5.x 대체안) |
| 9 | Q9-B가 SPN093(2025) 참조 | **SPN020**(202201) 으로 교체 |
| 10 | Q6 LEFT JOIN 행 증폭 | SPN057 **DISTINCT 선처리** |
| 11 | Q0-4 UNION 타입 충돌 | 전부 문자 캐스팅 |
| 12 | P1이 매출 없는 셀 누락 | 키 UNION 후 양방향 LEFT JOIN |
| 13 | P1-B 메뉴 믹스 혼입 | **MENU_PRC** 기반으로 교체 |
| 14 | `CAST(x AS CHAR)` PostgreSQL 1글자 truncate | 전 쿼리 【P】변형 병기 |
| 15 | 체크리스트 2시간 30분 과대 | **핵심 5개 20분 / 전체 1~1.5시간** 으로 재산정 |

---

# Phase 0 — 환경·스키마 (10분)

```sql
-- Q0-1  테이블 목록 (분할 여부·실제 명칭)  ★ 가장 먼저
--【M】
SHOW TABLES;
--【P】
SELECT table_name FROM information_schema.tables
WHERE table_schema='public' ORDER BY 1;
```

```sql
-- Q0-2  컬럼 목록 (정의서 대조)
--【M】
SHOW COLUMNS FROM SPN090;
--【P】
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name='spn090' ORDER BY ordinal_position;
```

```sql
-- Q0-2B  DB 버전 (CTE·창함수 지원 여부 결정)  ★신규
--【M】
SELECT VERSION();          -- 8.0 미만이면 WITH·LAG 불가 → 대체안 사용
--【P】
SELECT version();
```

```sql
-- Q0-3  주요 테이블 건수
--  ※ 대용량이면 오래 걸림. 느리면 건너뛰고 Q0-4로.
SELECT 'SPN088' AS t, COUNT(*) AS n FROM SPN088
UNION ALL SELECT 'SPN089', COUNT(*) FROM SPN089
UNION ALL SELECT 'SPN090', COUNT(*) FROM SPN090
UNION ALL SELECT 'SPN091', COUNT(*) FROM SPN091
UNION ALL SELECT 'CRD022', COUNT(*) FROM CRD022
UNION ALL SELECT 'CRD023', COUNT(*) FROM CRD023
UNION ALL SELECT 'CRD025', COUNT(*) FROM CRD025
UNION ALL SELECT 'CRD026', COUNT(*) FROM CRD026
UNION ALL SELECT 'SPN059', COUNT(*) FROM SPN059;
```

```sql
-- Q0-4  기간 범위   ※ 전부 문자로 캐스팅해 UNION 타입 충돌 방지 (v1 버그)
--【M】
SELECT 'SPN090' AS t, CAST(MIN(CRTR_YMD) AS CHAR(8)) AS d1, CAST(MAX(CRTR_YMD) AS CHAR(8)) AS d2 FROM SPN090
UNION ALL SELECT 'CRD025', CAST(MIN(CRTR_YM) AS CHAR(8)),  CAST(MAX(CRTR_YM) AS CHAR(8))  FROM CRD025
UNION ALL SELECT 'SPN059', CAST(MIN(STRD_DATE) AS CHAR(8)), CAST(MAX(STRD_DATE) AS CHAR(8)) FROM SPN059;
--【P】CAST(... AS TEXT) 로 교체.  ※ PostgreSQL에서 CAST(x AS CHAR)는 char(1)로 잘림
```

---

# Phase 1 — 판정 쿼리

## ★ 핵심 5개 (20분) — 이것만으로 주제 생사 판정

| 순서 | 쿼리 | 결정 |
|:-:|---|---|
| 1 | Q0-1 | 테이블명 (전제조건) |
| 2 | **Q8-B** | 부산 특수성 축 생사 |
| 3 | **Q4-A** | "소상공인" 제목 성립 + 결론 3층 |
| 4 | **Q1-B** | MPI 성립 여부 |
| 5 | **Q7-B** | NET_EXIT 성립 여부 |

나머지는 설계를 다듬는 용도라 2차 방문으로 미뤄도 된다.

---

## 【A】공간 키

### Q1-A · 코드 개수 (1차 신호)

```sql
SELECT 'SPN088_매출' AS src, COUNT(DISTINCT ADMNST_CD) AS code_n FROM SPN088
UNION ALL SELECT 'SPN089_현금매출', COUNT(DISTINCT ADMNST_CD) FROM SPN089
UNION ALL SELECT 'SPN090_매입',     COUNT(DISTINCT ADMNST_CD) FROM SPN090
UNION ALL SELECT 'SPN091_메뉴',     COUNT(DISTINCT ADMNST_CD) FROM SPN091;
```

부산 행정동 약 205 / 법정동 300+ → SPN090만 300 이상이면 의심.

### Q1-B ★결정적 · REL020 법정동코드와 직접 대조

> **v1의 개수 비교는 간접 증거였다.** REL020에 `A1 법정동코드(varchar 10)`가 있으므로 **직접 대조가 가능하다.**
> REL020은 반출 불가(`XXXXX`)지만 **오픈랩 내부 분석에는 사용 가능**하다.

```sql
--【M/P 공통】
SELECT
  (SELECT COUNT(*) FROM (SELECT DISTINCT ADMNST_CD AS cd FROM SPN090) s
     WHERE s.cd IN (SELECT DISTINCT A1 FROM REL020))                     AS spn090_in_법정동,
  (SELECT COUNT(DISTINCT ADMNST_CD) FROM SPN090)                         AS spn090_total,
  (SELECT COUNT(*) FROM (SELECT DISTINCT ADMNST_CD AS cd FROM SPN088) s
     WHERE s.cd IN (SELECT DISTINCT A1 FROM REL020))                     AS spn088_in_법정동,
  (SELECT COUNT(DISTINCT ADMNST_CD) FROM SPN088)                         AS spn088_total;
```

| 결과 | 판정 |
|---|---|
| SPN090 매칭률 **높고** SPN088 매칭률 **낮음** | **SPN090은 법정동 확정** → Q1-D |
| 둘 다 낮음 | 둘 다 행정동. **비고 오타. 정상 진행** |
| 둘 다 높음 | 코드 체계가 겹침 → Q1-C로 추가 확인 |

### Q1-C · SPN057 행정기관코드와 대조 (보조)

```sql
SELECT
  (SELECT COUNT(*) FROM (SELECT DISTINCT ADMNST_CD AS cd FROM SPN090) s
     WHERE s.cd IN (SELECT DISTINCT ADMNST_INST_CD10 FROM SPN057)) AS spn090_in_행정,
  (SELECT COUNT(*) FROM (SELECT DISTINCT ADMNST_CD AS cd FROM SPN088) s
     WHERE s.cd IN (SELECT DISTINCT ADMNST_INST_CD10 FROM SPN057)) AS spn088_in_행정;
```

### Q1-D · Plan B — 법정동 → 행정동 (v1의 SPN019 경로 폐기)

> **SPN019는 법정동 코드 컬럼이 없다.** 실제 컬럼은
> `CTPV_NM · HCODE(행정기관 코드) · GCODE(시군구 코드) · HNAME · GNAME · CRTR_YM · LOAD_YMD`
> 즉 행정동↔시군구 매핑이며 법정동 변환에 쓸 수 없다.

**대체 3경로**

| 순위 | 방법 | 비고 |
|:-:|---|---|
| **1** | **법정동-행정동 매핑표를 반입** | 행안부 공개 코드목록에서 부산만 추출. 수백 행 → **쿼리세트.xlsx의 시트로 넣으면 반입 파일 수 불변** |
| 2 | POP001(지번주소·집계구) → POP014(집계구↔동코드) | POP014 `TOWN_VAL`이 행정동인지 확인 필요 (Q1-E) |
| 3 | **구·군 상향** | 코드 앞 5자리는 법정동·행정동 **공통** → 시군구 조인은 항상 성립 |

```sql
-- Q1-E  POP014 동 코드 체계 확인 (경로 2 가능성)
SELECT COUNT(DISTINCT TOWN_VAL) AS town_n,
       COUNT(DISTINCT TZ_AREA_VAL) AS zone_n
FROM   POP014;
--  town_n 이 205 안팎이면 행정동, 300+ 이면 법정동
```

### Q2-A · CRD023 주소 형식

```sql
SELECT KOR_BZPLC_ADDR
FROM   CRD023
WHERE  KOR_BZPLC_ADDR IS NOT NULL AND KOR_BZPLC_ADDR <> ''
LIMIT  200;
```

### Q2-B · 주소·좌표 결측률

```sql
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR='' THEN 1 ELSE 0 END) AS addr_null,
       SUM(CASE WHEN LAT IS NULL OR LAT='' THEN 1 ELSE 0 END)                        AS coord_null,
       SUM(CASE WHEN (KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR='')
                 AND (LAT IS NULL OR LAT='') THEN 1 ELSE 0 END)                      AS both_null
FROM   CRD023;
```

### Q6 · SPN057 매핑 성공률 ※ DISTINCT 선처리 (v1 행 증폭 버그 수정)

```sql
--【M 8+ / P】
WITH d AS (SELECT DISTINCT ADM_CD FROM SPN059),
     m AS (SELECT DISTINCT ADMNST_CD FROM SPN057)
SELECT COUNT(*) AS spn059_dong,
       SUM(CASE WHEN m.ADMNST_CD IS NOT NULL THEN 1 ELSE 0 END) AS mapped
FROM   d LEFT JOIN m ON d.ADM_CD = m.ADMNST_CD;
```

```sql
--【M 5.x 대체】CTE 미지원
SELECT COUNT(*) AS spn059_dong,
       SUM(CASE WHEN m.ADMNST_CD IS NOT NULL THEN 1 ELSE 0 END) AS mapped
FROM  (SELECT DISTINCT ADM_CD FROM SPN059) d
LEFT JOIN (SELECT DISTINCT ADMNST_CD FROM SPN057) m
       ON d.ADM_CD = m.ADMNST_CD;
```

---

## 【B】업종 키

| 축 | 컬럼 | 분류표 |
|---|---|---|
| KCD (SPN088~091) | `INDST_SE_LCLS_CD` / `INDST_SE_MCLS_CD` | **SPN094** |
| 동백전 (SPN059/060) | `BUSIN_CD` | **SPN020** ← v1은 SPN093 오용 |
| KODATA (CRD022) | `INDST_CD_ID` | 산업분류 (KSIC 추정) |

### Q9-A · 코드 개수

```sql
SELECT 'KCD_대' AS ax, COUNT(DISTINCT INDST_SE_LCLS_CD) AS n FROM SPN088
UNION ALL SELECT 'KCD_중',  COUNT(DISTINCT INDST_SE_MCLS_CD) FROM SPN088
UNION ALL SELECT 'BC_업종', COUNT(DISTINCT BUSIN_CD)         FROM SPN059
UNION ALL SELECT 'KODATA',  COUNT(DISTINCT INDST_CD_ID)      FROM CRD022;
```

### Q9-B · 분류표 조회 ※ SPN020 사용

```sql
SELECT * FROM SPN094;   -- KCD 업종
SELECT * FROM SPN020;   -- BC카드 업종 (기준년월 202201 — 동백전 기간과 일치)
--  ※ SPN093은 2025년 기준이고 코드 자릿수도 달라(대분류 4자리 vs 2자리) 사용 금지

SELECT TP_BIZ_NM, COUNT(*) AS n
FROM   CRD023 GROUP BY TP_BIZ_NM ORDER BY n DESC LIMIT 100;
```

**매핑 실패 시**: 대분류 10개 내외 수동 매핑으로 후퇴. 반출지침의 "범주 10개 내외" 원칙에도 부합한다.

---

## 【C】식별 키

### Q2 · CRD022 브리지 무결성

```sql
SELECT COUNT(*)               AS rows,
       COUNT(DISTINCT BRNO)   AS brno_n,
       COUNT(DISTINCT IDCD)   AS idcd_n,
       COUNT(DISTINCT CRTR_YM) AS month_n
FROM   CRD022;
--  rows > idcd_n * month_n 이면 1기업:N사업자번호 관계 존재
```

### Q3 · CRD025 → CRD022 매칭률

```sql
SELECT COUNT(*) AS crd025_brno,
       SUM(CASE WHEN b.BRNO IS NOT NULL THEN 1 ELSE 0 END) AS matched
FROM  (SELECT DISTINCT BRNO FROM CRD025) a
LEFT JOIN (SELECT DISTINCT BRNO FROM CRD022) b ON a.BRNO=b.BRNO;
```

### Q3-B · CRD022 → CRD023 매칭률

```sql
SELECT COUNT(*) AS crd022_idcd,
       SUM(CASE WHEN c.IDCD IS NOT NULL THEN 1 ELSE 0 END) AS matched
FROM  (SELECT DISTINCT IDCD FROM CRD022) a
LEFT JOIN (SELECT DISTINCT IDCD FROM CRD023) c ON a.IDCD=c.IDCD;
```

---

## 【D】커버리지 — 결론 3층

### Q4-A ★ 소상공인 커버리지 ※ 최신 시점 고정 (v1 중복계수 버그 수정)

```sql
--【M 8+ / P】
WITH latest AS (
  SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD
),
snap AS (
  SELECT c.* FROM CRD022 c JOIN latest l ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx
)
SELECT CP_CD, ENP_SZE,
       CASE WHEN JOIN_CNT IS NULL THEN '미상'
            WHEN JOIN_CNT < 5     THEN '5명미만'
            WHEN JOIN_CNT < 10    THEN '5-9명'
            ELSE                       '10명이상' END AS size_band,
       COUNT(DISTINCT IDCD) AS ent_n,
       COUNT(DISTINCT BRNO) AS brno_n
FROM   snap
GROUP  BY 1,2,3
ORDER  BY ent_n DESC;
--  CP_CD   1:일반법인 2:공공기관 3:비영리법인 8:기타법인 9:개인
--  ENP_SZE 1:대기업 2:중소기업 3:중견기업 0:대상아님
```

```sql
--【M 5.x 대체】
SELECT c.CP_CD, c.ENP_SZE,
       CASE WHEN c.JOIN_CNT IS NULL THEN '미상'
            WHEN c.JOIN_CNT < 5 THEN '5명미만'
            WHEN c.JOIN_CNT < 10 THEN '5-9명'
            ELSE '10명이상' END AS size_band,
       COUNT(DISTINCT c.IDCD) AS ent_n
FROM   CRD022 c
JOIN  (SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD) l
       ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx
GROUP BY 1,2,3;
```

### Q4-B · 업종 대분류 × 규모 (업종별 임계 적용 준비)

```sql
--【M】SUBSTR  【P】SUBSTR (동일)
SELECT SUBSTR(c.INDST_CD_ID,1,1) AS industry_1,
       CASE WHEN c.JOIN_CNT < 5 THEN '5미만'
            WHEN c.JOIN_CNT < 10 THEN '5-9'
            ELSE '10이상' END AS size_band,
       COUNT(DISTINCT c.IDCD) AS n
FROM   CRD022 c
JOIN  (SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD) l
       ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx
GROUP BY 1,2 ORDER BY 1,2;
--  ※ INDST_CD_ID가 KSIC 알파벳 시작인지 숫자인지 여기서 확인.
--     숫자면 아래 Q4-C의 IN ('B','C','F','H') 조건을 숫자코드로 교체.
```

### Q4-C · L1 / L2 ※ 업종별 임계 + 미상 별도 보고 (v1 버그 2건 수정)

```sql
--【M 8+ / P】
WITH latest AS (
  SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD
),
snap AS (
  SELECT c.* FROM CRD022 c JOIN latest l ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx
),
sb AS (                       -- 소상공인 기준 충족 집합 A
  SELECT DISTINCT BRNO
  FROM   snap
  WHERE  JOIN_CNT IS NOT NULL
    AND  JOIN_CNT < CASE WHEN SUBSTR(INDST_CD_ID,1,1) IN ('B','C','F','H')
                         THEN 10 ELSE 5 END
),
unknown AS (                  -- 직원수 미상 — 구간 폭이 된다
  SELECT DISTINCT BRNO FROM snap WHERE JOIN_CNT IS NULL
),
tx AS (SELECT DISTINCT BRNO FROM CRD025)
SELECT
  (SELECT COUNT(*) FROM sb)                                    AS A_소상공인명부,
  (SELECT COUNT(*) FROM unknown)                               AS A_직원수미상,
  (SELECT COUNT(*) FROM sb JOIN tx ON sb.BRNO=tx.BRNO)         AS L1_완전관측,
  (SELECT COUNT(*) FROM sb LEFT JOIN tx ON sb.BRNO=tx.BRNO
     WHERE tx.BRNO IS NULL)                                    AS L2_부분관측;
--  L3 미관측 = N(외부 모집단) − A_소상공인명부
--  ※ A_직원수미상이 크면 그만큼을 구간 상·하한으로 제시 (단일 수치 주장 금지)
--  ※ KSIC 대분류 B:광업 C:제조업 F:건설업 H:운수및창고업 → 10명 미만
--     그 외 업종 → 5명 미만 (소상공인기본법 시행령)
```

---

## 【E】자료형·유효성

### Q5 · varchar 금액 값 패턴

```sql
SELECT CASH_PRCH_AMT, TAX_PRCH_AMT
FROM   SPN090
WHERE  CASH_PRCH_AMT IS NOT NULL
LIMIT  200;
```

### Q5-B · 정제 후에도 숫자가 아닌 행 규모

```sql
--【M】
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','')
                     REGEXP '^[0-9]+(\\.[0-9]+)?$' THEN 0 ELSE 1 END) AS bad
FROM   SPN090 WHERE CASH_PRCH_AMT IS NOT NULL;
--【P】
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','')
                     ~ '^[0-9]+(\.[0-9]+)?$' THEN 0 ELSE 1 END) AS bad
FROM   SPN090 WHERE CASH_PRCH_AMT IS NOT NULL;
```

### Q5-C · 정제 제외분의 금액 규모 ※ v1은 clean 쪽만 봤음

```sql
--【M】불량 행의 실제 값을 눈으로 확인 (0인지, 마스킹인지, 큰 값인지)
SELECT CASH_PRCH_AMT, COUNT(*) AS n
FROM   SPN090
WHERE  CASH_PRCH_AMT IS NOT NULL
  AND  REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') NOT REGEXP '^[0-9]+(\\.[0-9]+)?$'
GROUP  BY CASH_PRCH_AMT ORDER BY n DESC LIMIT 50;
--  마스킹(*)이 큰 금액에 몰려 있으면 m이 체계적으로 과소 추정된다 → 한계로 명시
```

### Q7 · 거래 시작·종료일 유효성

```sql
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>'' THEN 1 ELSE 0 END) AS has_start,
       SUM(CASE WHEN VAT_END_YMD  IS NOT NULL AND VAT_END_YMD <>'' THEN 1 ELSE 0 END) AS has_end
FROM   CRD025;
```

### Q7-B ★ 매출/매입 구분 실재

```sql
SELECT DATA_SE_SLS_PRCHS_CD, COUNT(*) AS n, COUNT(DISTINCT BRNO) AS biz
FROM   CRD025 GROUP BY DATA_SE_SLS_PRCHS_CD;
--  1:매출  2:매입  →  '2'가 충분해야 NET_EXIT 가능
```

---

## 【F】부산 특수성 축

> **공통 파생 정의** — v1은 age 계산이 네 곳에 중복돼 가드 수정이 누락되기 쉬웠다.
> 아래 `dbj` 블록을 각 쿼리 앞에 붙여 재사용한다.

```sql
--【M 8+ / P】공통 CTE — BIRTH_YEAR 가드 포함
WITH dbj AS (
  SELECT ADM_CD, BUSIN_CD, TRANS_CNT, TRANS_SUM,
         SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,6) AS ym,
         CAST(SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,4) AS SIGNED)
           - CAST(BIRTH_YEAR AS SIGNED)          AS age
  FROM   SPN059
  WHERE  BIRTH_YEAR REGEXP '^[0-9]{4}$'                 --【P】BIRTH_YEAR ~ '^[0-9]{4}$'
    AND  CAST(BIRTH_YEAR AS SIGNED) BETWEEN 1900 AND 2026
)
--【P】CAST(STRD_DATE AS CHAR(8)) → CAST(STRD_DATE AS TEXT)
--    CAST(x AS SIGNED)          → CAST(x AS INTEGER)
```

### Q8 · 연령 분포

```sql
WITH dbj AS ( /* 위 정의 */ )
SELECT age, SUM(TRANS_CNT) AS cnt, SUM(TRANS_SUM) AS amt
FROM   dbj GROUP BY age ORDER BY age;
```

### Q8-B ★ 60세 이상 비중 (AGE 축 생사)

```sql
WITH dbj AS ( /* 위 정의 */ )
SELECT SUM(CASE WHEN age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(TRANS_SUM),0) AS age_dep_60,
       SUM(CASE WHEN age>=60 THEN TRANS_CNT ELSE 0 END)
         / NULLIF(SUM(TRANS_CNT),0) AS age_dep_60_cnt
FROM   dbj;
```

| 결과 | 판정 |
|---|---|
| 60세 이상 **10% 이상** | AGE 축 진행 |
| **5% 미만** | AGE 폐기 → 부산 특수성을 **POP014 지역 인구 고령화**로 대체 |

### Q8-C · 전·후반 순위 안정성 (외삽 정당화)

```sql
WITH dbj AS ( /* 위 정의 */ )
SELECT ADM_CD, BUSIN_CD,
       SUM(CASE WHEN ym <'202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN ym <'202301' THEN TRANS_SUM ELSE 0 END),0) AS dep_h1,
       SUM(CASE WHEN ym>='202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN ym>='202301' THEN TRANS_SUM ELSE 0 END),0) AS dep_h2
FROM   dbj GROUP BY ADM_CD, BUSIN_CD;
--  결과로 Spearman 순위상관 계산 → ρ > 0.7 이면 외삽 정당화
```

---

# Phase 2 — 파이프라인

## P1 · MPI 재료 ※ 정제 가드 + 매출없는 셀 보존 (v1 버그 2건 수정)

```sql
--【M 8+】  ※【P】는 REGEXP→~ , DECIMAL→NUMERIC , CHAR→TEXT
WITH s AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CARD_SLS_AMT) AS card_amt
  FROM   SPN088 GROUP BY 1,2,3
),
c AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CASE WHEN REPLACE(REPLACE(CASH_SLS_AMT,',',''),' ','') REGEXP '^[0-9]+(\\.[0-9]+)?$'
                  THEN CAST(REPLACE(REPLACE(CASH_SLS_AMT,',',''),' ','') AS DECIMAL(20,2)) END) AS cash_amt,
         SUM(CASE WHEN REPLACE(REPLACE(TAX_SLS_AMT,',',''),' ','')  REGEXP '^[0-9]+(\\.[0-9]+)?$'
                  THEN CAST(REPLACE(REPLACE(TAX_SLS_AMT,',',''),' ','')  AS DECIMAL(20,2)) END) AS tax_amt,
         SUM(CASE WHEN REPLACE(REPLACE(CASH_SLS_AMT,',',''),' ','') REGEXP '^[0-9]+(\\.[0-9]+)?$'
                  THEN 0 ELSE 1 END)                                                            AS bad_cnt
  FROM   SPN089 GROUP BY 1,2,3
),
p AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') REGEXP '^[0-9]+(\\.[0-9]+)?$'
                  THEN CAST(REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') AS DECIMAL(20,2)) END) AS cash_prch,
         SUM(CASE WHEN REPLACE(REPLACE(TAX_PRCH_AMT,',',''),' ','')  REGEXP '^[0-9]+(\\.[0-9]+)?$'
                  THEN CAST(REPLACE(REPLACE(TAX_PRCH_AMT,',',''),' ','')  AS DECIMAL(20,2)) END) AS tax_prch,
         SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') REGEXP '^[0-9]+(\\.[0-9]+)?$'
                  THEN 0 ELSE 1 END)                                                             AS bad_cnt
  FROM   SPN090 GROUP BY 1,2,3
),
keys AS (                          -- 세 축의 키 합집합 → 매출 없는 셀도 보존
  SELECT ym, ADMNST_CD, ind FROM s
  UNION SELECT ym, ADMNST_CD, ind FROM c
  UNION SELECT ym, ADMNST_CD, ind FROM p
)
SELECT k.ym, k.ADMNST_CD, k.ind,
       COALESCE(s.card_amt,0)+COALESCE(c.cash_amt,0)+COALESCE(c.tax_amt,0)  AS S_total,
       COALESCE(p.cash_prch,0)+COALESCE(p.tax_prch,0)                       AS M_total,
       (COALESCE(p.cash_prch,0)+COALESCE(p.tax_prch,0))
         / NULLIF(COALESCE(s.card_amt,0)+COALESCE(c.cash_amt,0)+COALESCE(c.tax_amt,0),0) AS m_ratio,
       COALESCE(c.bad_cnt,0)+COALESCE(p.bad_cnt,0)                          AS bad_rows
FROM       keys k
LEFT JOIN  s ON k.ym=s.ym AND k.ADMNST_CD=s.ADMNST_CD AND k.ind=s.ind
LEFT JOIN  c ON k.ym=c.ym AND k.ADMNST_CD=c.ADMNST_CD AND k.ind=c.ind
LEFT JOIN  p ON k.ym=p.ym AND k.ADMNST_CD=p.ADMNST_CD AND k.ind=p.ind;
--  bad_rows > 0 인 셀은 m_ratio 신뢰도가 낮다 → 분석에서 제외하거나 별도 표기
```

## P1-B · 가격 전가 계수 π 재료 ※ MENU_PRC 사용 (v1 믹스 혼입 수정)

```sql
-- 메뉴 단위 가격의 월별 변화 → 메뉴별 변화율을 판매량 가중 평균
--【M 8+ / P】
WITH mp AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         MENU_STD_NM,
         AVG(MENU_PRC)      AS price,      -- 실제 가격 (믹스 무관)
         SUM(MENU_SLS_CNT)  AS qty
  FROM   SPN091 GROUP BY 1,2,3,4
)
SELECT a.ym, a.ADMNST_CD, a.ind,
       SUM((a.price - b.price)/NULLIF(b.price,0) * a.qty) / NULLIF(SUM(a.qty),0) AS d_price_w
FROM       mp a
INNER JOIN mp b
        ON a.ADMNST_CD=b.ADMNST_CD AND a.ind=b.ind AND a.MENU_STD_NM=b.MENU_STD_NM
       AND b.ym = /* a.ym 의 전월 */
--【M】DATE_FORMAT(DATE_SUB(STR_TO_DATE(CONCAT(a.ym,'01'),'%Y%m%d'), INTERVAL 1 MONTH),'%Y%m')
--【P】to_char(to_date(a.ym||'01','YYYYMMDD') - interval '1 month','YYYYMM')
GROUP BY a.ym, a.ADMNST_CD, a.ind;
--  동일 메뉴끼리 비교하므로 메뉴 구성 변화가 가격 변화로 오인되지 않는다
--  π 는 이후 패널회귀  d_price_w = α + π·Δm + ε  로 추정
```

## P2 · 종속변수 ※ SE_GB + 시점 근접 조건 (v1 과소집계 수정)

```sql
--【M 8+ / P】
WITH mv AS (                       -- 이전(移轉)으로 볼 기록만
  SELECT IDCD,
         CAST(SUBSTR(CRTR_YM,1,4) AS SIGNED)*12
           + CAST(SUBSTR(CRTR_YM,5,2) AS SIGNED) AS mv_mi
  FROM   CRD026
  WHERE  SE_GB IN ('0','2')        -- 0:부산내이동  2:전출   (1:전입은 폐업과 무관)
)
SELECT c.IDCD, c.CLSB_SE_CD, c.CLSB_YMD,
       CASE WHEN c.CLSB_SE_CD IS NOT NULL AND c.CLSB_SE_CD <> ''
                 AND mv.IDCD IS NULL
            THEN 1 ELSE 0 END AS closed_flag
FROM       CRD023 c
LEFT JOIN  mv
        ON c.IDCD = mv.IDCD
       AND c.CLSB_YMD IS NOT NULL AND c.CLSB_YMD <> ''
       AND ABS(mv.mv_mi
               - (CAST(SUBSTR(c.CLSB_YMD,1,4) AS SIGNED)*12
                  + CAST(SUBSTR(c.CLSB_YMD,5,2) AS SIGNED))) <= 3;
--  ※ 폐업 시점 ±3개월 이내의 전출·시내이동만 "이전"으로 보아 제외
--     v1은 시점 무관·구분 무관으로 전부 제외해 전입 업체의 폐업까지 놓쳤다
--  ※ 제외 전후 폐업률·AUC 차이를 정량 보고 (방법론적 기여 항목)
```

## P3 · NET_EXIT · HHI ※ DISTINCT + 전월 분모 (v1 버그 2건 수정)

```sql
--【M 8+ / P】
WITH mm AS (
  SELECT BRNO, CRTR_YM,
         COUNT(DISTINCT DLR_BRNO) AS n_partner,
         COUNT(DISTINCT CASE WHEN VAT_END_YMD IS NOT NULL AND VAT_END_YMD<>''
                              AND SUBSTR(VAT_END_YMD,1,6)=CRTR_YM
                             THEN DLR_BRNO END) AS ended,
         COUNT(DISTINCT CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>''
                              AND SUBSTR(VAT_STRT_YMD,1,6)=CRTR_YM
                             THEN DLR_BRNO END) AS started
  FROM   CRD025
  WHERE  DATA_SE_SLS_PRCHS_CD = '2'
  GROUP  BY BRNO, CRTR_YM
),
hh AS (
  SELECT BRNO, CRTR_YM,
         SUM(POWER(CAST(PT1 AS DECIMAL(12,6)),2)) AS hhi
  FROM   CRD025
  WHERE  DATA_SE_SLS_PRCHS_CD='2'
    AND  PT1 REGEXP '^[0-9]+(\\.[0-9]+)?$'      --【P】PT1 ~ '...'
  GROUP  BY BRNO, CRTR_YM
),
w AS (
  SELECT BRNO, CRTR_YM, n_partner, ended, started,
         LAG(n_partner) OVER (PARTITION BY BRNO ORDER BY CRTR_YM) AS prev_partner
  FROM   mm
)
SELECT w.BRNO, w.CRTR_YM, w.n_partner, w.prev_partner, w.ended, w.started,
       (w.ended - w.started) * 1.0 / NULLIF(w.prev_partner,0) AS net_exit,
       h.hhi
FROM       w LEFT JOIN hh h ON w.BRNO=h.BRNO AND w.CRTR_YM=h.CRTR_YM
ORDER BY   w.BRNO, w.CRTR_YM;
```

```sql
--【M 5.x 대체】LAG 미지원 → 전월 self-join
SELECT m.BRNO, m.CRTR_YM, m.n_partner, p.n_partner AS prev_partner,
       m.ended, m.started,
       (m.ended - m.started) * 1.0 / NULLIF(p.n_partner,0) AS net_exit
FROM       mm m
LEFT JOIN  mm p
        ON p.BRNO = m.BRNO
       AND p.CRTR_YM = DATE_FORMAT(
             DATE_SUB(STR_TO_DATE(CONCAT(m.CRTR_YM,'01'),'%Y%m%d'), INTERVAL 1 MONTH),'%Y%m');
```

> **PT1 의미 미확인**: 가액비중이 0~1 분수인지 0~100 백분율인지에 따라 HHI 스케일이 100배 달라진다.
> `SELECT MIN(CAST(PT1 AS DECIMAL(12,6))), MAX(...) FROM CRD025` 로 먼저 확인할 것.

## P4 · AGE_LEVEL · AGE_SLOPE (시간고정)

```sql
WITH dbj AS ( /* 【F】 공통 정의 */ )
SELECT ADM_CD, BUSIN_CD,
       SUM(CASE WHEN age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(TRANS_SUM),0)                                       AS age_level,
       SUM(CASE WHEN ym>='202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN ym>='202301' THEN TRANS_SUM ELSE 0 END),0)
       - SUM(CASE WHEN ym <'202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN ym <'202301' THEN TRANS_SUM ELSE 0 END),0) AS age_slope
FROM   dbj GROUP BY ADM_CD, BUSIN_CD;
--  동백전 21개월 전체로 1회 산출 → KODATA 42개월에 행정동×업종 키로 병합
--  age_slope 는 후반−전반 수준차 (엄밀한 기울기 아님, 명칭 주의)
```

---

# 부록 A — 문법 대체표

| 기능 | MySQL | PostgreSQL |
|---|---|---|
| 숫자→문자 | `CAST(x AS CHAR(8))` | `CAST(x AS TEXT)` / `x::text` — **길이 없는 CHAR는 char(1)로 잘림** |
| 문자→정수 | `CAST(x AS SIGNED)` | `CAST(x AS INTEGER)` |
| 문자→소수 | `CAST(x AS DECIMAL(20,2))` | `CAST(x AS NUMERIC(20,2))` |
| 정규식 | `x REGEXP '패턴'` (역슬래시 `\\`) | `x ~ '패턴'` (역슬래시 `\`) |
| 부정 정규식 | `x NOT REGEXP '패턴'` | `x !~ '패턴'` |
| 전월 계산 | `DATE_FORMAT(DATE_SUB(STR_TO_DATE(CONCAT(ym,'01'),'%Y%m%d'), INTERVAL 1 MONTH),'%Y%m')` | `to_char(to_date(ym\|\|'01','YYYYMMDD') - interval '1 month','YYYYMM')` |
| 문자열 연결 | `CONCAT(a,b)` | `a \|\| b` |
| 0 나누기 방지 | `NULLIF(x,0)` | 동일 |
| 제곱 | `POWER(x,2)` | 동일 |
| CTE (`WITH`) | **8.0+** 만 지원 | 지원 |
| 창 함수 (`LAG`) | **8.0+** 만 지원 | 지원 |
| 테이블 목록 | `SHOW TABLES` | `information_schema.tables` |

**Q0-2B로 버전을 먼저 확인할 것.** MySQL 5.x면 모든 `WITH`를 서브쿼리로, `LAG`를 self-join으로 바꿔야 한다 (대체안 병기됨).

---

# 부록 B — 실행 체크리스트

## 1차 방문

| 단계 | 쿼리 | 시간 |
|---|---|---|
| **판정** | Q0-1 · Q0-2B · **Q8-B · Q4-A · Q1-B · Q7-B** | **20분** |
| 확장 | Q1-A·C·E, Q2-A·B, Q3, Q3-B, Q4-B·C | 25분 |
| 자료형 | Q5 · Q5-B · Q5-C, Q7 | 15분 |
| 업종 | Q9-A · Q9-B | 20분 |
| 매핑 | Q6, Q8·Q8-C | 15분 |

**핵심 5개 20분 / 전체 1시간~1시간 30분.** 막히는 요인은 쿼리 실행이 아니라 테이블명·문법 왕복이다.

## 판정 → 경로

```
Q1-B  SPN090만 REL020 법정동과 매칭  ─→ 법정동 확정 ─→ 매핑표 반입 / 구·군 상향
      둘 다 매칭 안 됨                ─→ 비고 오타. 정상 진행

Q4-A  CP_CD=9 + 5명미만 충분 ─→ 계층2 전면
      부족                   ─→ 범위 한정 + 커버리지율을 결론 3층으로 승화

Q7-B  '2'(매입) 충분 ─→ NET_EXIT 진행
      희소           ─→ 계층2를 폐업 예측만으로 축소

Q8-B  60세+ 10% 이상 ─→ AGE 축 진행
      5% 미만        ─→ AGE 폐기, POP014 지역 고령화로 대체
```

---

# 부록 C — 반입 파일 구성 (3개 한도)

```
① 쿼리세트.xlsx
   시트1  Phase0·Phase1 쿼리 (MySQL)
   시트2  Phase1 쿼리 (PostgreSQL 변형)
   시트3  Phase2 파이프라인
   시트4  MySQL 5.x 대체안
   시트5  ★ 부산 법정동-행정동 매핑표   ← Q1-D Plan B 대비, 행안부 공개코드에서 추출
② 라이브러리목록.xlsx
③ 분석설계도.pdf   지표 정의 + 조인 관계도 + 판정 기준
```

**시트5가 v2의 핵심 추가**다. SPN019가 법정동 매핑이 아니라는 것이 확인된 이상, 법정동↔행정동 변환은 **반입으로만 확보 가능**하다. 파일 수는 늘지 않는다.
