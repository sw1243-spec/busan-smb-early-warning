# 오픈랩 3차 방문 준비 — 반입 목록 · 실행 계획 · 코드 이력

> 작성 2026-09-01 밤. **3차 방문(2026-09-02 예정) 직전 상태의 전체 기록.**
> 읽는 순서: `HANDOFF.md` → `오픈랩_1차방문_결과.md` → `오픈랩_2차방문_결과.md` → **이 문서**
> 실행 코드 자체는 `반입파일/④현장코드.xlsx` (= `현장코드_2026-09.md` 동일 내용, 1043줄).

---

# 1. 한눈에

| 항목 | 상태 |
|---|---|
| 현장코드 | **완성.** 18시트 1043줄. 정의서 원문 컬럼명 기준. mock 2회 전 구간 실행 검증 |
| 반입 목록 | **확정.** ④현장코드 + ③분석설계도 + ⑤법정동매핑 (3개, 총 ~89KB) |
| CRD022·023 | 202501~ 결손 확인. **2023/01~ 재추출 요청 예정** (사용자가 직접, 방문 당일 바로 처리된다고 함) |
| SPN082 | **신규 발견.** 생존편향 없는 가맹점 소멸률. 4관점 검증 완료(조건부 타당 3 · 보조지표로만 1). 게이트 A·B0·B1·B2 코드에 내장 |
| 일정 | 마감 9/18 → 반출심사 영업일 5일 역산 → **실질 분석 마감 9/10** |

---

# 2. 반입 신청 목록 (USB)

## 가져갈 것 — 3개

| # | 파일 | 크기 | 역할 |
|:-:|---|---|---|
| 1 | **`반입파일/④현장코드.xlsx`** | ~64KB | **본 작업물.** 셀 범위 복사 → Jupyter 붙여넣기 |
| 2 | `반입파일/③분석설계도.pdf` | 11KB | 반출요청서 산출과정 작성 참고 |
| 3 | `반입파일/⑤법정동매핑.xlsx` | 18KB | S4-1B(REL020 대조) 실패 시 백업. 부산 법정동 254건 |

## 두고 갈 것 — 2개 (반입 금지)

| 파일 | 이유 |
|---|---|
| `①쿼리세트.xlsx` | **컬럼명이 틀린 구버전** (STRD_DATE·PT1 위치·매입 단일컬럼 가정). 현장에서 ④와 충돌하면 사고. 살릴 시트(법정동 254)는 ⑤로 분리 완료 |
| `②라이브러리목록.xlsx` | DuckDB 1.4.5 등 설치 확인 완료. 역할 종료 |

---

# 3. 센터 요청 — CRD022·023 재추출

**2023/06이 아니라 2023/01로 요청한다.** 이미 받은 거래 테이블(CRD024·025·026)이 202301부터라, 06으로 받으면 5개월이 또 빈다. 추가 용량 약 0.5GB.

```
안녕하세요. 데이터 기간 재추출 요청드립니다.

CRD022  기업거래 : 기업정보     현재 2025/01~2026/06  →  요청 2023/01~2026/06
CRD023  기업거래 : 사업장정보   현재 2025/01~2026/06  →  요청 2023/01~2026/06

같은 KODATA 계열인 CRD024(재무)·CRD025(매출매입)·CRD026(전입전출)은
이미 2023/01~2026/06으로 받았습니다.

CRD022·023은 그 거래 데이터에 사업자 속성(개인/법인 구분, 직원수, 사업장 정보)을
연결하는 용도인데, 속성만 2025/01부터라 2023~2024년 거래에 속성을 붙일 수 없습니다.
분석 주제가 폐업이라 이 구간이 빠지면 결과가 편향됩니다.

추가 용량은 두 테이블 합계 약 0.5GB로 예상됩니다
(현재 CRD022 138MB / CRD023 214MB 기준 월평균 환산).

나머지 데이터는 요청대로 전부 수령했습니다. 감사합니다.
```

**반영 여부는 현장에서 S1-0 인벤토리로 30초에 확정된다** (CRD022 첫 파일명이 202301인지 202501인지). 코드는 양쪽 모두 대응 — 손댈 것 없음.

---

# 4. 현장 실행 순서 (④현장코드.xlsx 시트 순서 그대로)

```
S0   부팅 (경로 자동탐색·파일백업DB)          30초
S1   S1-0 인벤토리 → 헤더·구분자·컬럼수       4분
S1-4 REL020 SHP 실체·CRS                     2분
S2   뷰 26개 생성 + ncol 검증                 1분
S3   ★정의서 대조 컬럼 확정 + CRD023 좌표     10분
S4   게이트 (공간키·코드체계·PT1·폐업구분)     9분
S4-6 ★Q8-B 동백전 60세+ 비중                 10분
S5   MPI + S5-B 업종표준화 + S5-C 분해        25분
S6   고객 연령구조 (amt60≥5 일 때만)          15분
S7   S7-0 SPN082 소멸률 → 거래처·커버리지     30분
S8   반출 준비 (블록별 독립 저장)             12분
S9   POP019 대체축 (amt60<5 일 때만)          10분
S10  ★결과 요약 출력 → 99_결과기록 에 옮김     2분
```

## 멈춰서 판단하는 지점 (전부 코드 안 `# --- 판정` 주석에 있음)

| 위치 | 보는 값 | 갈림길 |
|---|---|---|
| S1-0 | CRD022 첫 파일명 | `202301` = 재추출 반영 → S7 본분석 승격 / `202501` = 생존편향 유지 → S7은 한계 근거 |
| S3-4 | `!!MISSING` / `~~FALLBACK` | MISSING 뜨면 그 블록 금지. S3-1 컬럼목록 보고 손으로 `C[..][..]=` 대입 |
| S3-6A | CRD023 `lot_ok/n` | <0.5면 공간조인 포기, 주소 파싱만. 좌표 범위로 좌표계 판정(129/35=WGS84) |
| S4-6 | `age_ok/rows_used` → `amt60` | age_ok<0.98이면 amt60 불신. amt60 ≥15 강 / 5~15 유지 / <5 → S9 / NULL = 파싱전멸 |
| S5 | `buy_cov` | <0.7이면 구군 MPI 순위 반출 보류 (ok/n 으로 판단 금지) |
| S5-C | 상위 구군 `buy_chg` vs `sell_chg` | 매출감소형 구군은 유형 분리 서술 (제목 자기부정 방어) |
| **S7-0 게이트A** | **op_sum·top10_op vs top10_amt·rk_corr·중구(26110) 상위 여부** | **소재지 → 가맹종료율로 채택 / 거주지 → S7-0 폐기(S6 보조로만) / 혼재 → USE_AMT 전면 배제** |
| S7-0 시차상관 | k=-3(위약) vs k=1,3,6 | k=-3도 유의하면 공통추세 → '선행' 주장 접고 상관 관찰로 후퇴 |

## 나올 때 반드시

1. **S10 실행** → 출력을 `99_결과기록` 시트에 손으로 옮기거나 화면 촬영 (노트북 반출 불가)
2. **반출요청서 그날 접수** — 심사 영업일 5일 + 부산시 심의

---

# 5. SPN082 발견과 검증 결과 (2026-09-01 밤)

## 무엇을 발견했나

`SPN082`(행정동별 가맹점 창폐업 카드소비)가 코드에서 정의만 되고 분석에 안 쓰이고 있었다. 내용:

```
CRTR_YM 202301~202606 · ADMNST_CD 행정동 10자리 · INDST_SE_*(카드사 분류)
BZMN_SE 개인/법인 · USE_AMT/CNT · NEW/OPRTNG/CLSB_SHOP_CNT
```

→ **생존편향 없는 폐업 대리지표**(소멸률 = CLSB/OPRTNG)가 행정동×월로 존재. CRD023(202501~, 생존편향)의 대안.

## 4관점 적대 검증 결과 (완료)

**semantics·inference·export = 조건부 타당 / joinability = 보조 지표로만.**
8개 에이전트(4 발견 + 4 반증) 24건 지적 전수 검증 완료. 확정분은 전부 코드에 반영됨.

| 확정된 지적 | 코드 대응 |
|---|---|
| **[BLOCKER] ADMNST_CD 비고 "부산지역민 거주지"** — 거주지면 소멸률이 아니라 '주민의 이용 가맹점 소멸 노출도'. MPI(소재지)와 붙이면 분석단위가 뒤집힌 채 에러 없이 통과 | **게이트 A (정량)**: 한 달치 `op_sum`(10만~25만=소재지 / 100만↑=거주지 중복계상), `top10_op`(≥35 상업지집중 / ≤20 인구비례), `top10_op vs top10_amt` 차 ≥12 또는 `rk_corr≤0.7`이면 **혼재**(→USE_AMT 전면 배제), `per_shop`, **중구(26110) 상위 여부**(상주인구 최소·상권 최대 = 단일 최고 판별자). 거주지 확정 시 S7-0 폐기하되 S6 보조지표('고령 동네일수록 이용 가게 소멸')로 재활용 가능 |
| **[BLOCKER였던 것] S1-0 판정문이 게이트 없이 "SPN082를 주 소스로" 지시** — CRD022 202501이면 그 문장이 그대로 발동 | **수정 완료.** "S7-0 게이트 통과가 전제. 여기서 결정하지 말 것"으로 교체 |
| CLSB는 사업장 폐업이 아니라 **BC카드 가맹계약 종료** (매입사 변경·PG 전환 혼입, 해지처리 지연이 시차 k를 이동시킴) | 용어를 '카드 가맹 종료율(BC카드 기준)'로. CRD023 폐업과의 구군 순위상관 교차검증은 최종계획서 §검증계획에 기존재 |
| **30개 파일 ≠ 42개월** — 기간이 파일명 정규식 추정이었음. 파일 하나에 여러 달일 수도 | **게이트 B0**: `GROUP BY 실제 ym` — 데이터에서 직접 월 커버리지 확인. 42행=결손없음 / 30행=12개월 결손(2023 앞쪽이면 k=6 머리 잘림) |
| OPRTNG 분모 정의 미확인 (기말재고 / 기중재고 / 당월 거래발생) | **게이트 B1**: 재고-플로우 항등식 `OPRTNG(t)≈OPRTNG(t-1)+NEW−CLSB` 잔차 비교(r_end/r_mid/r_none). **진단용 — 자동 폐기 금지**, 해석 각주만 달라짐. + **게이트 B2**: corr(OPRTNG, USE_CNT) |
| **MPI 분모가 매출** — 관계가 매출 하락으로 설명되면 제목("매출은 그대로인데")이 자기부정 | **S5-C 분해**: 구군별 buy_chg vs sell_chg. 매출감소형 구군은 유형 분리 |
| k=6은 12개월 주기의 역위상 — 계절성만으로 그림이 만들어질 수 있음. 공통추세 미배제 | **k=-3 리드 위약검정** 추가. k=-3이 유의하면 선행 주장 포기 |
| 업종 코드가 카드사 체계 — KCD(SPN088~090)와 조인 불가 | 업종 조인 안 함. 전 업종 합계(구군 단위)로만 결합 |
| 셀 단위 정수/정수 비율은 역변환 가능 → 반출 반려 위험 | 구군 16 단위로만 산출·반출. 행정동 단위 반출 금지 |
| 구군 16개 검정력: r≈0.65 이상만 탐지 가능 | 알고 있는 한계. p값은 참고치로 서술 (S8 주석에 기존 반영) |

## 아직 남은 것

- **S5-B 업종표준화(MPI_STD)가 반출지침의 '가중치변환'으로 분류될 소지** — 반출 신청 시 수치 빼고 순위만 내거나, 요청서에 '업종구성 표준화 지수(응용집계)'로 산출과정 명시. 센터 문의 사항
- joinability 관점 판정이 "보조 지표로만"이었다 — 게이트 A가 소재지로 확정되더라도 SPN082는 업종 조인 불가·BC카드 모집단 한정이라는 한계 위의 지표다. 보고서에서 CRD023(협소하나 정밀)과 SPN082(넓으나 대리)를 **상호 교차검증 쌍**으로 서술하는 것이 가장 방어적이다

---

# 6. 현장코드 이력 (2026-09-01 세션에서 한 것)

## 만든 것

`반입파일/④현장코드.xlsx` = `현장코드_2026-09.md` (동일 내용, 폰 참고용)

- 표 4시트: `00_실행순서` `00_판정표` `00_현장규칙` `99_결과기록(빈칸)`
- 코드 14시트: S0~S10 (S2B·S1-4·S4-6 포함), 1043줄
- A열 한 줄씩 · 텍스트 서식 → 셀 범위 복사 시 들여쓰기 보존
- 현장규칙: 백슬래시 금지 / % 포맷 금지 / continue 금지 / 중첩 2단 이내 / SQL 안 빈문자열 비교 금지(`length(trim(x))=0` 사용) / DuckDB 예약어(both·months) 별칭 금지

## 치명적 수정 이력 (다음 세션이 알아야 할 것)

1. **정의서 PDF 재대조로 컬럼명 전면 수정** — 이전 쿼리세트_v3는 컬럼명이 틀렸다:
   - 날짜: KCD=`CRTR_YMD`, KODATA=`CRTR_YM`, 동백전만 `STRD_DATE`
   - SPN089 매출 = `CASH_SLS_AMT`+`TAX_SLS_AMT` (2개), SPN090 매입 = `CASH_PRCH_AMT`+`TAX_PRCH_AMT` (2개)
   - **PT1은 CRD025에 있다** (SPN090 아님) · CRD025 거래처=`DLR_BRNO` · CRD023 좌표=`LOT`/`LAT`
   - 공간키: SPN059 `ADM_CD` char(8) vs KCD `ADMNST_CD` 10자리 → **행정동 직접 조인 불가, 구군(앞5자리)만**
2. **Q8-B 파싱 방어** — 기준일자 파싱 실패 시 amt60이 0.00으로 나와 축을 잘못 폐기하는 결함 수정. age 결측을 분모에서 제외 → 실패 시 NULL. `age_ok/rows_used ≥0.98` 게이트
3. **MPI 결손 방어** — LEFT JOIN+coalesce가 매출 결손을 '매출 0'으로 둔갑시키는 결함. `full2` 플래그 + 금액가중 `buy_cov` 게이트. 구군 집계는 full2=1 셀만
4. **NET_EXIT 생존편향 수정** — 전역 첫달/막달 차분이 이탈자만 정확히 제외하던 결함. 사업자 자기구간 차분 + `gone` 플래그로 교체. gone=0/1별 avg(d_np) 비교가 결론 1층 수치
5. **컬럼명 인용** — need()가 반환값을 큰따옴표로 감쌈 (정의서 표기에 괄호·공백 존재)
6. **MPI×AGE 교차는 겹치는 4개월(202306~202309) 횡단면만** — 전 구간 평균끼리 붙이면 다른 시대 비교
7. S5-B는 비율 단순평균이 아니라 **공통 가중치 직접표준화**
8. S6 분할점 202208(7/14 오류) → **202212(11/10)**, 표본 하한 CUT(median×0.1, None 가드)
9. 커널 사망 대비: `duckdb.connect('D:/lab2026.duckdb')` 파일백업 — 뷰가 재연결 후 생존. S0만 재실행하면 복구

## 검증 방법론 (재현 가능)

- 로컬에 **정의서 원문 컬럼명 그대로의 mock 27폴더** 생성 (`검증도구/make_mock2.py`)
- 함정 7개 심음: SPN090=법정동 / SPN059 8자리 / 특정 월 기준일자 공란 / SPN089 TAX 25% 공란 / CRD022 200명 결손 / CLSB_YMD 코드2만 / SPN092 마지막 파일 헤더 없음 → **전부 코드가 탐지**
- S0~S10 전 14블록 드라이런 0 실패 · xlsx 왕복 재컴파일 0 실패 · 한글 헤더 10/10 해석
- 적대 검증 2회: 현장코드 35건(5관점) + SPN082 24건(4관점), 코드리뷰 15건 중 11건 수정 반영

---

# 7. 남은 리스크 (우선순위)

| # | 리스크 | 대응 |
|:-:|---|---|
| 1 | **Q8-B amt60 < 5** (동백전 고령 이용률) | S9 POP019 대체축 준비됨. 결론 1·2층은 유지 |
| 2 | CRD022·023 재추출 미반영 | S7은 한계 근거로 강등. SPN082(S7-0)가 폐업 축 대체 |
| 3 | S7-0 게이트 A 실패 (거주지 판정) | S7-0 폐기, CRD023만 사용. 나머지 분석 무영향 |
| 4 | S5-B 반출 분류 논란 | 수치 빼고 순위만 반출. 센터 문의 |
| 5 | 반출 심사 지연 | **방문 당일 반출 신청 접수가 마지노선** |

---

# 8. 다음 세션 Claude에게

- 이 문서와 `현장코드_2026-09.md`가 최신이다. **쿼리세트 v1·v2·v3은 전부 컬럼명이 틀린 구버전** — 절대 사용 금지
- **코드 빌더·mock·드라이런 하네스는 `검증도구/`에 있다** (`build_field_code.py`·`make_mock2.py`·`run_dryrun.py`). 코드 수정 절차: ① `검증도구/build_field_code.py`의 S0~S10 리스트를 고친다 → ② `py 검증도구/run_dryrun.py`로 mock 드라이런(0 실패 확인) → ③ `py 검증도구/build_field_code.py`로 ④xlsx와 md 동시 재생성 → ④ xlsx 왕복 재컴파일 확인. md만 고치면 xlsx와 어긋난다 — **빌더가 유일한 원본이다**
- 방문 결과가 들어오면: `99_결과기록` 값 → 이 문서 §4 판단 지점 표와 대조 → 갈림길 확정 → 보고서 작성 단계로
- 작업 규칙(브루탈 크리틱 기본값·입지분석 제외·과장 금지·파일 덮어쓰기 전 확인)은 `CLAUDE.md`·`HANDOFF.md` §0 참조
