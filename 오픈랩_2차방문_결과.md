# 오픈랩 2차 방문 결과 — 파일 구조 확정

> 방문 2026-09-01 · 데이터 폴더 `D:/20260901_진세운`
> **HANDOFF.md → 오픈랩_1차방문_결과.md 다음으로 읽을 것.** 이 문서가 최신 현장 상황이다.
> 이번 방문의 성과는 **적재 파라미터 전량 확정**이다. 분석 쿼리는 아직 하나도 못 돌렸다.

---

# 1. 한눈에

| 항목 | 결과 |
|---|---|
| **재추출 반영** | **거의 전부 반영됨.** 1차 요청 목록이 처리되어 기간이 대폭 확대 |
| 총 용량 | **약 57 GB** (1차 때보다 크게 증가) |
| 인코딩 | **UTF-8** — 전 테이블 공통. CP949 아님 |
| 구분자 | **테이블마다 다름.** PIPE 19개 / COMMA 7개 |
| 헤더 | 각 폴더 첫 파일에 **있음** (2번째 파일 이후는 미확인) |
| 좌표계 | 관측된 좌표는 **WGS84 (EPSG:4326)** — 단, **어느 테이블인지 미확정** (§3-4) |
| 남은 결손 | **CRD022 · CRD023이 여전히 2025/01~** |
| 최대 성과 | **SPN059 21개월 확보 → 부산 특수성 축이 살아남** |
| 최대 미해결 | 게이트 쿼리 6개를 하나도 못 돌림 |

---

# 2. 실측 인벤토리 (2026-09-01 기준)

| 테이블 | 파일 | 실제 범위 | 용량 | 구분자 |
|---|---:|---|---:|:-:|
| CRD020 | 37 | 202306~202606 | 2,129 MB | PIPE |
| CRD021 | 37 | 202306~202606 | 3,878 MB | PIPE |
| **CRD022** | **18** | **202501~202606** | 138 MB | PIPE |
| **CRD023** | **18** | **202501~202606** | 214 MB | PIPE |
| CRD024 | 42 | 202301~202606 | 3,705 MB | PIPE |
| CRD025 | 41 | 202301~202606 | 1,011 MB | PIPE |
| CRD026 | 41 | 202301~202606 | 7 MB | PIPE |
| POP001 | 3 | 2021·2022·2023 (연) | 3 MB | **COMMA** |
| POP014 | 7 | 202108~202510 | 5 MB | **COMMA** |
| POP019 | 42 | 202301~202606 | 537 MB | PIPE |
| POP022 | 42 | 202301~202606 | 407 MB | PIPE |
| REL015 | 51 | 202107~202512 | 28 MB | **COMMA** |
| REL020 | 12 | 202312·202401 | 1,538 MB | (SHP) |
| SPN020 | 1 | 202201 | 0 MB | PIPE |
| SPN057 | 1 | (시점표기 없음) | 0 MB | **COMMA** |
| SPN058 | 20 | 202201~202309 | 4,723 MB | PIPE |
| SPN059 | 20 | 202201~202309 | 14,059 MB | PIPE |
| **SPN060** | 20 | 202201~202309 | **22,222 MB** | **COMMA** |
| SPN082 | 30 | 202301~202606 | 41 MB | PIPE |
| SPN088 | 35 | 202306~202604 | 638 MB | PIPE |
| SPN089 | 35 | 202306~202604 | 70 MB | PIPE |
| SPN090 | 35 | 202306~202604 | 78 MB | PIPE |
| SPN091 | 35 | 202306~202604 | 1,308 MB | PIPE |
| SPN092 | 6 | 202601~202606 | 160 MB | PIPE |
| SPN093 | 1 | 2025 | 0 MB | **COMMA** |
| SPN094 | 1 | 2025 | 0 MB | **COMMA** |
| TOU007 | 3 | 202112·202212·202312 | 36 MB | PIPE |

> **COMMA는 7개뿐**: POP001 · POP014 · REL015 · SPN057 · SPN060 · SPN093 · SPN094
> 나머지 19개는 전부 PIPE.

## 2-1. 위 표를 만든 코드 (재현·검증용)

```python
# 파일 수 · 기간 범위 · 용량
import os, re, glob
base = glob.glob("D:/2026*")[0].replace("\\", "/")
for name in sorted(os.listdir(base)):
    full = base + "/" + name
    if os.path.isdir(full):
        files = sorted(os.listdir(full))
        ym = sorted(set(re.findall(r"(\d{6})", " ".join(files))))
        size = sum(os.path.getsize(full + "/" + x) for x in files) / 1e6
        ext = set(os.path.splitext(x)[1] for x in files)
        print(name, len(files), ym[:1], ym[-1:], round(size), "MB", ext)
```

```python
# 구분자 판정
import os, glob
base = glob.glob("D:/2026*")[0].replace("\\", "/")
for name in sorted(os.listdir(base)):
    full = base + "/" + name
    if os.path.isdir(full):
        c = [x for x in sorted(os.listdir(full)) if x.endswith(".csv")]
        if c:
            head = open(full + "/" + c[0], "rb").readline()
            print(name, "PIPE" if b"|" in head else "COMMA")
```

> ⚠️ **이 판정의 한계**: 각 폴더의 **첫 파일 헤더 1줄**만 보고, **파이프 유무**로만 갈랐다.
> 따라서 `COMMA`는 "콤마다"가 아니라 **"파이프가 아니다"**라는 뜻이다.
> 탭·세미콜론일 가능성이 배제되지 않았다 → §5-1 확인 ②에서 검증할 것.

---

# 3. ★ 적재 파라미터 확정

## 3-1. 인코딩 = UTF-8

바이트 확인 결과 한글이 `\xec` `\xea` `\xeb` 로 시작하는 3바이트 시퀀스 → **UTF-8 확정**.
CP949 아님. DuckDB `read_csv` 기본값 그대로 사용 가능.

인터넷 차단 환경이라 현장에서 인코딩 관련 확장 설치가 불가능하므로, 이 확정은 중요하다.

## 3-2. 구분자는 테이블별로 명시 지정할 것

`read_csv_auto`의 자동 감지에 맡기지 말 것. 자동 감지는 첫 파일 일부만 보고 판단해 가끔 틀린다.

**특히 `SPN060`(22GB, 최대 테이블)이 혼자 COMMA다.** 여기서 `delim='|'`로 잘못 지정하면
전체가 한 컬럼으로 읽히고 **에러 없이 조용히 망가진다.**

## 3-3. 권장 적재 형태

```python
con.execute("""
CREATE OR REPLACE VIEW CRD025 AS
SELECT * FROM read_csv('D:/20260901_진세운/CRD025/*.csv',
  delim='|', header=true, all_varchar=true, filename=true)
""")
```

| 옵션 | 이유 |
|---|---|
| `all_varchar=true` | 일단 전부 문자열로 읽고 필요한 컬럼만 `TRY_CAST`. 타입 추론 실패로 파일이 통째로 안 읽히는 사고를 방지 |
| `filename=true` | 파일명의 `YYYYMM`을 컬럼으로 살림. 기준월이 파일명에만 있는 테이블 대비 |
| **뷰만 생성** | 57GB. 물리 테이블 생성 금지 |

## 3-4. 관측된 데이터 형태

```
콤마형   : "21010510010001",129.036128340,35.102156972,"","4010","편의점","1979",M,3,13500,ZZZZZZ
파이프형 : 202301|2611051000|L01|M001|1003|...|90|39435523|384|0|1
파이프형 : 20230601|2611051000|A|A01|13|171500.00|24
```

| 관측 | 함의 |
|---|---|
| 좌표 `129.03, 35.10` | **WGS84 (EPSG:4326)**. 5179·5186 아님 → REL020 SHP와 붙일 때 변환 필요 |
| `"1979", M` | 연령대 구간이 아니라 **생년 원시값 + 성별**. 연령 구간을 자유롭게 설계 가능 |
| `171500.00` vs `39435523` | 소수점 유무 혼재 → `TRY_CAST(... AS DOUBLE)` 가드 필수 |
| `""` · 연속 파이프 | 빈 값 표현이 두 가지 → NULL 처리 규칙 필요 |

### ⚠️ 위 콤마형 샘플의 출처 테이블은 미확정이다

**단정하지 말 것.** 화면에 여러 폴더의 바이트 덤프가 한꺼번에 출력되어, 이 줄이 어느 폴더 것인지
라벨과 매칭하지 못한 상태로 기록되었다.

확실한 것은 **콤마형이라는 것뿐**이다. 따라서 **CRD 계열은 아니다**(CRD020~026은 전부 PIPE).
출처는 COMMA 7개(POP001·POP014·REL015·SPN057·**SPN060**·SPN093·SPN094) 중 하나다.

| 후보 | 근거 | 반증 |
|---|---|---|
| **SPN060** (집계구 지역화폐, 22GB) | 생년·성별·거래금액(13500)·업종코드(4010)·집계구코드(14자리)·좌표가 모두 지역화폐 거래 스키마에 부합 | — |
| POP001 (집계구 코드 기본정보) | 14자리 집계구 코드·좌표 부합 | **생년·성별·금액이 있을 이유가 없다** |

→ **SPN060으로 추정하나 확정 아니다.** 다음 방문 §5-1 확인 ②에서 함께 판정할 것.

**이것이 중요한 이유**: HANDOFF §3은 `CRD023`에 좌표·한글사업장주소가 있다고 기록한다.
만약 위 좌표가 SPN060(지역화폐)의 것이라면, **CRD023 좌표는 아직 눈으로 확인된 적이 없다.**
공간조인 경로(`CRD023 좌표 → REL020 폴리곤 → 법정동`)가 미검증 상태라는 뜻이다.

## 3-5. 메모리 원칙

**SPN060 22GB + SPN059 14GB = 36GB > RAM 32GB.**

- pandas 로드 **금지**
- DuckDB 스트리밍 집계만 사용
- 최종 집계 결과(작은 것)만 `.df()`로 꺼낼 것

---

# 4. 재추출 반영 현황

1차 방문(8/31) 요청이 하루 만에 처리되어 대부분 반영되었다.

| 테이블 | 1차 수령 | 2차 실측 | 판정 |
|---|---|---|:-:|
| CRD020·021 | 2025/01~ | 202306~202606 | 반영 |
| **CRD022·023** | 2025/01~ | **202501~202606** | **미반영** |
| CRD024·025·026 | 2025/01~ | 202301~202606 | 요청 이상 |
| SPN088~091 | 2026/01~04 | 202306~202604 | 반영 |
| SPN058·059·060 | 2023/01~03 | 202201~202309 | 요청 이상 |
| SPN082 | 2026/01~06 | 202301~202606 | 반영 |
| POP019·022 | 완전 | 202301~202606 | 유지 |
| REL015 | 12개 | 51개 (202107~202512) | 반영 |
| **POP014** | 7개 | **7개 (202108~202510)** | 변화 없음 |
| **TOU007** | **0건** | **3개 (연 단위)** | **해결** |

## 4-1. ★ 부산 특수성 축이 살아났다

1차 방문 시점에는 SPN059가 **3개월**뿐이어서
"현재 3개월로는 부산 특수성 축이 통째로 죽음"이라고 기록했다.

**2차 실측 결과 SPN059는 202201~202309, 20개 파일(21개월).**
`AGE_SLOPE`(전반 11개월 vs 후반 10개월 비교) 산출이 **가능해졌다.**

HANDOFF §10의 "최대 리스크는 동백전 고령층 이용률(Q8-B)"은 여전히 유효하지만,
**기간 부족이라는 리스크는 해소되었다.** 남은 것은 실제 고령층 비중 값 자체다.

## 4-2. 남은 재요청 — CRD022 · CRD023

두 테이블만 여전히 **202501~202606 (18개월)** 이다.

```
CRD022  KODATA 기업거래 : 기업정보     2023/06 ~ 2026/04
CRD023  KODATA 기업거래 : 사업장정보   2023/06 ~ 2026/04
```

**왜 중요한가 — 생존편향이 걸린다**

CRD022는 개인사업자 여부(`CP_CD=9`)·직원수(`JOIN_CNT`)·기업규모를,
CRD023은 사업장 주소·좌표를 담는다. 즉 **사업자 속성 전부**다.

거래 데이터(CRD024·025)는 202301부터 있는데 속성이 202501부터면,
2023~2024년 거래에 속성을 붙일 때 **2025년 스냅샷을 소급 적용**하게 된다.

→ **2023~2024년 사이에 폐업한 사업자는 2025년 스냅샷에 없다.**
→ 폐업을 다루는 주제인데 **폐업한 사업자의 속성이 통째로 누락**된다.

이것은 "보고서에 한계로 명시"하고 넘어갈 수준이 아니라 **분석 설계의 근간을 흔든다.**
재요청이 안 되면 분석 구간을 2025/01~로 잘라야 하며, 그 경우 시차 설계(k=6)가 다시 계산되어야 한다.

---

# 5. 미확인 — 다음 방문 첫 순서

## 5-1. 현장에서만 가능한 확인 (우선순위 순)

| # | 확인 항목 | 왜 필요한가 | 소요 |
|:-:|---|---|:-:|
| **1** | **2번째 파일 이후 헤더 유무** | 헤더 없는데 `header=true`로 읽으면 **매달 첫 행이 조용히 사라짐.** 에러도 안 남 | 30초 |
| **2** | **COMMA 7개의 실제 구분자** | 판정 로직이 "헤더에 파이프 없으면 COMMA"였다. 콤마인지 탭인지 세미콜론인지는 **미검증** | 30초 |
| **3** | **REL020 SHP 실체** | `AL_D150` 명명규칙에서 추정만 함. 컬럼·행수·CRS 미확인 | 1분 |
| 4 | 각 테이블 `COUNT(*)` | 행 수를 모르면 쿼리 소요시간 예측 불가 → 방문 시간 배분 불가 | 수 분 |
| 5 | 같은 테이블 월별 파일의 컬럼 수 일치 | 시기별 스키마 변동 시 glob 읽기 실패 | 1분 |
| **6** | **CRD023에 좌표가 실제로 있는가** | 관측된 좌표는 콤마형이라 CRD가 아니다. **공간조인 경로(CRD023→REL020→법정동)가 미검증** (§3-4) | 30초 |

### 확인 코드

> **자기완결형이다.** 반입 환경은 붙여넣기가 불가능하므로 앞 셀 실행을 전제하지 않는다.
> (HANDOFF §11의 "플레이스홀더를 남기지 말 것" 교훈)

```python
# 0. 공통 — 이 셀을 먼저 실행. 한글 경로를 타이핑하지 않는다
import os, glob
base = glob.glob("D:/2026*")[0].replace("\\", "/")
print(base)
```

```python
# 1. 2번째 파일 헤더 유무
full = base + "/CRD025"
c = [x for x in sorted(os.listdir(full)) if x.endswith(".csv")]
print(open(full + "/" + c[0], "rb").readline()[:100])
print(open(full + "/" + c[1], "rb").readline()[:100])
# 두 줄이 같으면 매 파일 헤더 있음 / 다르면 첫 파일에만

# 2. COMMA 7개 실제 구분자
for t in ["POP001","POP014","REL015","SPN057","SPN060","SPN093","SPN094"]:
    full = base + "/" + t
    c = [x for x in sorted(os.listdir(full)) if x.endswith(".csv")][0]
    print(t, open(full + "/" + c, "rb").readline()[:120])

# 3. REL020 실체
import geopandas as gpd
g = gpd.read_file(base + "/REL020/AL_D150_26_20240125.shp")
print(g.shape, g.crs)
print(g.columns.tolist())
```

## 5-2. 게이트 쿼리 6개 — 하나도 못 돌렸다

`쿼리세트_v3.md`에 **"이 여섯 개를 먼저 돌리지 않으면 나머지가 조용히 틀린다"**
고 명시된 항목들이다. 2차 방문은 파일 구조 파악으로 시간이 전부 소진되었다.

| 게이트 | 확인 대상 | 상태 |
|---|---|:-:|
| Q0-2 | 컬럼 목록 (정의서 대조) | 미실행 |
| Q1-B0 | 코드 길이 분포 | 미실행 |
| Q4-B0 | `INDST_CD_ID` 알파벳/숫자 | 미실행 |
| Q10 | `CLSB_SE_CD` 값 체계 (폐업/휴업 혼재) | 미실행 |
| Q11 | `PT1` 스케일 (0~1 / 0~100) | 미실행 |
| Q8-B | 동백전 60세 이상 비중 | 미실행 |

> Q0-1(테이블 목록)·Q0-2B(DB 버전)는 **파일 기반이므로 불필요**하다.

**→ 집에서 준비할 코드는 "게이트 결과를 먼저 출력하고 그 값에 따라 분기"하는 구조여야 한다.**
> 게이트 결과를 가정하고 본 분석까지 한 번에 짜면, 가정이 틀렸을 때 다음 방문도 통째로 날아간다.
> 남은 방문 기회가 사실상 1~2회다.

---

# 6. 이번 방문에서 실제로 깎인 시간 — 재발 방지

전체 시간의 대부분이 **손타이핑 오타**로 소모되었다. 인터넷 차단이라 코드를 붙여넣을 수 없어
모든 코드를 화면 보고 수(手)입력해야 했다.

| 사고 | 원인 | 다음 방문 대책 |
|---|---|---|
| `"D:\20260901_진세운"` → `D:\x8260901_진세운` | `\2`가 **8진 이스케이프**로 해석됨 | **경로에 백슬래시 금지. 슬래시만** |
| 한글 경로 반복 타이핑 | 폴더명에 한글 | `base = glob.glob("D:/2026*")[0].replace("\\","/")` — **한글 타이핑 제거** |
| `os.pathjoin` / `endswitj` / `if`→`of` / `name`→`na, e` | 손타이핑 | 문자열 이어붙이기(`a + "/" + b`)로 통일, 함수 호출 최소화 |
| `%` 포맷 인자 개수 불일치 | `"%s %d %.0f" % (...)` | **`%` 포맷 금지. `print(a, b, c)` 콤마 나열만** |
| `continue` 아래 들여쓰기 사고 (에러도 출력도 없음) | 조건 분기 | **`continue` 사용 금지. 긍정 조건 `if x:` 로만** |
| 들여쓰기 6칸 등 어긋남 | 손타이핑 | 중첩 **2단계 이내**로 설계 |

## 6-1. 반입 파일 작성 규칙

**반입은 xls·xlsx·pdf·이미지만. `.py` 불가.**

> ⚠️ 파일 개수(3개)에 대해 사용자는 2026-09-01에 **"일단 반입은 가능함"**이라고 확인했다.
> 다만 이것이 **슬롯 재사용/교체를 센터에 확인한 결과인지, 반입 절차 자체가 가능하다는 뜻인지는 불명확**하다.
> 4번째 파일(분석 코드 xlsx)을 들고 들어갈 수 있는지 **재방문 예약 시 함께 확인할 것.**
> 안 되면 `①쿼리세트.xlsx` 안에 코드 시트를 추가해 통합한다.

| 규칙 | 이유 |
|---|---|
| **PDF보다 xlsx** | PDF는 복사 시 들여쓰기·따옴표가 깨진다. xlsx는 셀 복사 → Jupyter 붙여넣기가 깔끔 |
| **한 행에 한 줄** | 셀 하나에 블록을 통째로 넣으면 줄바꿈이 사라진다 |
| **셀 서식을 텍스트로 먼저 지정** | `=`로 시작하는 셀이 수식으로 변환되는 사고 방지 |
| **경로는 자동 탐색** | `glob.glob("D:/2026*")[0]` — 한글 타이핑 원천 제거 |
| **슬래시만 사용** | 백슬래시 이스케이프 사고 방지 |

---

# 7. 확정 사실 요약 (다음 세션이 신뢰해도 되는 표)

| 항목 | 확정값 | 근거 |
|---|---|---|
| 인코딩 | **UTF-8** 전 테이블 | 바이트 실측 (`\xec\xea\xeb`) |
| 구분자 | PIPE 19 / COMMA 7 (§2 표) | 첫 파일 헤더 실측 |
| 헤더 | 첫 파일에 있음 | 바이트 실측 |
| 총 용량 | 약 57 GB | `os.path.getsize` 합산 |
| 최대 테이블 | SPN060 22.2 GB (COMMA) | 실측 |
| 좌표계 | WGS84 (EPSG:4326) | `129.036128340, 35.102156972` — **출처 테이블 미확정**(§3-4) |
| 고객 속성 | **생년 원시값 + 성별(M/F)** | `"1979", M` — 위와 동일 줄. 출처 미확정 |
| 금액 형식 | 소수점 유무 혼재 | `171500.00` / `39435523` |
| 동백전 기간 | 202201~202309 (21개월) | 실측 — **축 부활** |
| 동백전 ∩ KCD | 202306~202309 (**4개월**) | 계산 |
| 동백전 ∩ KODATA | 202301~202309 (**9개월**) | 계산 |
| 사업자 속성 결손 | CRD022·023이 202501~ | 실측 |
| 분석 도구 | DuckDB 1.4.5 · polars 1.36.1 · pandas 2.3.3 · geopandas · sklearn | 화면 확인 |

---

# 8. 다음 행동

| # | 작업 | 주체 | 기한 |
|:-:|---|---|---|
| **1** | **CRD022·023 기간 재요청** (2023/06~) | 센터 연락 | 즉시 |
| **2** | **재방문 예약** | 센터 연락 | 즉시 |
| 3 | 분석 코드 xlsx 작성 (게이트 분기 구조) | 자체 | 방문 전 |
| 4 | 계획서의 "동백전 시계열" 표현 수정 | 자체 | 보고서 작성 시 |
| 5 | 방문 시: §5-1 확인 1~3 → 게이트 6개 → Q8-B | 현장 | 다음 방문 |

## 8-1. 일정 압박

```
2026-09-01  오늘 (2차 방문)
2026-09-18  접수 마감 17:00  ← 완성 보고서
```

반출 심사가 **영업일 5일** + 부산시 등 제공기관 심의 포함이므로,
**실제 분석 완료 마감은 9/10 전후**다. 남은 방문 기회는 사실상 **1~2회**.

---

# 9. 설계상 확정된 손실

| # | 손실 | 영향 | 대응 |
|:-:|---|---|---|
| 1 | 동백전 ∩ KCD = **4개월** | 동백전×카드매출 **시계열 결합 불가** | 횡단면 1회 비교로 축소. 계획서 문구 수정 |
| 2 | CRD022·023 202501~ | **생존편향** — 폐업자 속성 누락 | 재요청. 실패 시 분석 구간 축소 + 시차 재설계 |
| 3 | POP014 7개 파일 | 인구 통제변수 시점 부족 | 재요청 목록에 포함 |
| 4 | 게이트 6개 미실행 | 본 분석 설계가 가정 위에 있음 | 코드를 **분기 구조**로 작성 |
