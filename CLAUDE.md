# 2026 부산 Big Data 활용 대회 — 프로젝트 지침

> 이 파일은 Claude Code가 세션 시작 시 자동으로 읽는다.
> **상세 맥락은 `HANDOFF.md`에 있다. 작업 시작 전 반드시 읽을 것.**

---

## 작업 규칙 (사용자 지정)

1. **브루탈 크리틱이 기본값이다.** 요청이 없어도 비판적으로 진행한다. 장점보다 **깨질 지점을 먼저** 말한다.
2. **검증 없이 새 방향을 제안하지 않는다.** 제안 시 "무엇이 확인되면 살고 죽는지" 게이트를 함께 건다.
3. **억지로 단점을 만들지 않는다.** 객관적으로 본다. 과장했으면 명시적으로 정정한다.
4. **입지분석(최적 입지 선정)은 제외.** 사용자가 명시적으로 배제했다.
5. 파일을 쓰기 전 **같은 이름 파일이 있는지 먼저 확인**한다.
6. 데이터 컬럼을 언급할 때는 **데이터명이 아니라 `데이터 반출 관련 오픈랩/3.데이터_정의서.pdf` 원문**을 근거로 한다.

---

## 프로젝트 한 줄

**「매출은 그대로인데 왜 문을 닫는가」** — 매입(원가)·거래관계·고객 연령구조로 부산 소상공인 위기를 조기 포착하고, 위기 유형별 지원 프로그램의 존재 여부를 데이터로 검증한다.

| | |
|---|---|
| 접수 마감 | **2026-09-18(금) 17:00** — 완성 보고서 제출 |
| 제출물 | 시각화 1장 PDF · 보고서(HWPX+PDF, 10P 이하) · 활용데이터 → 압축 1개 |
| 팀 | 2명 |
| 현재 평가 | **8.6 / 10** |

---

## 파일 인덱스

### ★ 현행 — 이것만 사용

| 파일 | 역할 |
|---|---|
| `HANDOFF.md` | **최우선.** 전체 맥락·검증사실·폐기목록·다음행동 |
| `오픈랩_1차방문_결과.md` · `오픈랩_2차방문_결과.md` | **2·3번째로 읽을 것.** 실측 인벤토리·적재 파라미터 확정 |
| `오픈랩_3차방문_준비.md` | 4번째. 반입목록·SPN082 검증·코드 이력·판단 지점 |
| **`오픈랩_3차방문_결과.md`** | **5번째. 최신 상태.** 반출물 9개 해석·판정 대조·죽은 축과 새 발견·열린 질문 |
| `0_부산 데이터 오픈랩 09.03. 반출 신청 데이터_260904/` | **★반출물 원본 9개.** 보고서·시각화의 유일한 데이터 소스 |
| `현장코드_2026-09.md` | **★현장 실행코드 (1043줄).** `반입파일/④현장코드.xlsx` 와 동일 내용. 폰 참고용 |
| `최종계획서_수정반영본.md` | 본 계획서. 조인키 지도 · 데이터 문제 8건 · 논리 문제 4건 |
| `오픈랩_신청목록.md` · `data/오픈랩_신청목록_25건.csv` | 신청 25건 |
| `반입파일/` (xlsx 3 + pdf 1) | 방문 시 USB 반입. **④현장코드.xlsx 가 실제 작업용** |
| `선행연구_중복검토.md` | RISS·KCI·DBpia 검토 결과 |
| `검증도구/` (py 3개) | **④현장코드 재생성 원본.** build_field_code.py 수정 → run_dryrun.py 검증 → 재빌드 |
| `.claude/agents/` (3개) | **전담 에이전트.** report-architect(뼈대) · report-reviewer(심사위원 검수) · logic-validator(독립 검증, 서사 문서 접근 금지) |
| `데이터 반출 관련 오픈랩/` (PDF 4) | **원본 근거.** 정의서·반출지침 |
| `2026년 Big Data 활용 대회 작성 서식.hwpx` | 제출 서식 원본 |

### ⚠️ 구버전 — 사용 금지 (버그 있음)

| 파일 | 사유 |
|---|---|
| `쿼리세트.md` (v1) · `쿼리세트_v2.md` | 플레이스홀더·자료형 버그 |
| **`쿼리세트_v3.md`** | **컬럼명이 틀렸다.** `STRD_DATE`·`PT1(SPN090)`·매입금액 단일컬럼 가정 등. **`현장코드_2026-09.md` 사용** |
| `최종계획서.md` | **수정반영본 사용** |
| `기획서_보강안.md` · `부산_소상공인_마진압박_조기경보_기획서.md` | 중간 산출물. 수정반영본에 흡수됨 |
| `문제해결안_1_2_3.md` | 대부분 v3에 반영됨. SPN019 관련 서술은 **틀렸음**(아래 참조) |
| `오픈랩_진행계획.md` · `오픈랩_통화_질문지.md` | 초기 문서. 관리자 답변으로 대체됨 |

---

## 재제안 금지 — 이미 죽은 것

**7번 시도해 7번 실패했다.**

| 폐기 | 사인 |
|---|---|
| 불법주정차 "신고 안 된 위반" 찾기 | 실제 위반량 준거 데이터 없음 |
| 고령화 × 민원 신고율 | 구군 상관 **r = +0.001** |
| 과태료 부과율 지역 격차 | 6개 구 중 5개 **부과 100%** |
| 신고→단속 대응시간 | 26,419건 중 **유효 360건** |
| 교통카드 고령자 이동 | **경로우대 카테고리 없음** |
| 15분 도시 생활권 | 산출물이 입지분석 |
| SPN019로 법정동→행정동 변환 | **SPN019에 법정동 컬럼 없음** |

---

## 검증 완료 — 재확인 불필요

정의서 원문 대조 완료. 신뢰해도 된다.

```
CRD025.DATA_SE_SLS_PRCHS_CD = 1:매출 2:매입
CRD022.CP_CD = 9:개인 (개인사업자 식별 가능) · JOIN_CNT(직원수) 있음
CRD023 = BRNO 없음 (IDCD·ENT_CD만) → CRD022 경유 필수
CRD023.KOR_BZPLC_ADDR = 한글사업장주소 있음 (좌표만 있는 게 아님)
SPN019 = 법정동 컬럼 없음. 행정동↔시군구 매핑이다
REL020.A1 = 법정동코드(10). 반출 불가하나 내부 대조용 사용 가능
SPN089·SPN090 금액 = varchar(50) → 형변환 가드 필수
CRD026.SE_GB = 1:전입 2:전출 0:부산내이동
SPN059/060 = 생년·성별 있음
동백전(SPN058/059/060)만 원본 반출 가능. KCD·KODATA는 XXXOO
REL020 = SHP 파일이다 (이전 "오픈랩에 없음" 판단은 오류. 정정됨)
부산 고령인구비율 25.3% — 광역시 1위 (KOSIS DT_1YL20631)
```

### ★ 정의서 원문 컬럼명 (2026-09-01 PDF 재대조. 이전 문서들이 틀렸다)

```
KCD 날짜   = CRTR_YMD   (STRD_DATE 아님)
KODATA 날짜 = CRTR_YM (6자리)
동백전 날짜 = STRD_DATE (8자리)  ← 유일하게 STRD_DATE 다

SPN088 매출 = CARD_SLS_AMT                        (1개)
SPN089 매출 = CASH_SLS_AMT + TAX_SLS_AMT          (2개! 하나만 쓰면 절반 소실)
SPN090 매입 = CASH_PRCH_AMT + TAX_PRCH_AMT        (2개!)
업종        = INDST_SE_LCLS_CD / INDST_SE_MCLS_CD (2개. 대분류 사용)

PT1(가액비중) = CRD025 에 있다. SPN090 이 아니다
CRD025 거래처 = DLR_BRNO · 사업자 = BRNO
CRD023 폐업   = CLSB_SE_CD + CLSB_YMD  ← 일자가 채워진 코드만 진짜 폐업
POP019       = ADMNST_INST_CD(행정기관코드) · AGE_GRP · HAB_PPLTN
TOU007       = YYYYMM_BASE · ADM_CD(8) · EMP_NUM_CLASS · BUSINESS_NUM
SPN057       = ADMNST_CD(8) ↔ ADMNST_INST_CD10(10) 브리지

★ 공간키 자릿수가 다르다
  SPN059 ADM_CD = char(8)   /  SPN088~090 ADMNST_CD = 10자리
  → 행정동 직접 조인 불가. 앞 5자리(구군)에서만 결합한다
★ SPN090 비고만 "법정동 코드 10자리" (088/089/091은 행정동) → 게이트 S4-1로 판정
```

---

## 오픈랩 제약

| | |
|---|---|
| 반출 가능 | 응용집계(비율·순위·증감율·지수·모델결과·검정값) · 시각화 |
| 반출 불가 | 원본 · 발췌 · **단순집계(z-정규화·MIN-MAX·가중치변환 포함)** |
| 범주 원칙 | **10개 내외** → 행정동(205) 위험, 구·군(16) 안전 |
| 심사 | 영업일 5일 + 부산시 심의 |
| 반입 | **xls·xlsx·pdf·이미지만. 3개, 30MB.** `.sql`·`.py` 불가 |
| 환경 | **인터넷 차단** · Python 3.9 · RAM 32GB · 작업물 90일 보관 · 재방문 가능 |

---

## 진행 상황 (2026-09-06 · 반출 완료, 보고서 단계)

- ✅ 이용신청 25건 · 1차(8/31)·2차(9/1) 방문 · 재추출 반영 57GB 수령
- ✅ **현장코드 완성** (④현장코드.xlsx, 1043줄) — mock 검증·적대검증 2회·코드리뷰 반영
- ✅ 반입 목록 확정: ④현장코드 + ③분석설계도 + ⑤법정동매핑 (①②는 두고 감)
- ✅ 3차 방문(9/3) · 반출 심사 통과 · 반출물 9개 수령(9/4) ← `오픈랩_3차방문_결과.md`
- ✅ 축 이동 확정(9/6): "고령 상권 신진대사 정지 = 지표 사각지대" · **정체 상권 5곳(중·서·영도·사상·사하)**
- ✅ **`제출자료/` 패키지 완성(9/6, 27파일)** — 초안 v2 · 수치사전(블라인드 검증 64/64 일치) · 각주/표/용어 방화벽 · 시각화 4종 · 활용데이터 11개 · KOSIS 근거(2025년 25.3% 원문 확인). **팀원이 HWPX 직접 집필**
- ⏳ 잔여: KOSIS 웹 원문 스크린샷 저장(4_근거) · **웨이브 가점(붙임4-2): 데이터 2건 다운로드+브라이텍스 캡처** ← `제출자료/4_근거/빅데이터웨이브_가점_가이드.md` · HWPX 집필·최종 검증 · 압축 1개 제출(9/18 17:00)

## 현장에서 바뀐 것 (중요)

```
데이터는 CSV다 (DB 아님) → SQL 대신 DuckDB 사용
  FROM SPN090  →  FROM 'D:/20260901_진세운/SPN090/*.csv'

REL020은 SHP 파일이다 → 좌표→법정동 공간조인 가능
  (이전 "행정동 경계 SHP 없음" 판단은 오류. 정정됨)

SPN057은 원본이 2023.07 한 달치 → 정상. 재요청 불필요
TOU007 빈 폴더 원인 = 요청 시작월이 원본 범위 밖. 기간 고치면 나옴
분석 구간을 2023/06~2026/04 (23개월)로 통일
```

---

## 최대 리스크 (반출 후 갱신)

~~동백전 60세+ 비중~~ → 19.76%로 해소. ~~게이트 A~~ → 사진 판독(9/6)으로 해소, 축 이동 채택 완료.
현재 최대 리스크는 **HWPX 집필 중 용어·수치 이탈** — `제출자료/2_보고서_재료/`의
`용어_방화벽.md`·`수치사전.md`를 벗어난 표현·숫자가 원고에 들어가는 것.
발견쌍(-0.612/-0.550)은 탐색적 표기(G3 문구) 필수. 원가압박 축은 지지되지 않음으로 확정(3.1에 정면 기술).
