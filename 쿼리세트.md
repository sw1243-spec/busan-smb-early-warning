# 오픈랩 쿼리 세트

> 부산 소상공인 마진압박 조기경보 · 2026-08-28
> **반입 시 xlsx로 변환** (`.sql` 반입 불가 / xls·xlsx·pdf·이미지 3개 30MB)

---

## 사용 규칙

| | |
|---|---|
| **실행 순서** | Phase 0 → Phase 1 → (판정) → Phase 2 |
| **DB 엔진 미확인** | 문법 차이 나는 구문은 【M】MySQL / 【P】PostgreSQL 병기. **인터넷 차단이므로 현장 검색 불가** |
| **테이블명 미확인** | TRN001은 `TRN001_2022_1Q`처럼 분기 분할. **KCD·KODATA 분할 여부 미확인** → Phase 0 Q0-1로 먼저 확인 |
| **대용량 주의** | `DISTINCT`를 먼저 뽑은 뒤 조인. 원테이블 직접 조인 금지 |

---

# Phase 0 — 환경·스키마 확인 (10분)

```sql
-- Q0-1  테이블 목록 (분할 여부·실제 명칭 확인)
--【M】
SHOW TABLES;
--【P】
SELECT table_name FROM information_schema.tables WHERE table_schema='public' ORDER BY 1;
```

```sql
-- Q0-2  컬럼 목록 (정의서와 실제 일치 확인)
--【M】
SHOW COLUMNS FROM SPN090;
--【P】
SELECT column_name, data_type FROM information_schema.columns
WHERE  table_name='spn090' ORDER BY ordinal_position;
```

```sql
-- Q0-3  주요 테이블 건수 (작업량 산정)
SELECT 'SPN088' AS t, COUNT(*) AS n FROM SPN088
UNION ALL SELECT 'SPN089', COUNT(*) FROM SPN089
UNION ALL SELECT 'SPN090', COUNT(*) FROM SPN090
UNION ALL SELECT 'SPN091', COUNT(*) FROM SPN091
UNION ALL SELECT 'CRD022', COUNT(*) FROM CRD022
UNION ALL SELECT 'CRD023', COUNT(*) FROM CRD023
UNION ALL SELECT 'CRD024', COUNT(*) FROM CRD024
UNION ALL SELECT 'CRD025', COUNT(*) FROM CRD025
UNION ALL SELECT 'CRD026', COUNT(*) FROM CRD026
UNION ALL SELECT 'SPN059', COUNT(*) FROM SPN059
UNION ALL SELECT 'SPN060', COUNT(*) FROM SPN060;
```

```sql
-- Q0-4  기간 범위 (정의서 표기와 실제 일치 확인)
SELECT 'SPN090' AS t, MIN(CRTR_YMD) AS d1, MAX(CRTR_YMD) AS d2 FROM SPN090
UNION ALL SELECT 'CRD025', MIN(CRTR_YM), MAX(CRTR_YM) FROM CRD025
UNION ALL SELECT 'SPN059', MIN(STRD_DATE), MAX(STRD_DATE) FROM SPN059;
```

---

# Phase 1 — 판정 쿼리

> **판정 결과에 따라 Phase 2의 경로가 갈린다. 반드시 순서대로.**

## 【A】공간 키 — 조인 성립 여부

### Q1-A ★최우선 · SPN090 코드 체계

정의서에서 SPN090만 컬럼명은 `행정동코드(10자리)`인데 **비고는 `법정동 코드 10자리`** 로 자기모순. 개수로 판별한다.

```sql
SELECT 'SPN088_매출'   AS src, COUNT(DISTINCT ADMNST_CD) AS code_n FROM SPN088
UNION ALL SELECT 'SPN089_현금매출', COUNT(DISTINCT ADMNST_CD)       FROM SPN089
UNION ALL SELECT 'SPN090_매입',     COUNT(DISTINCT ADMNST_CD)       FROM SPN090
UNION ALL SELECT 'SPN091_메뉴',     COUNT(DISTINCT ADMNST_CD)       FROM SPN091;
```

| 결과 | 판정 |
|---|---|
| 넷 다 **200 안팎** | 비고 오타. **정상 진행** |
| SPN090만 **300 이상** | 실제 법정동 → Q1-C(SPN019 경유) |

### Q1-B · 집합 차이 (개수가 같아도 확인)

```sql
--【M/P 공통】
WITH s AS (SELECT DISTINCT ADMNST_CD AS cd FROM SPN088),
     p AS (SELECT DISTINCT ADMNST_CD AS cd FROM SPN090)
SELECT
  (SELECT COUNT(*) FROM s LEFT JOIN p ON s.cd=p.cd WHERE p.cd IS NULL) AS only_sales,
  (SELECT COUNT(*) FROM p LEFT JOIN s ON p.cd=s.cd WHERE s.cd IS NULL) AS only_prch;
```

양쪽 차집합 모두 **10 미만**이면 동일 체계.

### Q1-C · Plan B — SPN019 매핑 구조

```sql
SELECT * FROM SPN019 LIMIT 50;
--  행정동·법정동 코드가 실제로 양쪽 다 있는지 확인
--  (데이터명은 "행정동-법정동 매핑"이나 컬럼 목록에 법정동 표기 불명확)
```

### Q2-A · CRD023 주소 형식 (파싱 규칙 확정)

```sql
SELECT KOR_BZPLC_ADDR
FROM   CRD023
WHERE  KOR_BZPLC_ADDR IS NOT NULL AND KOR_BZPLC_ADDR <> ''
LIMIT  200;
--  지번주소 / 도로명주소 / "부산광역시" 시작 여부 / 구·동 토큰 일관성 확인
```

### Q2-B · CRD023 주소·좌표 결측률

```sql
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR='' THEN 1 ELSE 0 END) AS addr_null,
       SUM(CASE WHEN LAT IS NULL OR LAT='' THEN 1 ELSE 0 END)                        AS coord_null,
       SUM(CASE WHEN (KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR='')
                 AND (LAT IS NULL OR LAT='') THEN 1 ELSE 0 END)                      AS both_null
FROM   CRD023;
```

`both_null`이 큰 비중이면 그만큼은 지역 매핑 불가 → 분석 대상에서 제외하고 규모 명시.

### Q6 · SPN057 매핑 성공률 (KCD 10자리 ↔ 동백전 8자리)

```sql
WITH d AS (SELECT DISTINCT ADM_CD FROM SPN059)
SELECT COUNT(*) AS spn059_dong,
       SUM(CASE WHEN m.ADMNST_CD IS NOT NULL THEN 1 ELSE 0 END) AS mapped
FROM   d LEFT JOIN SPN057 m ON d.ADM_CD = m.ADMNST_CD;
```

실패 시 자릿수 정규화 후 재시도 → 그래도 실패면 행정동명 텍스트 매칭.

---

## 【B】업종 키 — 3축 조인의 두 번째 관문 ★신규

> **수정반영본에 공간 키 지도는 있으나 업종 키 지도가 없다.**
> 3축은 `행정동 × 업종`으로 붙는다. 행정동이 맞아도 **업종이 안 맞으면 조인이 성립하지 않는다.**

| 축 | 업종 컬럼 | 분류 체계 |
|---|---|---|
| KCD (SPN088~091) | `INDST_SE_LCLS_CD` / `INDST_SE_MCLS_CD` | **KCD 분류** (SPN094) |
| 동백전 (SPN059/060) | `BUSIN_CD` / `BUSIN_NM` | **BC카드 분류** (SPN093) |
| KODATA (CRD022/023) | `INDST_CD_ID` / `TP_BIZ_NM`(텍스트) | **산업분류 or 자체코드** |

**세 체계가 전부 다르다. 표준 매핑 테이블이 인벤토리에 없다.**

### Q9-A · 각 축 업종 코드 개수·값

```sql
SELECT 'KCD_대' AS ax, COUNT(DISTINCT INDST_SE_LCLS_CD) AS n FROM SPN088
UNION ALL SELECT 'KCD_중',  COUNT(DISTINCT INDST_SE_MCLS_CD) FROM SPN088
UNION ALL SELECT 'BC_업종', COUNT(DISTINCT BUSIN_CD)         FROM SPN059
UNION ALL SELECT 'KODATA',  COUNT(DISTINCT INDST_CD_ID)      FROM CRD022;
```

### Q9-B · 업종명 텍스트 대조 (수동 매핑표 작성용)

```sql
-- KCD 업종 목록
SELECT * FROM SPN094;

-- BC카드 업종 목록
SELECT * FROM SPN093;

-- KODATA 업종명 상위값
SELECT TP_BIZ_NM, COUNT(*) AS n
FROM   CRD023
GROUP  BY TP_BIZ_NM
ORDER  BY n DESC
LIMIT  100;
```

**대응 방침**: 코드 매핑이 불가하면 **대분류 수준 수동 매핑**(음식·소매·개인서비스 등 10개 내외)으로 후퇴하고 **매핑률을 보고**한다. 대분류는 반출지침의 "범주 10개 내외" 원칙과도 부합한다.

---

## 【C】식별 키 — 계층2 조인

### Q2 · CRD022 브리지 무결성

```sql
SELECT COUNT(*)               AS rows,
       COUNT(DISTINCT BRNO)   AS brno_n,
       COUNT(DISTINCT IDCD)   AS idcd_n,
       COUNT(DISTINCT ENT_CD) AS entcd_n
FROM   CRD022;
```

`rows > idcd_n` 이면 1기업 : N행(기준연월 반복 등) → 최신 시점만 뽑는 로직 필요.

### Q3 · CRD025 → CRD022 매칭률

```sql
WITH a AS (SELECT DISTINCT BRNO FROM CRD025)
SELECT COUNT(*) AS crd025_brno,
       SUM(CASE WHEN b.BRNO IS NOT NULL THEN 1 ELSE 0 END) AS matched
FROM   a LEFT JOIN (SELECT DISTINCT BRNO FROM CRD022) b ON a.BRNO=b.BRNO;
```

| 결과 | 판정 |
|---|---|
| 80% 이상 | 계층2 진행 |
| 80% 미만 | 매칭 집합으로 한정. 미매칭분은 **L2로 보고** |

### Q3-B · CRD022 → CRD023 매칭률 (IDCD 경유)

```sql
WITH a AS (SELECT DISTINCT IDCD FROM CRD022)
SELECT COUNT(*) AS crd022_idcd,
       SUM(CASE WHEN c.IDCD IS NOT NULL THEN 1 ELSE 0 END) AS matched
FROM   a LEFT JOIN (SELECT DISTINCT IDCD FROM CRD023) c ON a.IDCD=c.IDCD;
```

**CRD025 → CRD022 → CRD023 전체 경로가 뚫려야 NET_EXIT ↔ 폐업 연결이 성립한다.**

---

## 【D】커버리지 — 결론 3층 근거

### Q4-A ★ 소상공인 기준 커버리지

```sql
SELECT CP_CD,
       ENP_SZE,
       CASE WHEN JOIN_CNT IS NULL THEN '미상'
            WHEN JOIN_CNT < 5     THEN '5명미만'
            WHEN JOIN_CNT < 10    THEN '5-9명'
            ELSE                       '10명이상' END AS size_band,
       COUNT(DISTINCT IDCD) AS ent_n,
       COUNT(DISTINCT BRNO) AS brno_n
FROM   CRD022
GROUP  BY 1,2,3
ORDER  BY ent_n DESC;
--  CP_CD   1:일반법인 2:공공기관 3:비영리법인 8:기타법인 9:개인
--  ENP_SZE 1:대기업 2:중소기업 3:중견기업 0:대상아님
```

> **개인사업자 ≠ 소상공인.** 소상공인은 상시근로자 기준
> (제조·건설·운수·광업 10명 미만 / 그 외 5명 미만)

### Q4-B · 업종 대분류 × 규모 (업종별 기준 적용)

```sql
--【M】SUBSTR   【P】SUBSTRING  — 둘 다 SUBSTR 동작
SELECT SUBSTR(INDST_CD_ID,1,1) AS industry_1,
       CASE WHEN JOIN_CNT < 5 THEN '5미만'
            WHEN JOIN_CNT < 10 THEN '5-9'
            ELSE '10이상' END AS size_band,
       COUNT(DISTINCT IDCD) AS n
FROM   CRD022
GROUP  BY 1,2
ORDER  BY 1,2;
```

### Q4-C · L1 / L2 계산

```sql
WITH sb AS (            -- 소상공인 기준 충족 집합 A
  SELECT DISTINCT BRNO
  FROM   CRD022
  WHERE  JOIN_CNT IS NOT NULL AND JOIN_CNT < 10
),
tx AS (SELECT DISTINCT BRNO FROM CRD025)   -- 거래 관측 집합 B
SELECT
  (SELECT COUNT(*) FROM sb)                                        AS A_명부,
  (SELECT COUNT(*) FROM sb JOIN tx ON sb.BRNO=tx.BRNO)             AS L1_완전관측,
  (SELECT COUNT(*) FROM sb LEFT JOIN tx ON sb.BRNO=tx.BRNO
     WHERE tx.BRNO IS NULL)                                        AS L2_부분관측;
--  L3 미관측 = N(외부 모집단 통계) − A_명부
```

---

## 【E】자료형·유효성

### Q5 ★ varchar 금액 값 패턴 (조용히 틀리는 문제)

```sql
SELECT CASH_PRCH_AMT, TAX_PRCH_AMT
FROM   SPN090
WHERE  CASH_PRCH_AMT IS NOT NULL
LIMIT  200;
--  콤마 / 공백 / 마스킹문자(*, -, N/A) 존재 여부를 눈으로 확인
```

```sql
-- Q5-B  숫자 변환 불가 행 규모
--【M】
SELECT COUNT(*) AS bad
FROM   SPN090
WHERE  CASH_PRCH_AMT REGEXP '[^0-9.]';
--【P】
SELECT COUNT(*) AS bad
FROM   SPN090
WHERE  CASH_PRCH_AMT ~ '[^0-9.]';
```

```sql
-- Q5-C  정제 후 분포 비교 (정제 전후 m 왜곡 확인)
--【M】
SELECT MIN(CAST(REPLACE(CASH_PRCH_AMT,',','') AS DECIMAL(20,2))) AS mn,
       MAX(CAST(REPLACE(CASH_PRCH_AMT,',','') AS DECIMAL(20,2))) AS mx,
       AVG(CAST(REPLACE(CASH_PRCH_AMT,',','') AS DECIMAL(20,2))) AS av
FROM   SPN090
WHERE  CASH_PRCH_AMT REGEXP '^[0-9,.]+$';
--【P】CAST(REPLACE(...) AS NUMERIC)  /  WHERE ... ~ '^[0-9,.]+$'
```

### Q7 · CRD025 거래 시작·종료일 유효성 (NET_EXIT 성립)

```sql
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>'' THEN 1 ELSE 0 END) AS has_start,
       SUM(CASE WHEN VAT_END_YMD  IS NOT NULL AND VAT_END_YMD <>'' THEN 1 ELSE 0 END) AS has_end,
       COUNT(DISTINCT DATA_SE_SLS_PRCHS_CD) AS se_kinds
FROM   CRD025;
```

```sql
-- Q7-B  매출/매입 구분 실재 확인
SELECT DATA_SE_SLS_PRCHS_CD, COUNT(*) AS n, COUNT(DISTINCT BRNO) AS biz
FROM   CRD025
GROUP  BY DATA_SE_SLS_PRCHS_CD;
--  1:매출  2:매입   → 2가 충분히 있어야 NET_EXIT 가능
```

---

## 【F】부산 특수성 축

### Q8 ★ 동백전 고객 연령 분포 (AGE 축 생사)

```sql
--【M】
SELECT CAST(SUBSTR(CAST(STRD_DATE AS CHAR),1,4) AS SIGNED) - CAST(BIRTH_YEAR AS SIGNED) AS age,
       SUM(TRANS_CNT) AS cnt, SUM(TRANS_SUM) AS amt
FROM   SPN059
WHERE  BIRTH_YEAR IS NOT NULL AND BIRTH_YEAR <> ''
GROUP  BY 1 ORDER BY 1;
--【P】CAST(SUBSTR(STRD_DATE::text,1,4) AS INTEGER) - BIRTH_YEAR::INTEGER
```

```sql
-- Q8-B  60세 이상 비중 (판정용 요약)
--【M】
SELECT SUM(CASE WHEN yr - CAST(BIRTH_YEAR AS SIGNED) >= 60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(TRANS_SUM),0) AS age_dep_60
FROM   (SELECT CAST(SUBSTR(CAST(STRD_DATE AS CHAR),1,4) AS SIGNED) AS yr,
               BIRTH_YEAR, TRANS_SUM
        FROM SPN059 WHERE BIRTH_YEAR IS NOT NULL AND BIRTH_YEAR<>'') t;
```

| 결과 | 판정 |
|---|---|
| 60세 이상 **10% 이상** | AGE 축 진행 |
| **5% 미만** | AGE 폐기 → 부산 특수성을 **POP014·POP019 지역 인구 고령화**로 대체 |

### Q8-C · AGE 순위 안정성 (외삽 정당화)

```sql
SELECT ADM_CD, BUSIN_CD,
       SUM(CASE WHEN half='H1' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN half='H1' THEN TRANS_SUM ELSE 0 END),0) AS dep_h1,
       SUM(CASE WHEN half='H2' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN half='H2' THEN TRANS_SUM ELSE 0 END),0) AS dep_h2
FROM  (SELECT ADM_CD, BUSIN_CD, TRANS_SUM,
              CAST(SUBSTR(CAST(STRD_DATE AS CHAR),1,4) AS SIGNED)
                - CAST(BIRTH_YEAR AS SIGNED) AS age,
              CASE WHEN CAST(STRD_DATE AS CHAR) < '20230101' THEN 'H1' ELSE 'H2' END AS half
       FROM SPN059 WHERE BIRTH_YEAR IS NOT NULL AND BIRTH_YEAR<>'') t
GROUP BY ADM_CD, BUSIN_CD;
--  결과를 내보내 Spearman 순위상관 계산 → ρ > 0.7 이면 외삽 정당화
```

---

# Phase 2 — 파이프라인 (판정 통과 후)

## P1 · MPI 재료 — 행정동 × 업종 × 월

```sql
-- 매출 3종 합산
--【M】SUBSTR(CRTR_YMD,1,6)  【P】동일
WITH sales AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CARD_SLS_AMT) AS card_amt
  FROM   SPN088 GROUP BY 1,2,3
),
cash AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CAST(REPLACE(CASH_SLS_AMT,',','') AS DECIMAL(20,2))) AS cash_amt,
         SUM(CAST(REPLACE(TAX_SLS_AMT ,',','') AS DECIMAL(20,2))) AS tax_amt
  FROM   SPN089 GROUP BY 1,2,3
),
prch AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CAST(REPLACE(CASH_PRCH_AMT,',','') AS DECIMAL(20,2))) AS cash_prch,
         SUM(CAST(REPLACE(TAX_PRCH_AMT ,',','') AS DECIMAL(20,2))) AS tax_prch
  FROM   SPN090 GROUP BY 1,2,3
)
SELECT s.ym, s.ADMNST_CD, s.ind,
       (COALESCE(s.card_amt,0)+COALESCE(c.cash_amt,0)+COALESCE(c.tax_amt,0))   AS S_total,
       (COALESCE(p.cash_prch,0)+COALESCE(p.tax_prch,0))                        AS M_total,
       (COALESCE(p.cash_prch,0)+COALESCE(p.tax_prch,0))
         / NULLIF(COALESCE(s.card_amt,0)+COALESCE(c.cash_amt,0)+COALESCE(c.tax_amt,0),0) AS m_ratio
FROM       sales s
LEFT JOIN  cash  c ON s.ym=c.ym AND s.ADMNST_CD=c.ADMNST_CD AND s.ind=c.ind
LEFT JOIN  prch  p ON s.ym=p.ym AND s.ADMNST_CD=p.ADMNST_CD AND s.ind=p.ind;
```

> **Q5 결과에 따라 `REPLACE` 정제 규칙을 수정할 것.** 마스킹 문자가 있으면 조건 추가.

## P1-B · 가격 전가 계수 π 재료

```sql
SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
       SUM(MENU_SLS_AMT) / NULLIF(SUM(MENU_SLS_CNT),0) AS avg_price
FROM   SPN091
GROUP  BY 1,2,3;
--  월별 평균 단가 → Δ가격 산출 → 패널회귀 Δ가격 = α + π·Δm + ε
```

## P2 · 종속변수 — 폐업 (이전 제외)

```sql
SELECT c.IDCD,
       c.CLSB_SE_CD, c.CLSB_YMD,
       CASE WHEN c.CLSB_SE_CD IS NOT NULL AND c.CLSB_SE_CD <> ''
             AND e.IDCD IS NULL
            THEN 1 ELSE 0 END AS closed_flag
FROM        CRD023 c
LEFT JOIN  (SELECT DISTINCT IDCD FROM CRD026) e ON c.IDCD = e.IDCD;
--  CRD026(전입전출)에 있으면 "이전"이므로 폐업에서 제외
--  ※ CRD026의 식별키가 BRNO/IDCD 중 무엇인지 Q0-2로 먼저 확인
```

## P3 · NET_EXIT · HHI — 사업자 × 월

```sql
WITH mm AS (
  SELECT BRNO, CRTR_YM,
         COUNT(DISTINCT DLR_BRNO) AS n_partner,
         SUM(CASE WHEN VAT_END_YMD  IS NOT NULL AND VAT_END_YMD <>''
                   AND SUBSTR(VAT_END_YMD,1,6)  = CRTR_YM THEN 1 ELSE 0 END) AS ended,
         SUM(CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>''
                   AND SUBSTR(VAT_STRT_YMD,1,6) = CRTR_YM THEN 1 ELSE 0 END) AS started
  FROM   CRD025
  WHERE  DATA_SE_SLS_PRCHS_CD = '2'          -- 매입만
  GROUP  BY BRNO, CRTR_YM
),
hh AS (
  SELECT BRNO, CRTR_YM,
         SUM(POWER(CAST(PT1 AS DECIMAL(10,6)),2)) AS hhi
  FROM   CRD025
  WHERE  DATA_SE_SLS_PRCHS_CD = '2'
  GROUP  BY BRNO, CRTR_YM
)
SELECT m.BRNO, m.CRTR_YM, m.n_partner, m.ended, m.started,
       (m.ended - m.started) * 1.0 / NULLIF(m.n_partner,0) AS net_exit,
       h.hhi
FROM       mm m
LEFT JOIN  hh h ON m.BRNO=h.BRNO AND m.CRTR_YM=h.CRTR_YM
ORDER BY   m.BRNO, m.CRTR_YM;
```

> `PT1`(가액비중)이 varchar이므로 **CAST 전 Q5식 값 점검 필요.**
> 분모는 원 설계상 "전월 매입처 수"이나, 위는 당월 기준 근사. 정확한 전월 참조는 창 함수 사용:
> `LAG(n_partner) OVER (PARTITION BY BRNO ORDER BY CRTR_YM)` — 【M】MySQL 8+ 지원

## P4 · AGE_LEVEL · AGE_SLOPE — 행정동 × 업종 (시간고정)

```sql
SELECT ADM_CD, BUSIN_CD,
       SUM(CASE WHEN age>=60 THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(TRANS_SUM),0)                                AS age_level,
       SUM(CASE WHEN age>=60 AND ym>='202301' THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN ym>='202301' THEN TRANS_SUM ELSE 0 END),0)
       - SUM(CASE WHEN age>=60 AND ym< '202301' THEN TRANS_SUM ELSE 0 END)
         / NULLIF(SUM(CASE WHEN ym< '202301' THEN TRANS_SUM ELSE 0 END),0)
                                                                    AS age_slope
FROM  (SELECT ADM_CD, BUSIN_CD, TRANS_SUM,
              SUBSTR(CAST(STRD_DATE AS CHAR),1,6) AS ym,
              CAST(SUBSTR(CAST(STRD_DATE AS CHAR),1,4) AS SIGNED)
                - CAST(BIRTH_YEAR AS SIGNED) AS age
       FROM SPN059 WHERE BIRTH_YEAR IS NOT NULL AND BIRTH_YEAR<>'') t
GROUP BY ADM_CD, BUSIN_CD;
--  동백전 21개월 전체로 1회 산출 → KODATA 42개월에 행정동×업종 키로 병합
```

---

# 부록 A — 문법 대체표

| 기능 | MySQL | PostgreSQL |
|---|---|---|
| 문자열 자르기 | `SUBSTR(s,1,4)` | `SUBSTR(s,1,4)` (동일) |
| 숫자→문자 | `CAST(x AS CHAR)` | `x::text` / `CAST(x AS TEXT)` |
| 문자→정수 | `CAST(x AS SIGNED)` | `CAST(x AS INTEGER)` / `x::int` |
| 문자→소수 | `CAST(x AS DECIMAL(20,2))` | `CAST(x AS NUMERIC(20,2))` |
| 정규식 매칭 | `x REGEXP '패턴'` | `x ~ '패턴'` |
| 0 나누기 방지 | `NULLIF(x,0)` | 동일 |
| NULL 대체 | `COALESCE(x,0)` | 동일 |
| 제곱 | `POWER(x,2)` | 동일 |
| 테이블 목록 | `SHOW TABLES` | `information_schema.tables` |
| 컬럼 목록 | `SHOW COLUMNS FROM t` | `information_schema.columns` |
| 창 함수 | MySQL **8.0+** 만 지원 | 지원 |
| 문자열 연결 | `CONCAT(a,b)` | `a \|\| b` / `CONCAT` |

---

# 부록 B — 실행 체크리스트

| 순서 | 쿼리 | 소요 | 판정 대상 |
|:-:|---|---|---|
| 1 | Q0-1~4 | 10분 | 테이블명·건수·기간 |
| 2 | **Q1-A** | 5분 | **SPN090 코드 체계** |
| 3 | Q1-B / Q1-C | 10분 | 집합 차이 / Plan B |
| 4 | Q2-A·B | 10분 | CRD023 주소 파싱 가능성 |
| 5 | **Q9-A·B** | 20분 | **업종 키 3축 대조** ★신규 |
| 6 | Q2·Q3·Q3-B | 15분 | 식별 키 경로 |
| 7 | **Q4-A·B·C** | 20분 | **커버리지 = 결론 3층** |
| 8 | **Q5·Q5-B·C** | 15분 | **varchar 금액 정제규칙** |
| 9 | Q7·Q7-B | 10분 | NET_EXIT 성립 |
| 10 | **Q8·Q8-B** | 15분 | **AGE 축 생사** |
| 11 | Q6 / Q8-C | 15분 | 매핑률 / 외삽 정당화 |

**총 약 2시간 30분.** 오전에 끝내고 오후에 Phase 2 착수.

---

# 부록 C — 판정 → 경로 요약

```
Q1-A  200 안팎  ──→ 정상 진행
      300 이상  ──→ Q1-C(SPN019 매핑) ──→ 실패 시 구·군 상향

Q9    업종 코드 매핑 가능 ──→ 중업종 단위
      불가                ──→ 대분류 10개 수동 매핑 (반출지침에도 부합)

Q3    80% 이상 ──→ 계층2 전면
      80% 미만 ──→ 매칭집합 한정 + 미매칭을 L2로 보고

Q4    소상공인 충분 ──→ 계층2 전면
      부족          ──→ 범위 한정 + 커버리지율을 결론 3층으로 승화

Q5    숫자만    ──→ 그대로 CAST
      문자 혼입 ──→ 정제규칙 확정 후 P1 수정

Q8    60세+ 10% 이상 ──→ AGE 축 진행
      5% 미만        ──→ AGE 폐기, POP014·POP019로 대체
```
