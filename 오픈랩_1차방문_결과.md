# 오픈랩 1차 방문 결과 및 추가 요청

> 방문 2026-08-31 · 데이터 폴더 `D:\20260901_진세운`
> **HANDOFF.md 다음으로 읽을 것.** 이 문서가 최신 현장 상황이다.

---

# 1. 한눈에

| 항목 | 결과 |
|---|---|
| 데이터 형식 | **CSV 파일** (DB 아님) — 폴더 27개, 파일 272개 |
| 수령 상태 | **확인용 일부만.** 기간을 지정하면 재추출해 줌 |
| 빈 폴더 | **TOU007 하나** — 원인 규명됨 (아래 3-2) |
| 최대 문제 | 축 간 기간이 안 겹침 → **재요청으로 해결 가능** |
| 다음 행동 | **아래 5장 요청 목록을 센터에 전달** |

---

# 2. 받은 데이터 현황

## 2-1. 폴더 27개

```
CRD020 CRD021 CRD022 CRD023 CRD024 CRD025 CRD026
POP001 POP014 POP019 POP022
REL015 REL020
SPN020 SPN057 SPN058 SPN059 SPN060 SPN082
SPN088 SPN089 SPN090 SPN091 SPN092 SPN093 SPN094
TOU007
```

신청 25건 + SPN092·SPN093(추가 제공) = 27개. **누락 없음.**

## 2-2. 신청 기간 vs 실제 수령

`요청데이터.txt`의 체크(✓) 뒤 날짜 = **그 시점부터 일부만 받은 것**

| 코드 | 신청 | 실제 수령 | 상태 |
|---|---|---|:-:|
| CRD020~026 | 2023/01~2026/06 | **2025/01~** (18개월) | 부분 |
| SPN088~091 | 2023/06~2026/04 | **2026/01~04** (4개월) | 부분 |
| SPN058·059·060 | 2022/01~2023/09 | **2023/01~03** (3개월) | 부분 |
| SPN082 | 2023/01~2026/06 | 2026/01~06 (6개월) | 부분 |
| POP014 | 2021/08~2025/08 | 7개 파일 | 확인 필요 |
| REL015 | 2021/07~2025/12 | 12개 파일 | 확인 필요 |
| POP019·POP022 | 2023/01~2026/06 | 42개월 | **완전** |
| POP001 | 2021/01~2023/12 | 3년치 | **완전** |
| REL020 | 2022/08~2024/01 | 2시점 | **완전** |
| SPN020·057·093·094 | 각 1시점~1년 | 전량 | **완전** |
| TOU007 | 2021/12~2023/12 | **0건** | 빈 폴더 |

---

# 3. 현장에서 확인된 사실

## 3-1. 데이터는 CSV다 (DB 아님)

관리자가 앞서 "DB 테이블 제공"이라 한 것은 **TRN001(교통카드)에 대한 답**이었다. 우리가 신청한 25건은 **CSV 파일**로 제공된다.

→ **SQL을 그대로 못 쓴다.** 대응은 6-1 참조.

## 3-2. TOU007이 빈 이유 — 제공 불가가 아니다

```
TOU007 원본 보유 기간 : 2021/12 ~ 2023/12
요청한 시작월          : 2026/01   ← 범위 밖
결과                  : 0건
```

**기간만 고쳐 재요청하면 나온다.**

## 3-3. REL020은 SHP 파일이다 ★

`dbf · shp · shx · prj · fix` = **Shapefile 세트.** CSV가 아니라 공간 데이터다.

이전 판단(“행정동 경계 SHP는 오픈랩에 없다”)을 **정정한다.** REL020을 반출등급만 보고 형식을 확인하지 않은 오류였다.

**이것이 열어주는 경로**

```
CRD023 좌표(위경도) ─공간조인→ REL020 필지 → 법정동코드 → SPN057 → 행정동
```

주소 텍스트 파싱에만 의존하지 않아도 된다. `geopandas`가 라이브러리 목록에 있어 사용 가능.

**확인할 것**: `prj` 파일의 좌표계. 보통 `EPSG:5186`(중부원점) 또는 `5179`. CRD023 위경도(`EPSG:4326`)와 다르면 변환 필요.

```python
import geopandas as gpd
g = gpd.read_file(r"D:\20260901_진세운\REL020\...shp", encoding="cp949")
print(g.crs, len(g))
print(g.columns.tolist())
```

## 3-4. SPN057은 1개월이 정상

원본 자체가 **2023년 7월 한 달치**다. 데이터 목록표 기준일자 `202307~202307`로 확인됨.

행정동 코드 매핑표는 시점이 아니라 **대조표**라 한 시점이면 전 기간에 쓴다. **재요청 불필요.**

---

# 4. 이전 판단 중 정정하는 것

| # | 이전 판단 | 정정 |
|:-:|---|---|
| 1 | "핵심 데이터 5개가 안 나왔다" | **틀림.** 폴더 27개 모두 존재. 진단 스크립트가 파일명의 `\d{6}` 패턴으로만 묶어 월 표기 없는 폴더가 목록에서 빠진 것 |
| 2 | "행정동 경계 SHP는 오픈랩에 없다" | **틀림.** REL020이 SHP로 제공됨 |
| 3 | "POP014·REL015는 정상 수령" | **부정확.** 파일 수가 적어 확인 필요. 전 기간 요청에 포함 |
| 4 | "TOU007 제공 불가일 수 있다" | **틀림.** 요청 기간이 원본 범위 밖이었던 것 |

---

# 5. 추가 요청 목록 (복사용)

```
데이터 기간을 아래와 같이 지정하여 추가 요청드립니다.
분석 구간을 2023/06~2026/04로 통일하여 기존 신청 대비 기간을 줄였습니다.

■ 필수 (전 기간 필요)

SPN088  KCD 사업장_카드매출              2023/06 ~ 2026/04
SPN089  KCD 사업장_현금매출              2023/06 ~ 2026/04
SPN090  KCD 사업장_매입                 2023/06 ~ 2026/04
SPN091  KCD 사업장_메뉴                 2023/06 ~ 2026/04
SPN059  행정동단위 지역화폐 거래 통계      2022/01 ~ 2023/09

■ 핵심 분석 (기존 신청 2023/01~2026/06 → 축소)

CRD025  KODATA 기업거래 : 매출매입       2023/06 ~ 2026/04
CRD022  KODATA 기업거래 : 기업정보       2023/06 ~ 2026/04
CRD023  KODATA 기업거래 : 사업장정보     2023/06 ~ 2026/04
CRD024  KODATA 기업거래 : 재무정보       2023/06 ~ 2026/04
CRD026  KODATA 전입 전출 기업 목록       2023/06 ~ 2026/04

■ 통제변수

POP019  KT 행정동별 성연령별 생활인구     2023/06 ~ 2026/04
POP022  KT 행정동별 시간대별 생활인구     2023/06 ~ 2026/04
CRD020  NICE평가정보 소득통계           2023/06 ~ 2026/04
CRD021  NICE평가정보 대출 및 연체        2023/06 ~ 2026/01
REL015  지역별 전월세 금액 면적 평균      2023/06 ~ 2025/12
SPN082  BC카드 행정동별 가맹점 창폐업     2023/06 ~ 2026/04

■ 축소 요청

SPN060  집계구단위 지역화폐 거래 통계     2023/01 ~ 2023/09
SPN058  전통시장별 지역화폐 거래 통계     2023/01 ~ 2023/09
POP014  지역별 인구 합계                2023/01 ~ 2025/08

■ 재요청 (현재 빈 폴더)

TOU007  지역 업종 규모 사업체 합계       2023/01 ~ 2023/12
        ※ 앞서 시작월을 2026/01로 요청드렸는데, 원본 보유 기간이
          2021/12~2023/12라 범위 밖이어서 0건이 된 것으로 보입니다.

■ 추가 요청 불필요 (수령 완료)

POP001  집계구 코드 기본정보
REL020  월별 공시지가 데이터 (SHP)
SPN020  BC카드 업종 분류 코드
SPN057  부산광역시 행정동·행정기관·행정구역 코드 매핑
SPN093  BC카드 업종코드
SPN094  KCD 업종 분류
```

---

# 6. 기간 산정 근거

## 6-1. 분석 구간을 2023/06~2026/04 (23개월) 로 통일한 이유

KCD 원본 범위와 정확히 일치한다. 여기에 KODATA를 맞추면 **42개월 → 23개월**로 줄면서 두 축이 완전히 겹친다.

빠지는 구간은 KODATA의 2023/01~05와 2026/05~06인데, **KCD가 202306부터라 그 기간엔 원가 지표 자체가 없어 쓸 수 없다.** 분석 손실 없음.

## 6-2. 데이터별 시간 요구가 다르다

| 유형 | 필요한 것 | 기간 |
|---|---|---|
| **추세** (KCD·SPN059) | 전년 대비·전후반 비교 | 길게 — 못 줄임 |
| **핵심 분석** (KODATA) | 시차 k=6 + 시간분할 | 23개월이면 학습 17 + 검증 6 |
| **통제변수** | 분석 구간 전체 커버 | 구간과 동일. 자르면 관측치가 통째로 빠짐 |
| **수준·분포** (SPN060·058) | 한 시점의 모습 | 9개월이면 충분 |
| **기준표** (코드·모집단) | 대조용 | 1시점~1년 |

## 6-3. 못 줄이는 것의 이유

- **KCD 23개월**: `Δm`이 전년동월 대비라 13개월 + 이동평균 2개월이 최소. 23개월이면 Δm을 11시점 계산. 자르면 시점이 급감
- **SPN059 21개월**: `AGE_SLOPE`가 전반/후반 비교. 21개월이면 전반 11 + 후반 10. 현재 3개월로는 **부산 특수성 축이 통째로 죽음**

## 6-4. SPN060을 9개월로 줄인 근거

`AGE`는 SPN059(행정동)로 산출한다. SPN060(집계구)은 **지도 시각화용 보조**다. 집계구는 부산에 수천 개라 행정동(205)의 수십 배 행이 나오므로 절감폭이 크다. 반출지침의 "범주 10개 내외" 원칙상 집계구 단위 결과는 어차피 반출이 어렵다.

---

# 7. 설계 변경 사항

## 7-1. SQL → DuckDB

데이터가 CSV이므로 `쿼리세트_v3.md`의 SQL을 그대로 못 쓴다. **DuckDB를 쓰면 거의 그대로 살아난다.**

```sql
FROM SPN090          →    FROM 'D:/20260901_진세운/SPN090/*.csv'
```

DuckDB는 CSV를 SQL로 직접 조회하고 와일드카드로 여러 파일을 한 번에 읽는다. 라이브러리 목록에 이미 포함되어 있다.

## 7-2. 방문 전 확인해야 할 3가지 (아직 미확인)

```python
import os, glob

D = r"D:\20260901_진세운"
print(f"{'폴더':<10} {'파일':>5} {'MB':>9}   헤더")
print("=" * 100)
for name in sorted(os.listdir(D)):
    d = os.path.join(D, name)
    if not os.path.isdir(d): continue
    files = [f for f in glob.glob(os.path.join(d, "**", "*.*"), recursive=True)
             if os.path.isfile(f)]
    mb = sum(os.path.getsize(f) for f in files) / 1024 / 1024
    head, enc_used = "", ""
    if files:
        f0 = sorted(files)[0]
        for enc in ("utf-8-sig", "cp949", "utf-8"):
            try:
                with open(f0, encoding=enc) as fh: head = fh.readline().strip()
                enc_used = enc; break
            except Exception: continue
    flag = "  ← 비었음" if not files else ""
    print(f"{name:<10} {len(files):>5} {mb:>9.1f}   [{enc_used}] {head[:90]}{flag}")
```

| 확인 | 왜 중요한가 |
|---|---|
| **헤더가 한글인가 영문인가** | 한글이면 v3 쿼리의 컬럼명을 전부 바꿔야 함 |
| **인코딩** | `cp949`면 DuckDB 읽을 때 지정 필요 |
| **파일 크기** | 수백 MB면 pandas 금지, DuckDB 필수 |

---

# 8. 다음 행동

| # | 작업 | 상태 |
|:-:|---|:-:|
| 1 | **5장 요청 목록을 센터에 전달** | 대기 |
| 2 | REL020 SHP 좌표계 확인 (`prj`) | 미확인 |
| 3 | CSV 헤더 한글/영문 확인 (7-2 코드) | 미확인 |
| 4 | 재수령 후 `쿼리세트_v3.md`를 DuckDB용으로 전환 | 대기 |
| 5 | W1 판정 쿼리 실행 (Q1-B·Q4-A·Q7-B·Q8-B·Q10·Q11) | 대기 |

**3번은 재요청과 무관하게 지금 할 수 있다.** 헤더가 한글이면 쿼리 전면 수정이 필요하므로 미리 알아야 한다.
