# 오픈랩 쿼리 세트 v3 (최종)

> 2026-08-28 · **모든 쿼리가 복사·붙여넣기 즉시 실행 가능**
> 반입: xlsx 변환 (`.sql` 불가 / xls·xlsx·pdf·이미지 **3개** 30MB)

---

## v2에서 고친 것

| # | v2 문제 | v3 조치 |
|:-:|---|---|
| 1 | `WITH dbj AS ( /* 위 정의 */ )` 4곳 — **복붙 시 문법 오류** | 전 쿼리에 **완전 인라인**. 반복은 의도적 |
| 2 | `AND b.ym = /* 전월 */` — 실행 불가 | 실제 식으로 교체 (M/P 병기) |
| 3 | MySQL 5.x 대체안이 `mm` CTE 참조 — **5.x엔 CTE가 없어 그 대체안도 실패** | 임시테이블 방식 + 서브쿼리 중복 방식 병기 |
| 4 | Q1-B가 길이·공백 정규화 없이 `IN` 비교 | `TRIM` + `JOIN`, **길이 분포 사전 확인(Q1-B0) 추가** |
| 5 | Q2 브리지 진단 `rows > idcd_n * month_n` — **논리 오류** | 1:N 관계를 직접 검사로 교체 |
| 6 | 정규식 `'\\.'` — `NO_BACKSLASH_ESCAPES` 모드에서 깨짐 | `'[.]'` 문자클래스로 교체 (M/P 동일 동작) |
| 7 | P2가 CRD023 월별 스냅샷 중복 미처리 | **최신 CRTR_YM 고정** |
| 8 | `CLSB_SE_CD` 값 체계 미확인 — 휴업이 폐업으로 잡힐 위험 | **Q10 신규** |
| 9 | `PT1` 스케일 미확인 (0~1인지 0~100인지) | **Q11 신규** |
| 10 | Q4-C의 `IN ('B','C','F','H')`가 숫자코드면 **조용히 전부 false** | 형식 가드 + Q4-B0 사전 확인 |
| 11 | P3에서 `mm`과 `hh`의 행 집합 불일치 | 제외율 동시 보고 |
| 12 | Q7-B 판정에 수치 기준 없음 | 기준 명시 |

---

# 실행 의존성

```
Q0-1 (테이블명) ─→ 모든 쿼리의 테이블명 확정
Q0-2B (DB버전) ─→ CTE·LAG 사용 가능 여부 → 5.x면 대체안 사용
Q1-B0 (코드길이) ─→ Q1-B 비교 전 정규화 필요 여부
Q4-B0 (업종코드 형식) ─→ Q4-C의 업종 조건 확정
Q10 (폐업코드 값) ─→ P2의 폐업 판정식 확정
Q11 (PT1 스케일) ─→ P3의 HHI 해석
```

**이 여섯 개를 먼저 돌리지 않으면 나머지가 조용히 틀린다.**

---

# Phase 0 — 환경 (10분)

```sql
-- Q0-1 ★ 테이블 목록
--【M】
SHOW TABLES;
--【P】
SELECT table_name FROM information_schema.tables
WHERE table_schema='public' ORDER BY 1;
```

```sql
-- Q0-2 컬럼 목록 (정의서 대조)
--【M】
SHOW COLUMNS FROM SPN090;
--【P】
SELECT column_name, data_type FROM information_schema.columns
WHERE table_name='spn090' ORDER BY ordinal_position;
```

```sql
-- Q0-2B ★ DB 버전 — CTE·창함수 가능 여부 결정
--【M】
SELECT VERSION();
--【P】
SELECT version();
--  MySQL 8.0 미만이면 WITH·LAG 불가 → 각 쿼리의 【5.x】 블록 사용
```

```sql
-- Q0-3 건수  ※ 대용량이면 느림. 오래 걸리면 중단하고 Q0-4로
SELECT 'SPN088' AS t, COUNT(*) AS n FROM SPN088
UNION ALL SELECT 'SPN089', COUNT(*) FROM SPN089
UNION ALL SELECT 'SPN090', COUNT(*) FROM SPN090
UNION ALL SELECT 'SPN091', COUNT(*) FROM SPN091
UNION ALL SELECT 'CRD022', COUNT(*) FROM CRD022
UNION ALL SELECT 'CRD023', COUNT(*) FROM CRD023
UNION ALL SELECT 'CRD025', COUNT(*) FROM CRD025
UNION ALL SELECT 'CRD026', COUNT(*) FROM CRD026
UNION ALL SELECT 'SPN059', COUNT(*) FROM SPN059
UNION ALL SELECT 'TOU007', COUNT(*) FROM TOU007;
```

```sql
-- Q0-4 기간 범위  ※ 전부 문자 캐스팅 (UNION 타입 충돌 방지)
--【M】
SELECT 'SPN090' AS t, CAST(MIN(CRTR_YMD) AS CHAR(8)) AS d1, CAST(MAX(CRTR_YMD) AS CHAR(8)) AS d2 FROM SPN090
UNION ALL SELECT 'CRD025', CAST(MIN(CRTR_YM) AS CHAR(8)),   CAST(MAX(CRTR_YM) AS CHAR(8))   FROM CRD025
UNION ALL SELECT 'SPN059', CAST(MIN(STRD_DATE) AS CHAR(8)), CAST(MAX(STRD_DATE) AS CHAR(8)) FROM SPN059;
--【P】CAST(... AS TEXT)   ※ 길이 없는 CHAR는 PostgreSQL에서 char(1)로 잘림
```

---

# Phase 1 — 판정

## ★ 핵심 6개 (25분)

| 순 | 쿼리 | 결정 |
|:-:|---|---|
| 1 | Q0-1 · Q0-2B | 테이블명 · 문법 분기 |
| 2 | **Q8-B** | 부산 특수성 축 생사 |
| 3 | **Q4-A** | "소상공인" 제목 성립 · 결론 3층 |
| 4 | **Q1-B0 → Q1-B** | MPI 성립 여부 |
| 5 | **Q7-B** | NET_EXIT 성립 여부 |
| 6 | **Q10** | 폐업 종속변수 오염 여부 |

---

## 【A】공간 키

### Q1-B0 ★ 코드 길이 분포 (비교 전 정규화 확인)

```sql
--【M/P 공통】※ MySQL은 LENGTH, PostgreSQL도 LENGTH 동작
SELECT 'SPN090' AS t, LENGTH(TRIM(ADMNST_CD)) AS len, COUNT(DISTINCT ADMNST_CD) AS n
FROM   SPN090 GROUP BY 2
UNION ALL
SELECT 'SPN088', LENGTH(TRIM(ADMNST_CD)), COUNT(DISTINCT ADMNST_CD) FROM SPN088 GROUP BY 2
UNION ALL
SELECT 'REL020', LENGTH(TRIM(A1)),        COUNT(DISTINCT A1)        FROM REL020 GROUP BY 2
UNION ALL
SELECT 'SPN057', LENGTH(TRIM(ADMNST_INST_CD10)), COUNT(DISTINCT ADMNST_INST_CD10) FROM SPN057 GROUP BY 2;
```

**길이가 다르면 Q1-B의 비교가 전부 0으로 나온다.** 다르면 `LPAD`로 맞추고 진행.

### Q1-A 코드 개수 (1차 신호)

```sql
SELECT 'SPN088_매출' AS src, COUNT(DISTINCT ADMNST_CD) AS code_n FROM SPN088
UNION ALL SELECT 'SPN089_현금매출', COUNT(DISTINCT ADMNST_CD) FROM SPN089
UNION ALL SELECT 'SPN090_매입',     COUNT(DISTINCT ADMNST_CD) FROM SPN090
UNION ALL SELECT 'SPN091_메뉴',     COUNT(DISTINCT ADMNST_CD) FROM SPN091;
--  부산 행정동 약 205 / 법정동 300+
```

### Q1-B ★결정적 · REL020 법정동코드 직접 대조

```sql
--【M/P 공통】※ TRIM + JOIN 으로 교체 (v2의 IN 비교는 길이 차이에 취약)
SELECT
  (SELECT COUNT(*) FROM (SELECT DISTINCT TRIM(ADMNST_CD) AS cd FROM SPN090) s
     JOIN (SELECT DISTINCT TRIM(A1) AS cd FROM REL020) r ON s.cd=r.cd)  AS spn090_in_bjd,
  (SELECT COUNT(DISTINCT TRIM(ADMNST_CD)) FROM SPN090)                   AS spn090_total,
  (SELECT COUNT(*) FROM (SELECT DISTINCT TRIM(ADMNST_CD) AS cd FROM SPN088) s
     JOIN (SELECT DISTINCT TRIM(A1) AS cd FROM REL020) r ON s.cd=r.cd)  AS spn088_in_bjd,
  (SELECT COUNT(DISTINCT TRIM(ADMNST_CD)) FROM SPN088)                   AS spn088_total;
```

| 결과 | 판정 |
|---|---|
| SPN090 매칭률 높고 SPN088 낮음 | **법정동 확정** → Q1-D |
| 둘 다 낮음 | 비고 오타. **정상 진행** |
| 둘 다 높음 | 체계가 겹침 → Q1-C |

### Q1-C SPN057 행정기관코드 대조 (보조)

```sql
SELECT
  (SELECT COUNT(*) FROM (SELECT DISTINCT TRIM(ADMNST_CD) AS cd FROM SPN090) s
     JOIN (SELECT DISTINCT TRIM(ADMNST_INST_CD10) AS cd FROM SPN057) m ON s.cd=m.cd) AS spn090_in_adm,
  (SELECT COUNT(*) FROM (SELECT DISTINCT TRIM(ADMNST_CD) AS cd FROM SPN088) s
     JOIN (SELECT DISTINCT TRIM(ADMNST_INST_CD10) AS cd FROM SPN057) m ON s.cd=m.cd) AS spn088_in_adm;
```

### Q1-D Plan B — 법정동 → 행정동

> **SPN019는 법정동 컬럼이 없어 사용 불가.** 확인 완료.

| 순위 | 방법 |
|:-:|---|
| 1 | **법정동-행정동 매핑표 반입** (쿼리세트.xlsx 시트5) |
| 2 | POP001(지번주소·집계구) → POP014(집계구↔동코드) |
| 3 | **구·군 상향** — 코드 앞 5자리는 두 체계 공통 |

```sql
-- Q1-E  POP014 동 코드 체계 (경로 2 가능성)
SELECT COUNT(DISTINCT TOWN_VAL) AS town_n,
       COUNT(DISTINCT TZ_AREA_VAL) AS zone_n
FROM   POP014;
--  town_n 205 안팎 = 행정동 / 300+ = 법정동
```

### Q2-A CRD023 주소 형식

```sql
SELECT KOR_BZPLC_ADDR
FROM   CRD023
WHERE  KOR_BZPLC_ADDR IS NOT NULL AND KOR_BZPLC_ADDR <> ''
LIMIT  200;
```

### Q2-B 주소·좌표 결측률

```sql
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR='' THEN 1 ELSE 0 END) AS addr_null,
       SUM(CASE WHEN LAT IS NULL OR LAT='' THEN 1 ELSE 0 END)                        AS coord_null,
       SUM(CASE WHEN (KOR_BZPLC_ADDR IS NULL OR KOR_BZPLC_ADDR='')
                 AND (LAT IS NULL OR LAT='') THEN 1 ELSE 0 END)                      AS both_null
FROM   CRD023;
```

### Q6 SPN057 매핑 성공률

```sql
--【M8+/P】
SELECT COUNT(*) AS spn059_dong,
       SUM(CASE WHEN m.cd IS NOT NULL THEN 1 ELSE 0 END) AS mapped
FROM  (SELECT DISTINCT TRIM(ADM_CD) AS cd FROM SPN059) d
LEFT JOIN (SELECT DISTINCT TRIM(ADMNST_CD) AS cd FROM SPN057) m ON d.cd=m.cd;
--  ※ 서브쿼리만 쓰므로 5.x에서도 그대로 동작
```

---

## 【B】업종 키

### Q4-B0 ★ INDST_CD_ID 형식 확인 (Q4-C 조건 확정)

```sql
SELECT SUBSTR(TRIM(INDST_CD_ID),1,1) AS first_char, COUNT(*) AS n
FROM   CRD022
GROUP  BY 1 ORDER BY n DESC;
--  결과가 알파벳(A~U) → Q4-C의 IN ('B','C','F','H') 그대로 사용
--  결과가 숫자        → KSIC 숫자코드 체계. Q4-C 조건을 숫자 범위로 교체해야 함
--                       (미확인 상태로 진행하면 전 업종이 5명 기준으로 처리되어 조용히 틀림)
```

### Q9-A 업종 코드 개수

```sql
SELECT 'KCD_대' AS ax, COUNT(DISTINCT INDST_SE_LCLS_CD) AS n FROM SPN088
UNION ALL SELECT 'KCD_중',  COUNT(DISTINCT INDST_SE_MCLS_CD) FROM SPN088
UNION ALL SELECT 'BC_업종', COUNT(DISTINCT BUSIN_CD)         FROM SPN059
UNION ALL SELECT 'KODATA',  COUNT(DISTINCT INDST_CD_ID)      FROM CRD022;
```

### Q9-B 분류표 조회

```sql
SELECT * FROM SPN094;   -- KCD 업종 (기준 2025 — 데이터는 202306~202604, 시점 차이 한계로 명시)
SELECT * FROM SPN020;   -- BC카드 업종 (기준 202201 — 동백전 기간과 일치)
--  ※ SPN093은 2025년 기준·대분류 4자리로 동백전(2자리)과 체계가 달라 사용 금지

SELECT TP_BIZ_NM, COUNT(*) AS n
FROM   CRD023 GROUP BY TP_BIZ_NM ORDER BY n DESC LIMIT 100;
```

**매핑 실패 시** 대분류 10개 내외 수동 매핑으로 후퇴. 반출지침의 "범주 10개 내외" 원칙과 부합.

---

## 【C】식별 키

### Q2 ★ CRD022 브리지 1:N 관계 직접 검사 (v2 진단 로직 오류 수정)

```sql
-- 같은 IDCD·같은 월에 사업자번호가 2개 이상인 경우가 있는가
SELECT COUNT(*) AS multi_brno_cases
FROM  (SELECT IDCD, CRTR_YM, COUNT(DISTINCT BRNO) AS c
       FROM   CRD022 GROUP BY IDCD, CRTR_YM HAVING COUNT(DISTINCT BRNO) > 1) t;

-- 전체 규모
SELECT COUNT(*) AS rows, COUNT(DISTINCT BRNO) AS brno_n,
       COUNT(DISTINCT IDCD) AS idcd_n, COUNT(DISTINCT CRTR_YM) AS month_n
FROM   CRD022;
```

`multi_brno_cases > 0` 이면 1기업:N사업자번호 → 조인 시 행 증폭 주의.

### Q3 CRD025 → CRD022 매칭률

```sql
SELECT COUNT(*) AS crd025_brno,
       SUM(CASE WHEN b.BRNO IS NOT NULL THEN 1 ELSE 0 END) AS matched
FROM  (SELECT DISTINCT BRNO FROM CRD025) a
LEFT JOIN (SELECT DISTINCT BRNO FROM CRD022) b ON a.BRNO=b.BRNO;
--  80% 이상 → 계층2 진행 / 미만 → 매칭집합 한정, 미매칭을 L2로 보고
```

### Q3-B CRD022 → CRD023 매칭률

```sql
SELECT COUNT(*) AS crd022_idcd,
       SUM(CASE WHEN c.IDCD IS NOT NULL THEN 1 ELSE 0 END) AS matched
FROM  (SELECT DISTINCT IDCD FROM CRD022) a
LEFT JOIN (SELECT DISTINCT IDCD FROM CRD023) c ON a.IDCD=c.IDCD;
```

---

## 【D】커버리지

### Q4-A ★ 소상공인 커버리지 (최신 시점 고정)

```sql
--【M8+/P】
WITH latest AS (SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD)
SELECT c.CP_CD, c.ENP_SZE,
       CASE WHEN c.JOIN_CNT IS NULL THEN '미상'
            WHEN c.JOIN_CNT < 5     THEN '5명미만'
            WHEN c.JOIN_CNT < 10    THEN '5-9명'
            ELSE                         '10명이상' END AS size_band,
       COUNT(DISTINCT c.IDCD) AS ent_n,
       COUNT(DISTINCT c.BRNO) AS brno_n
FROM   CRD022 c JOIN latest l ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx
GROUP  BY 1,2,3 ORDER BY ent_n DESC;
--  CP_CD 1:일반법인 2:공공기관 3:비영리법인 8:기타법인 9:개인
--  ENP_SZE 1:대기업 2:중소기업 3:중견기업 0:대상아님
```

```sql
--【5.x】CTE 없이 — 서브쿼리 조인
SELECT c.CP_CD, c.ENP_SZE,
       CASE WHEN c.JOIN_CNT IS NULL THEN '미상'
            WHEN c.JOIN_CNT < 5 THEN '5명미만'
            WHEN c.JOIN_CNT < 10 THEN '5-9명'
            ELSE '10명이상' END AS size_band,
       COUNT(DISTINCT c.IDCD) AS ent_n
FROM   CRD022 c
JOIN  (SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD) l
       ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx
GROUP BY 1,2,3;
```

### Q4-C L1 / L2 (업종별 임계 + 미상 별도)

```sql
--【M8+/P】※ Q4-B0 결과가 알파벳일 때. 숫자면 IN 조건을 교체할 것
WITH latest AS (SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD022 GROUP BY IDCD),
snap AS (SELECT c.* FROM CRD022 c JOIN latest l ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx),
sb AS (SELECT DISTINCT BRNO FROM snap
       WHERE JOIN_CNT IS NOT NULL
         AND JOIN_CNT < CASE WHEN SUBSTR(TRIM(INDST_CD_ID),1,1) IN ('B','C','F','H')
                             THEN 10 ELSE 5 END),
unk AS (SELECT DISTINCT BRNO FROM snap WHERE JOIN_CNT IS NULL),
tx AS (SELECT DISTINCT BRNO FROM CRD025)
SELECT (SELECT COUNT(*) FROM sb)                                AS A_소상공인,
       (SELECT COUNT(*) FROM unk)                               AS A_직원수미상,
       (SELECT COUNT(*) FROM sb JOIN tx ON sb.BRNO=tx.BRNO)     AS L1_완전관측,
       (SELECT COUNT(*) FROM sb LEFT JOIN tx ON sb.BRNO=tx.BRNO
          WHERE tx.BRNO IS NULL)                                AS L2_부분관측;
--  L3 = N − A_소상공인   (N은 TOU007에서 산출 → Q12)
--  A_직원수미상이 크면 그만큼을 구간 상·하한으로 제시. 단일 수치 주장 금지
--  KSIC 대분류 B:광업 C:제조업 F:건설업 H:운수및창고업 → 10명 미만, 그 외 5명 미만
```

### Q12 ★신규 · TOU007로 모집단 N 산출 (외부 통계 의존 제거)

```sql
SELECT 종사자_규모_구분_컬럼 AS size_band,   -- Q0-2로 실제 컬럼명 확인 후 교체
       SUM(사업자_수_컬럼)   AS biz_n
FROM   TOU007
WHERE  기준년월_컬럼 LIKE '2023%'
GROUP  BY 1 ORDER BY biz_n DESC;
--  소상공인 기준(10명/5명 미만)에 해당하는 구간 합 = N
--  KODATA(202301~)와 2023년이 겹치므로 시점 통일 가능
--  ※ TOU007 컬럼 영문명은 정의서에 없어 Q0-2로 먼저 확인 필요
```

---

## 【E】자료형·유효성

### Q5 varchar 금액 값 패턴

```sql
SELECT CASH_PRCH_AMT, TAX_PRCH_AMT
FROM   SPN090 WHERE CASH_PRCH_AMT IS NOT NULL LIMIT 200;
```

### Q5-B 정제 후에도 숫자가 아닌 행 규모

```sql
--【M】※ 정규식을 [.] 문자클래스로 — 백슬래시 이스케이프 모드 영향 없음
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','')
                     REGEXP '^[0-9]+([.][0-9]+)?$' THEN 0 ELSE 1 END) AS bad
FROM   SPN090 WHERE CASH_PRCH_AMT IS NOT NULL;
--【P】REGEXP → ~ 로 교체, 패턴 동일
```

### Q5-C 불량 행의 실제 값 (편향 방향 확인)

```sql
--【M】
SELECT CASH_PRCH_AMT, COUNT(*) AS n
FROM   SPN090
WHERE  CASH_PRCH_AMT IS NOT NULL
  AND  REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') NOT REGEXP '^[0-9]+([.][0-9]+)?$'
GROUP  BY CASH_PRCH_AMT ORDER BY n DESC LIMIT 50;
--【P】NOT REGEXP → !~
--  마스킹(*)이 큰 금액에 몰려 있으면 m이 체계적으로 과소 추정 → 한계로 명시
```

### Q7 거래 시작·종료일 유효성

```sql
SELECT COUNT(*) AS tot,
       SUM(CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>'' THEN 1 ELSE 0 END) AS has_start,
       SUM(CASE WHEN VAT_END_YMD  IS NOT NULL AND VAT_END_YMD <>'' THEN 1 ELSE 0 END) AS has_end
FROM   CRD025;
```

### Q7-B ★ 매출/매입 구분 실재

```sql
SELECT DATA_SE_SLS_PRCHS_CD, COUNT(*) AS n, COUNT(DISTINCT BRNO) AS biz
FROM   CRD025 GROUP BY DATA_SE_SLS_PRCHS_CD;
```

| 결과 | 판정 |
|---|---|
| `'2'`의 biz가 전체 사업자의 **50% 이상** | NET_EXIT 전면 진행 |
| **20~50%** | 매입 보유 사업자로 분석 대상 한정 |
| **20% 미만** | NET_EXIT 폐기 → 계층2를 폐업 예측만으로 축소 |

### Q10 ★신규 · 폐업구분코드 값 체계 (종속변수 오염 방지)

```sql
SELECT CLSB_SE_CD, COUNT(*) AS n, COUNT(DISTINCT IDCD) AS ent_n,
       MIN(CLSB_YMD) AS d1, MAX(CLSB_YMD) AS d2
FROM   CRD023
GROUP  BY CLSB_SE_CD ORDER BY n DESC;
--  ※ 값이 폐업/휴업/정상 등으로 갈리면 P2의 조건을 '폐업'만으로 좁혀야 한다
--     v2는 "비어있지 않으면 폐업"으로 처리해 휴업까지 폐업으로 셀 위험이 있었다
```

### Q11 ★신규 · PT1(가액비중) 스케일

```sql
--【M】
SELECT MIN(CAST(PT1 AS DECIMAL(14,6))) AS mn,
       MAX(CAST(PT1 AS DECIMAL(14,6))) AS mx,
       AVG(CAST(PT1 AS DECIMAL(14,6))) AS av,
       COUNT(*) AS n
FROM   CRD025
WHERE  PT1 REGEXP '^[0-9]+([.][0-9]+)?$';
--【P】DECIMAL → NUMERIC, REGEXP → ~
--  max가 1 근처 → 분수.  100 근처 → 백분율 (HHI가 10000배 커짐)
--  분수가 아니면 P3에서 PT1/100 으로 정규화할 것
```

---

## 【F】부산 특수성 축

> **각 쿼리에 연령 파생 정의를 전부 인라인했다.** 반복은 의도적이다 — 복사 한 번으로 실행되어야 한다.

### Q8 연령 분포

```sql
--【M8+】
WITH dbj AS (
  SELECT ADM_CD, BUSIN_CD, TRANS_CNT, TRANS_SUM,
         SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,6) AS ym,
         CAST(SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,4) AS SIGNED)
           - CAST(BIRTH_YEAR AS SIGNED) AS age
  FROM   SPN059
  WHERE  BIRTH_YEAR REGEXP '^[0-9]{4}$'
    AND  CAST(BIRTH_YEAR AS SIGNED) BETWEEN 1900 AND 2026
)
SELECT age, SUM(TRANS_CNT) AS cnt, SUM(TRANS_SUM) AS amt
FROM   dbj GROUP BY age ORDER BY age;
--【P】CAST(STRD_DATE AS CHAR(8))→CAST(STRD_DATE AS TEXT), AS SIGNED→AS INTEGER, REGEXP→~
```

```sql
--【5.x】서브쿼리 방식
SELECT age, SUM(TRANS_CNT) AS cnt, SUM(TRANS_SUM) AS amt
FROM  (SELECT TRANS_CNT, TRANS_SUM,
              CAST(SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,4) AS SIGNED)
                - CAST(BIRTH_YEAR AS SIGNED) AS age
       FROM   SPN059
       WHERE  BIRTH_YEAR REGEXP '^[0-9]{4}$'
         AND  CAST(BIRTH_YEAR AS SIGNED) BETWEEN 1900 AND 2026) t
GROUP BY age ORDER BY age;
```

### Q8-B ★ 60세 이상 비중 — AGE 축 생사

```sql
--【M8+】
WITH dbj AS (
  SELECT TRANS_CNT, TRANS_SUM,
         CAST(SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,4) AS SIGNED)
           - CAST(BIRTH_YEAR AS SIGNED) AS age
  FROM   SPN059
  WHERE  BIRTH_YEAR REGEXP '^[0-9]{4}$'
    AND  CAST(BIRTH_YEAR AS SIGNED) BETWEEN 1900 AND 2026
)
SELECT SUM(CASE WHEN age>=60 THEN TRANS_SUM ELSE 0 END)/NULLIF(SUM(TRANS_SUM),0) AS age_dep_60_amt,
       SUM(CASE WHEN age>=60 THEN TRANS_CNT ELSE 0 END)/NULLIF(SUM(TRANS_CNT),0) AS age_dep_60_cnt,
       SUM(TRANS_SUM) AS tot_amt
FROM   dbj;
```

| 결과 | 판정 |
|---|---|
| 60세+ **10% 이상** | AGE 축 진행 |
| **5% 미만** | AGE 폐기 → 부산 특수성을 POP014·POP019 지역 인구 고령화로 대체 |

### Q8-C 전·후반 순위 안정성 (외삽 정당화)

```sql
--【M8+】
WITH dbj AS (
  SELECT ADM_CD, BUSIN_CD, TRANS_SUM,
         SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,6) AS ym,
         CAST(SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,4) AS SIGNED)
           - CAST(BIRTH_YEAR AS SIGNED) AS age
  FROM   SPN059
  WHERE  BIRTH_YEAR REGEXP '^[0-9]{4}$'
    AND  CAST(BIRTH_YEAR AS SIGNED) BETWEEN 1900 AND 2026
)
SELECT ADM_CD, BUSIN_CD,
       SUM(CASE WHEN ym <'202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         /NULLIF(SUM(CASE WHEN ym <'202301' THEN TRANS_SUM ELSE 0 END),0) AS dep_h1,
       SUM(CASE WHEN ym>='202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         /NULLIF(SUM(CASE WHEN ym>='202301' THEN TRANS_SUM ELSE 0 END),0) AS dep_h2
FROM   dbj GROUP BY ADM_CD, BUSIN_CD;
--  결과로 Spearman 순위상관 → ρ > 0.7 이면 외삽 정당화
```

---

# Phase 2 — 파이프라인

## P1 MPI 재료 (정제 가드 + 매출없는 셀 보존)

```sql
--【M8+】※【P】REGEXP→~ , DECIMAL→NUMERIC
WITH s AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         SUM(CARD_SLS_AMT) AS card_amt
  FROM   SPN088 GROUP BY 1,2,3),
c AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
    SUM(CASE WHEN REPLACE(REPLACE(CASH_SLS_AMT,',',''),' ','') REGEXP '^[0-9]+([.][0-9]+)?$'
             THEN CAST(REPLACE(REPLACE(CASH_SLS_AMT,',',''),' ','') AS DECIMAL(20,2)) END) AS cash_amt,
    SUM(CASE WHEN REPLACE(REPLACE(TAX_SLS_AMT,',',''),' ','')  REGEXP '^[0-9]+([.][0-9]+)?$'
             THEN CAST(REPLACE(REPLACE(TAX_SLS_AMT,',',''),' ','')  AS DECIMAL(20,2)) END) AS tax_amt,
    SUM(CASE WHEN REPLACE(REPLACE(CASH_SLS_AMT,',',''),' ','') REGEXP '^[0-9]+([.][0-9]+)?$'
             THEN 0 ELSE 1 END) AS bad_cnt
  FROM   SPN089 GROUP BY 1,2,3),
p AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
    SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') REGEXP '^[0-9]+([.][0-9]+)?$'
             THEN CAST(REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') AS DECIMAL(20,2)) END) AS cash_prch,
    SUM(CASE WHEN REPLACE(REPLACE(TAX_PRCH_AMT,',',''),' ','')  REGEXP '^[0-9]+([.][0-9]+)?$'
             THEN CAST(REPLACE(REPLACE(TAX_PRCH_AMT,',',''),' ','')  AS DECIMAL(20,2)) END) AS tax_prch,
    SUM(CASE WHEN REPLACE(REPLACE(CASH_PRCH_AMT,',',''),' ','') REGEXP '^[0-9]+([.][0-9]+)?$'
             THEN 0 ELSE 1 END) AS bad_cnt
  FROM   SPN090 GROUP BY 1,2,3),
keys AS (
  SELECT ym, ADMNST_CD, ind FROM s
  UNION SELECT ym, ADMNST_CD, ind FROM c
  UNION SELECT ym, ADMNST_CD, ind FROM p)
SELECT k.ym, k.ADMNST_CD, k.ind,
       COALESCE(s.card_amt,0)+COALESCE(c.cash_amt,0)+COALESCE(c.tax_amt,0) AS S_total,
       COALESCE(p.cash_prch,0)+COALESCE(p.tax_prch,0)                      AS M_total,
       (COALESCE(p.cash_prch,0)+COALESCE(p.tax_prch,0))
         /NULLIF(COALESCE(s.card_amt,0)+COALESCE(c.cash_amt,0)+COALESCE(c.tax_amt,0),0) AS m_ratio,
       COALESCE(c.bad_cnt,0)+COALESCE(p.bad_cnt,0) AS bad_rows
FROM       keys k
LEFT JOIN  s ON k.ym=s.ym AND k.ADMNST_CD=s.ADMNST_CD AND k.ind=s.ind
LEFT JOIN  c ON k.ym=c.ym AND k.ADMNST_CD=c.ADMNST_CD AND k.ind=c.ind
LEFT JOIN  p ON k.ym=p.ym AND k.ADMNST_CD=p.ADMNST_CD AND k.ind=p.ind;
--  bad_rows > 0 인 셀은 신뢰도 낮음 → 제외하거나 별도 표기
```

## P1-B 가격 전가 계수 π (전월식 실제 코드로 교체)

```sql
--【M8+】
WITH mp AS (
  SELECT SUBSTR(CRTR_YMD,1,6) AS ym, ADMNST_CD, INDST_SE_MCLS_CD AS ind,
         MENU_STD_NM, AVG(MENU_PRC) AS price, SUM(MENU_SLS_CNT) AS qty
  FROM   SPN091 GROUP BY 1,2,3,4)
SELECT a.ym, a.ADMNST_CD, a.ind,
       SUM((a.price-b.price)/NULLIF(b.price,0)*a.qty)/NULLIF(SUM(a.qty),0) AS d_price_w
FROM       mp a
INNER JOIN mp b
        ON a.ADMNST_CD=b.ADMNST_CD AND a.ind=b.ind AND a.MENU_STD_NM=b.MENU_STD_NM
       AND b.ym = DATE_FORMAT(DATE_SUB(STR_TO_DATE(CONCAT(a.ym,'01'),'%Y%m%d'),
                                       INTERVAL 1 MONTH),'%Y%m')
GROUP BY a.ym, a.ADMNST_CD, a.ind;
--【P】b.ym = to_char(to_date(a.ym||'01','YYYYMMDD') - interval '1 month','YYYYMM')
--  동일 메뉴끼리 비교 → 메뉴 구성 변화가 가격 변화로 오인되지 않음
```

## P2 종속변수 (SE_GB + 시점 근접 + 최신 스냅샷)

```sql
--【M8+】※ Q10 결과에 따라 CLSB_SE_CD 조건을 실제 '폐업' 값으로 좁힐 것
WITH latest AS (SELECT IDCD, MAX(CRTR_YM) AS mx FROM CRD023 GROUP BY IDCD),
snap AS (SELECT c.* FROM CRD023 c JOIN latest l ON c.IDCD=l.IDCD AND c.CRTR_YM=l.mx),
mv AS (
  SELECT IDCD,
         CAST(SUBSTR(CRTR_YM,1,4) AS SIGNED)*12 + CAST(SUBSTR(CRTR_YM,5,2) AS SIGNED) AS mv_mi
  FROM   CRD026 WHERE SE_GB IN ('0','2'))
SELECT s.IDCD, s.CLSB_SE_CD, s.CLSB_YMD,
       CASE WHEN s.CLSB_SE_CD IS NOT NULL AND s.CLSB_SE_CD<>''
                 AND mv.IDCD IS NULL THEN 1 ELSE 0 END AS closed_flag
FROM       snap s
LEFT JOIN  mv
        ON s.IDCD = mv.IDCD
       AND s.CLSB_YMD IS NOT NULL AND s.CLSB_YMD <> ''
       AND ABS(mv.mv_mi - (CAST(SUBSTR(s.CLSB_YMD,1,4) AS SIGNED)*12
                           + CAST(SUBSTR(s.CLSB_YMD,5,2) AS SIGNED))) <= 3;
--  SE_GB 0:부산내이동 2:전출 만 이전으로 간주 (1:전입은 폐업과 무관)
--  폐업 시점 ±3개월 이내 이동만 제외
--  ※ v2는 CRD023 월별 스냅샷 중복을 처리하지 않아 업체당 행이 여러 개였다
```

## P3 NET_EXIT · HHI

```sql
--【M8+/P】
WITH mm AS (
  SELECT BRNO, CRTR_YM,
         COUNT(DISTINCT DLR_BRNO) AS n_partner,
         COUNT(DISTINCT CASE WHEN VAT_END_YMD IS NOT NULL AND VAT_END_YMD<>''
                              AND SUBSTR(VAT_END_YMD,1,6)=CRTR_YM THEN DLR_BRNO END) AS ended,
         COUNT(DISTINCT CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>''
                              AND SUBSTR(VAT_STRT_YMD,1,6)=CRTR_YM THEN DLR_BRNO END) AS started
  FROM   CRD025 WHERE DATA_SE_SLS_PRCHS_CD='2'
  GROUP  BY BRNO, CRTR_YM),
hh AS (
  SELECT BRNO, CRTR_YM,
         SUM(POWER(CAST(PT1 AS DECIMAL(14,6)),2)) AS hhi,
         COUNT(*) AS pt1_used
  FROM   CRD025
  WHERE  DATA_SE_SLS_PRCHS_CD='2' AND PT1 REGEXP '^[0-9]+([.][0-9]+)?$'
  GROUP  BY BRNO, CRTR_YM),
raw AS (
  SELECT BRNO, CRTR_YM, COUNT(*) AS pt1_total
  FROM   CRD025 WHERE DATA_SE_SLS_PRCHS_CD='2' GROUP BY BRNO, CRTR_YM),
w AS (
  SELECT BRNO, CRTR_YM, n_partner, ended, started,
         LAG(n_partner) OVER (PARTITION BY BRNO ORDER BY CRTR_YM) AS prev_partner
  FROM   mm)
SELECT w.BRNO, w.CRTR_YM, w.n_partner, w.prev_partner, w.ended, w.started,
       (w.ended - w.started)*1.0/NULLIF(w.prev_partner,0) AS net_exit,
       h.hhi,
       h.pt1_used, r.pt1_total,
       1.0 - h.pt1_used*1.0/NULLIF(r.pt1_total,0) AS pt1_excl_rate   -- HHI 신뢰도
FROM       w
LEFT JOIN  hh  h ON w.BRNO=h.BRNO AND w.CRTR_YM=h.CRTR_YM
LEFT JOIN  raw r ON w.BRNO=r.BRNO AND w.CRTR_YM=r.CRTR_YM
ORDER BY   w.BRNO, w.CRTR_YM;
--  pt1_excl_rate 가 크면 HHI가 과소 추정됨 → 해당 행 제외 또는 한계 명시
--  Q11에서 PT1이 백분율(0~100)로 판명되면 CAST(PT1 AS DECIMAL)/100 으로 교체
```

```sql
--【5.x】CTE·LAG 없음 → 임시테이블 방식 (v2 대체안은 mm을 참조해 실행 불가였음)
CREATE TEMPORARY TABLE mm AS
SELECT BRNO, CRTR_YM,
       COUNT(DISTINCT DLR_BRNO) AS n_partner,
       COUNT(DISTINCT CASE WHEN VAT_END_YMD IS NOT NULL AND VAT_END_YMD<>''
                            AND SUBSTR(VAT_END_YMD,1,6)=CRTR_YM THEN DLR_BRNO END) AS ended,
       COUNT(DISTINCT CASE WHEN VAT_STRT_YMD IS NOT NULL AND VAT_STRT_YMD<>''
                            AND SUBSTR(VAT_STRT_YMD,1,6)=CRTR_YM THEN DLR_BRNO END) AS started
FROM   CRD025 WHERE DATA_SE_SLS_PRCHS_CD='2'
GROUP  BY BRNO, CRTR_YM;

CREATE INDEX ix_mm ON mm (BRNO, CRTR_YM);

SELECT m.BRNO, m.CRTR_YM, m.n_partner, p.n_partner AS prev_partner,
       m.ended, m.started,
       (m.ended-m.started)*1.0/NULLIF(p.n_partner,0) AS net_exit
FROM       mm m
LEFT JOIN  mm p
        ON p.BRNO=m.BRNO
       AND p.CRTR_YM = DATE_FORMAT(DATE_SUB(STR_TO_DATE(CONCAT(m.CRTR_YM,'01'),'%Y%m%d'),
                                            INTERVAL 1 MONTH),'%Y%m');
--  ※ CREATE TEMPORARY TABLE 권한이 없으면 mm 서브쿼리를 두 번 반복해 self-join
```

## P4 AGE_LEVEL · AGE_SLOPE (인라인 정의)

```sql
--【M8+】
WITH dbj AS (
  SELECT ADM_CD, BUSIN_CD, TRANS_SUM,
         SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,6) AS ym,
         CAST(SUBSTR(CAST(STRD_DATE AS CHAR(8)),1,4) AS SIGNED)
           - CAST(BIRTH_YEAR AS SIGNED) AS age
  FROM   SPN059
  WHERE  BIRTH_YEAR REGEXP '^[0-9]{4}$'
    AND  CAST(BIRTH_YEAR AS SIGNED) BETWEEN 1900 AND 2026
)
SELECT ADM_CD, BUSIN_CD,
       SUM(CASE WHEN age>=60 THEN TRANS_SUM ELSE 0 END)/NULLIF(SUM(TRANS_SUM),0) AS age_level,
       SUM(CASE WHEN ym>='202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         /NULLIF(SUM(CASE WHEN ym>='202301' THEN TRANS_SUM ELSE 0 END),0)
     - SUM(CASE WHEN ym <'202301' AND age>=60 THEN TRANS_SUM ELSE 0 END)
         /NULLIF(SUM(CASE WHEN ym <'202301' THEN TRANS_SUM ELSE 0 END),0) AS age_slope
FROM   dbj GROUP BY ADM_CD, BUSIN_CD;
--  age_slope 는 후반−전반 수준차 (엄밀한 기울기 아님 — 보고서 표기 주의)
```

---

# 부록 A — 문법 대체표

| 기능 | MySQL | PostgreSQL |
|---|---|---|
| 숫자→문자 | `CAST(x AS CHAR(8))` | `CAST(x AS TEXT)` — **길이 없는 CHAR는 char(1)로 잘림** |
| 문자→정수 | `CAST(x AS SIGNED)` | `CAST(x AS INTEGER)` |
| 문자→소수 | `CAST(x AS DECIMAL(20,2))` | `CAST(x AS NUMERIC(20,2))` |
| 정규식 | `x REGEXP '패턴'` | `x ~ '패턴'` |
| 부정 정규식 | `x NOT REGEXP '패턴'` | `x !~ '패턴'` |
| 소수점 리터럴 | **`[.]`** 사용 (`\.`는 이스케이프 모드 영향) | `[.]` 동일 |
| 전월 | `DATE_FORMAT(DATE_SUB(STR_TO_DATE(CONCAT(ym,'01'),'%Y%m%d'),INTERVAL 1 MONTH),'%Y%m')` | `to_char(to_date(ym\|\|'01','YYYYMMDD')-interval '1 month','YYYYMM')` |
| 문자열 연결 | `CONCAT(a,b)` | `a \|\| b` |
| 0 나누기 | `NULLIF(x,0)` | 동일 |
| 문자열 길이 | `LENGTH(x)` | 동일 |
| CTE / 창함수 | **8.0+** | 지원 |

---

# 부록 B — 실행 체크리스트

| 단계 | 쿼리 | 시간 |
|---|---|---|
| **환경** | Q0-1 · Q0-2B | 5분 |
| **판정** | **Q8-B · Q4-A · Q1-B0 · Q1-B · Q7-B · Q10** | **25분** |
| 전제 확인 | Q4-B0 · Q11 · Q0-2(TOU007 컬럼) | 15분 |
| 공간 | Q1-A · Q1-C · Q1-E · Q2-A · Q2-B · Q6 | 20분 |
| 식별 | Q2 · Q3 · Q3-B | 10분 |
| 커버리지 | Q4-C · Q12 | 15분 |
| 자료형 | Q5 · Q5-B · Q5-C · Q7 | 15분 |
| 업종 | Q9-A · Q9-B | 15분 |
| AGE | Q8 · Q8-C | 10분 |

**핵심 6개 30분 / 전체 약 2시간.** 시간을 먹는 건 실행이 아니라 테이블명·문법 왕복이다.

---

# 부록 C — 판정 → 경로

```
Q1-B   SPN090만 REL020 법정동과 매칭 ─→ 법정동 확정 ─→ 매핑표 반입 / 구·군 상향
       둘 다 매칭 안 됨               ─→ 비고 오타. 정상 진행

Q4-A   CP_CD=9 + 소규모 충분 ─→ 계층2 전면
       부족                  ─→ 범위 한정 + 커버리지율을 결론 3층으로 승화

Q7-B   '2' biz 50%+ ─→ NET_EXIT 전면
       20~50%       ─→ 매입 보유 사업자로 한정
       20% 미만     ─→ NET_EXIT 폐기, 폐업 예측만

Q8-B   60세+ 10%+  ─→ AGE 축 진행
       5% 미만     ─→ AGE 폐기, POP014·POP019로 대체

Q10    CLSB_SE_CD가 폐업/휴업 혼재 ─→ P2 조건을 '폐업' 값만으로 좁힘
Q11    PT1이 0~100 백분율          ─→ P3에서 /100 정규화
Q4-B0  INDST_CD_ID가 숫자          ─→ Q4-C 업종 조건을 숫자 범위로 교체
```

---

# 부록 D — 반입 파일 (3개 한도)

```
① 쿼리세트.xlsx
   시트1  Phase0 · Phase1 (MySQL 8+)
   시트2  Phase1 PostgreSQL 변형
   시트3  Phase2 파이프라인
   시트4  MySQL 5.x 대체안
   시트5  ★ 부산 법정동-행정동 매핑표 (행안부 공개코드에서 부산 추출)
② 라이브러리목록.xlsx
③ 분석설계도.pdf
```
