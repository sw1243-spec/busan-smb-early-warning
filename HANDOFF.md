# 인수인계 — 2026 부산 Big Data 활용 대회

> **새 대화·새 세션에서 이 프로젝트를 이어받는 Claude가 가장 먼저 읽는 문서.**
> 아래 내용은 이미 검증된 것과 이미 폐기된 것을 구분한다. 재확인·재제안 금지 항목이 명시되어 있다.
> 최종 갱신: **2026-09-01** (2차 방문 반영)
>
> ⚠️ **읽는 순서**: 이 문서 → 방문결과 1·2차 → `오픈랩_3차방문_준비.md` → **`오픈랩_3차방문_결과.md`(최신. 반출물 해석)**
> 데이터 형식·인코딩·구분자·실측 인벤토리는 2차 방문 결과가, **반입목록·현장코드·SPN082 검증·판단 지점은 3차 방문 준비 문서가 최신 근거**다.

---

# 0. 이 문서를 읽는 Claude에게

**작업 규칙 (사용자가 명시적으로 요청한 기본값)**

1. **브루탈 크리틱이 디폴트다.** 요청이 없어도 비판적으로 진행한다. 장점보다 **깨질 지점을 먼저** 말한다.
2. **검증 없이 새 방향을 제안하지 않는다.** 제안할 때는 "무엇이 확인되면 살고 죽는지" 게이트를 함께 건다.
3. **억지로 단점을 만들어내지 않는다.** 객관적으로 본다. 과장했으면 명시적으로 정정한다.
4. **입지분석(최적 입지 선정)은 제외한다.** 사용자가 명시적으로 배제했다.
5. 파일을 새로 쓰기 전 **같은 이름 파일이 있는지 먼저 확인**한다. (이전에 덮어쓴 적 있음)

**이 프로젝트에서 실제로 있었던 실패 패턴**
> 질문을 세우고 → 데이터를 찾고 → 없거나 변량이 죽어 폐기. 이걸 6번 반복했다.
> 순서를 뒤집어야 한다. **데이터를 먼저 열고 거기서 질문을 만든다.**

---

# 1. 대회 개요

| 항목 | 내용 |
|---|---|
| 대회 | 2026년 Big Data 활용 대회 (부산광역시 · 부산테크노파크) |
| 부문 | **빅데이터 분석 및 시각화** (붙임 2 서식) |
| 접수 마감 | **2026-09-18(금) 17:00** |
| 최종발표 | 2026-10-29, 본선 14팀 |
| 팀 | **2명** |
| 제출물 | 데이터시각화 1장 PDF · 분석보고서(HWPX+PDF) · 활용데이터(xls/csv) → **압축 1개 파일** |
| 보고서 분량 | **10P 이하 (표지 제외)**, 목차 변경 가능 |
| 가점 | **데이터 오픈랩 방문 + Big-데이터웨이브 사용** (서식 붙임 4) |

**서식 붙임 2 목차**: 1.분석개요 → 2.분석방법(활용데이터 표 + 분석도구·전처리·기법·검증·시각화) → 3.분석결과 → 4.활용방안 → 5.기대효과 → 6.기타

> ⚠️ 사용자 확인: **9/18은 완성 보고서 마감**이다. 기획서만 내는 게 아니다.
> 따라서 오픈랩 방문·분석·반출 심사(영업일 5일)가 전부 그 전에 끝나야 한다.

---

# 2. 확정된 주제

## 제목
> **매출은 그대로인데 왜 문을 닫는가**
> — 원가·거래관계·고객구조로 본 부산 소상공인 위기 신호와 정책 공백

## 한 줄
매출이 아니라 **매입(원가)·거래관계·고객 연령구조**로 위기를 조기 포착하고, 위기 유형별로 **대응 프로그램이 존재하는지**를 데이터로 검증한다.

## 결론 3층 (이게 뼈대다)

| 층 | 결론 | 처방 |
|:-:|---|---|
| 1 | 매출만 봐서는 안 보이는 위기가 있다 | 원가·거래관계 지표 도입 |
| 2 | **찾아도 보낼 곳이 없다** | 원가 압박형·고객구조 전환형 프로그램 신설·재배분 |
| 3 | **애초에 못 찾는 사람들이 있다** | 소상공인 멤버십(자동 안내) + 오프라인 발굴 |

3층이 최종 도달점이다. **분석의 커버리지 한계를 감추지 않고, 그 한계 자체를 부산시 정책의 데이터 사각지대로 정량화**한다.

## 왜 부산인가 (심사 대비 핵심)

소상공인 원가 압박은 전국 공통이다. 부산 고유 조건은 **인구 구조**다.

| 지역 | 고령인구비율 (2025) |
|---|---:|
| **부산** | **25.3%** |
| 대구 | 22.1% |
| 서울 | 20.4% |
| 전국 | 21.2% |

출처: KOSIS 「고령인구비율(시도/시/군/구)」 `DT_1YL20631` — **검증 완료**

초고령 도시에서는 **상권의 고객층이 함께 늙는다.** 이 유형(④ 고객구조 전환형)에는 기존 처방이 하나도 통하지 않는다. 마케팅·온라인 전환·상권 활성화 전부 인구 구조 문제를 못 건드린다.

## 최종 처방 — 소상공인 멤버십

개인 복지에는 **복지멤버십(맞춤형급여안내)**이 있다. 소득·재산을 분석해 받을 수 있는 복지를 **먼저 알려준다**(푸시). 소상공인은 **기업마당(bizinfo)**에서 직접 검색해야 한다(풀).

**같은 신청주의 문제인데 개인 복지는 2022년부터 풀었고 소상공인은 그대로다.**

우리 유형 분류가 그 판정 엔진이 된다. 그리고 **낙인 문제가 여기서 해소된다** — 같은 명단이라도 "폐업 위험군입니다"는 낙인이지만 "바우처 대상입니다"는 혜택이다.

> 예산 반론 대응: 소상공인 지원은 재량지출이라 **총액은 이미 고정**이다. 진짜 장벽은 배분 정당성이고, RISK 스코어가 그것을 제공한다. 따라서 제안은 "자동 안내"가 아니라 **"위험도 기반 자동 선정"**까지 갈 수 있다.

---

# 3. ★ 검증된 데이터 사실 — 재확인 불필요

정의서 PDF(`데이터 반출 관련 오픈랩/3.데이터_정의서.pdf`) 원문과 대조해 확인한 것. **이 표를 신뢰해도 된다.**

| 사실 | 근거 |
|---|---|
| `CRD025.DATA_SE_SLS_PRCHS_CD` = **1:매출 2:매입** | 정의서 명시. 매입 식별 가능 |
| `CRD022.CP_CD` = 1:일반법인 2:공공기관 3:비영리법인 8:기타법인 **9:개인** | 개인사업자 식별 가능 |
| `CRD022.ENP_SZE` = 1:대기업 2:중소기업 3:중견기업 0:대상아님 | |
| `CRD022`에 **`JOIN_CNT`(직원수)** 있음 | 소상공인 기준 적용 가능 |
| **`CRD023`에 사업자번호(BRNO) 없음** | `ENT_CD`·`IDCD`만 존재 → CRD022 경유 필수 |
| `CRD023`에 **`KOR_BZPLC_ADDR`(한글사업장주소)** 있음 | 좌표만 있는 게 아님. 주소 파싱 가능 |
| **`SPN019`에 법정동 코드 컬럼 없음** | 실제: `CTPV_NM·HCODE·GCODE·HNAME·GNAME`. 행정동↔시군구 매핑이다 |
| **`REL020.A1` = 법정동코드(10)** | 반출은 불가하나 **내부 대조용으로 사용 가능** |
| `SPN090`만 컬럼명 `행정동코드(10자리)` / 비고 `법정동 코드 10자리` — **자기모순** | SPN088·089·091은 일치. 오타 가능성 높으나 확인 필요 |
| `SPN089·SPN090` 금액이 **varchar(50)**, SPN088은 decimal | 형변환 가드 필수 |
| `CRD026.SE_GB` = **1:전입 2:전출 0:부산내이동** | 전입은 폐업과 무관 |
| `SPN059/060`에 **생년·성별** 있음 | 고객 연령구조 관측 가능 |
| 동백전(SPN058/059/060)만 **원본 반출 가능(OOOOO)** | KCD·KODATA는 XXXOO |
| ~~행정동 경계 SHP는 오픈랩에 없음~~ → **정정: REL020이 SHP다** | 1·2차 방문 실측. `AL_D150_26_*.shp/dbf/shx/prj` 수령 확인 (1.5GB). 반출등급만 보고 형식을 확인하지 않은 오류였다 |
| `TOU007`에 종사자 규모 구분 + 사업자 수 | 결론 3층의 모집단 N을 내부에서 산출 가능 |
| `SPN020`(BC업종, 202201) vs `SPN093`(2025, 자릿수 다름) | 동백전 해석엔 **SPN020** |
| TRN001 카드소유주유형 = **어른/청소년/어린이** | 경로우대 없음 (교통카드 주제가 죽은 이유) |

## 3-1. ★ 2차 방문(2026-09-01) 실측으로 추가 확정된 것

> 상세는 `오픈랩_2차방문_결과.md` §2·§3·§7. 아래는 **재확인 불필요**한 확정값이다.

| 사실 | 값 |
|---|---|
| **데이터 형식** | **DB 아님. CSV 파일**(월별 분할) + REL020만 SHP |
| **인코딩** | **UTF-8** — 전 테이블 공통. CP949 아님 |
| **구분자** | **테이블마다 다름.** PIPE 19개 / **COMMA 7개**(POP001·POP014·REL015·SPN057·**SPN060**·SPN093·SPN094) |
| **헤더** | 각 폴더 **첫 파일에는 있음** (2번째 파일 이후는 미확인 → §5) |
| **총 용량** | **약 57 GB.** 최대 SPN060 22.2GB, SPN059 14.1GB → **pandas 로드 금지, DuckDB 스트리밍 필수** |
| **좌표계** | 관측 좌표는 **WGS84(EPSG:4326)** — 단 **출처 테이블 미확정**. 콤마형이므로 **CRD 계열은 아님**(CRD는 전부 PIPE). SPN060 추정 |
| **고객 속성** | **생년 원시값 + 성별(M/F)**. 연령대 구간이 아니라 원시 생년 → 구간 자유 설계 가능. 출처는 위와 동일 줄 |
| **금액 형식** | 소수점 유무 혼재(`171500.00` / `39435523`) → `TRY_CAST(... AS DOUBLE)` 가드 필수 |
| **분석 도구** | DuckDB **1.4.5** · polars 1.36.1 · pandas 2.3.3 · geopandas · scikit-learn |
| **동백전 기간** | **202201~202309 (21개월)** — 1차 때 3개월이던 것이 재추출로 확대. **부산 특수성 축 부활** |
| **동백전 ∩ KCD** | **4개월**(202306~202309) → 시계열 결합 불가 |
| **동백전 ∩ KODATA** | **9개월**(202301~202309) |
| **CRD022·023** | **202501~202606만 수령** — 사업자 속성 결손. **생존편향 위험** (§5-11) |

**DB 엔진 관련 항목(Q0-1·Q0-2B)은 무의미해졌다.** 파일 기반이므로 SQL은 DuckDB로 실행한다.

---

# 4. ★ 폐기된 것 — 재제안 금지

**7번 시도해서 7번 죽었다. 아래를 다시 꺼내면 8번째다.**

| # | 폐기된 주제·설계 | 사인 |
|:-:|---|---|
| 1 | 불법주정차 "신고되지 않은 위반" 찾기 | 실제 위반량을 잴 준거 데이터 부재 |
| 2 | 고령화 × 민원 신고율 | 구군 상관 **r = +0.001** (16개 구 실측) |
| 3 | 과태료 부과율 지역 격차 | 6개 구 중 5개가 **부과 100%**, 변량 0 |
| 4 | 신고→단속 대응시간 | 해운대 26,419건 중 **유효 360건** |
| 5 | 교통카드(TRN001) 고령자 이동 | **경로우대 카테고리가 스키마에 없음** |
| 6 | 15분 도시 생활권 진단 | 산출물이 입지분석 → 사용자가 배제 |
| 7 | SPN019 경유 법정동→행정동 변환 | SPN019에 법정동 컬럼 없음 |

**민원 데이터 관련 추가 사실** (다시 파지 말 것)
- 부산 민원 2025년 708,354건, 교통 60.9% — 권익위 민원빅데이터에서 추출 완료
- 연령 정보 **84.5% 결측**, 60대 민원이 1년 새 144% 급증(수집방식 변경 추정) → 연령 분석 불가
- 부산 6개 구 불법주정차 신고 원본 확보했으나 대부분 "부과된 건만" 수록

---

# 5. ⚠️ 미확인 — 사실로 전제 금지

| # | 항목 | 확인 방법 |
|:-:|---|---|
| 1 | SPN090이 행정동인가 법정동인가 | 쿼리 Q1-B (REL020 대조) |
| 2 | KODATA에 영세 개인사업자가 얼마나 있는가 | Q4-A |
| 3 | 동백전 60세 이상 고객 비중 | **Q8-B — 부산 특수성 축의 생사** |
| 4 | `CLSB_SE_CD` 값 체계 (폐업/휴업 혼재 여부) | Q10 |
| 5 | `PT1`(가액비중)이 0~1인가 0~100인가 | Q11 |
| 6 | `INDST_CD_ID`가 알파벳인가 숫자인가 | Q4-B0 |
| ~~7~~ | ~~DB 엔진 종류·버전~~ | **해소됨.** 파일 기반 → DuckDB 1.4.5 사용 |
| ~~8~~ | ~~테이블 분할 여부~~ | **해소됨.** 월별 CSV 분할 확인 |
| 9 | TOU007 컬럼 영문명 | Q0-2 (정의서에 없음) |
| 10 | 지원 프로그램 2026년 금액·대상 | 발표 2주 전 공고 재확인 |
| **11** | **각 폴더 2번째 파일 이후 헤더 유무** | 헤더 없는데 `header=true`로 읽으면 **매달 첫 행이 조용히 소실.** 에러 안 남 → **다음 방문 최우선** |
| **12** | **COMMA 7개의 실제 구분자** | 판정이 "파이프 없으면 COMMA"였다. 탭·세미콜론 가능성 미검증. **SPN060(22GB)이 여기 포함** |
| **13** | **REL020 SHP 실체** (컬럼·행수·CRS) | `AL_D150` 명명규칙 추정만 함. geopandas로 실제 열어본 적 없음 |
| **14** | 각 테이블 행 수 | 용량만 알고 행 수 모름 → 쿼리 소요시간·방문 시간 배분 불가 |
| **15** | 같은 테이블 월별 파일의 컬럼 수 일치 | 시기별 스키마 변동 시 glob 읽기 실패 |
| **16** | **관측된 좌표·생년의 출처 테이블** | 콤마형이라 CRD 계열이 아니다. **`CRD023`의 좌표는 아직 눈으로 본 적 없음** → 공간조인 경로(CRD023→REL020→법정동)가 **미검증** |

---

# 6. 오픈랩 제약 (관리자 답변으로 확인됨)

## 반출
- **원본·발췌·단순집계**: KCD·KODATA·NICE·KT·BC는 **불가**
- **응용집계**(비율·증감율·순위·범주·지수·모델링 결과·통계 검정값)·**시각화**: 가능
- **z-정규화·MIN-MAX·가중치변환은 단순집계로 분류 → 반출 불가.** 백분위 순위로 대체할 것
- 범주 개수 **10개 내외** 원칙 → 행정동(205)×업종 패널은 위험, **구·군(16)** 단위는 안전
- 심사 **영업일 5일**, 부산시 등 제공기관 심의 포함. 반출요청서에 출처·산출과정 상세 명시
- TIF 불가, 15GB 이상 불가

## 반입
- **xls·xlsx·pdf·이미지만. 3개, 30MB.** `.sql`·`.py`·`.txt` **불가**
- 준비 완료: `반입파일/①쿼리세트.xlsx` `②라이브러리목록.xlsx` `③분석설계도.pdf` (총 39KB)

## 환경
- **인터넷 차단.** 라이브러리 사전 신청 필수 (현장 설치 불가)
- Python 3.9 / Anaconda 22.05 / R 4.2.1 / Tableau / QGIS / DBeaver
- 분석실 PC: CPU 2.50GHz 8코어 / **RAM 32GB**
- **작업물 90일 보관 · 재방문 가능**
- 이용신청 소요기간 없음
- 8월 시스템 점검 기간 있음 (일정 확인 필요)
- 위치: 부산 해운대구 센텀동로 45, CENTAP 5층 / 051-744-8494 / 평일 09~18시(점심 11:30~13:00 제외)

---

# 7. 파일 목록

## 새 세션에 반드시 가져갈 것
| 파일 | 역할 |
|---|---|
| **HANDOFF.md** | 이 문서 |
| **최종계획서_수정반영본.md** | 본 계획서 (조인키 지도·데이터 문제 8건·논리 문제 4건 포함) |
| ~~쿼리세트_v3.md~~ | **폐기. 컬럼명이 틀렸다** (STRD_DATE·PT1 위치·매입 단일컬럼). **`현장코드_2026-09.md`(=반입파일/④현장코드.xlsx) 사용** |
| **오픈랩_1차방문_결과.md** | 8/31 방문. 재요청 목록·데이터 형식 최초 확인 |
| **오픈랩_2차방문_결과.md** | **9/1 방문. 적재 파라미터 확정·실측 인벤토리. 현장 작업의 최신 근거** |
| **오픈랩_신청목록.md** + `data/오픈랩_신청목록_25건.csv` | 신청 25건 |
| **반입파일/** | 방문 시 USB로 반입. **파일 개수 제한은 해결됨(반입 가능 확인)** |

## 참고용
| 파일 | 역할 |
|---|---|
| 선행연구_중복검토.md | RISS·KCI·DBpia 검토. 중복 없음 확인. Tier 1·2 목록 |
| 문제해결안_1_2_3.md | 코드리뷰 대응 (일부는 v3에 이미 반영) |
| 데이터 반출 관련 오픈랩/ (PDF 4개) | **원본 근거.** 정의서·반출지침 |
| data/ | 권익위 민원 데이터, 오픈랩 인벤토리 240건, 불법주정차 원본 |
| 2026년 Big Data 활용 대회 작성 서식.hwpx | 제출 서식 원본 |

> ⚠️ `쿼리세트.md`(v1)·`쿼리세트_v2.md`는 **버그가 있는 구버전**이다. v3만 사용할 것.

---

# 8. 독창성 논거 (심사·보고서용)

**해외 선행연구가 이미 있다.** "국내 최초"라고 쓰면 안 된다.
- Kontokosta, Hong & Korsberg (2017) *Equity in 311 Reporting*
- Kontokosta & Hong (2021) *Bias in smart city governance*, Sustainable Cities and Society
- Boxer (2025) *Estimating Reporting Bias in 311 Complaint Data*, NYU Marron Institute

**진짜 독창성은 제도 구조에 있다.**
> 311은 자발적 민원 창구지만, 한국의 주민신고제·지원제도는 **신청 여부가 곧 수혜 여부**가 되는 구조다. 신청주의의 편향이 곧 급부의 편향이 된다.

**국내 공백 (RISS·KCI·DBpia 검색 결과 0건)**: `민원 편향` `신고 편향` `신고 성향` `과태료 부과율 지역 비교` `민원 데이터 대표성`

---

# 9. 다음 행동 (우선순위)

> 2026-09-01 기준으로 전면 갱신. 1·2차 방문이 완료되어 이전 목록은 대부분 소진되었다.

| # | 작업 | 상태 |
|:-:|---|---|
| ~~1~~ | ~~오픈랩 이용신청 25건~~ | **완료.** 27개 폴더 수령 |
| ~~2~~ | ~~1차 방문~~ / ~~2차 방문~~ | **완료** (8/31, 9/1) |
| ~~3~~ | ~~라이브러리 목록 제출~~ | **완료.** DuckDB·polars·geopandas·sklearn 설치 확인 |
| ~~4~~ | ~~센터 문의: DB 엔진 / 반입 절차~~ | **해소.** 파일 기반 확인 · 반입 가능 확인 |
| **1** | **CRD022·023 기간 재요청** (2023/06~2026/04) — **생존편향 차단** | **미착수 · 최우선** |
| **2** | **재방문 예약** — 남은 기회 사실상 1~2회 | **미착수 · 최우선** |
| **3** | **분석 코드 xlsx 작성** — 게이트 분기 구조 | 미착수 |
| 4 | 계획서의 "동백전 시계열" 표현 → 횡단면으로 수정 | 미착수 |
| 5 | ①쿼리세트.xlsx 시트5(법정동-행정동 매핑) | **REL020 SHP로 대체 가능성 있음** (§5-13 확인 후 판단) |
| 6 | 서류용 파일럿: 지원사업 유형별 재분류 → 정책 공백 선증명 | 미착수 |

## 9-1. 다음 방문 실행 순서 (고정)

```
① 2번째 파일 헤더 유무          (30초)  ← 틀리면 모든 집계가 조용히 어긋남
② COMMA 7개 실제 구분자          (30초)  ← SPN060 22GB가 걸려 있음
③ REL020 SHP 실체·CRS           (1분)
④ 게이트 6개: Q0-2·Q1-B0·Q4-B0·Q10·Q11
⑤ Q8-B (동백전 60세+ 비중)      ← 부산 특수성 축의 값
⑥ 본 분석
```

**게이트를 건너뛰고 본 분석부터 하면 안 된다.** 2차 방문에서 게이트를 하나도 못 돌렸으므로,
현재 본 분석 설계는 전부 **가정 위에 서 있다.**

## 9-2. 일정

```
2026-09-01  2차 방문 (오늘)
2026-09-10  전후 — 실제 분석 완료 마감  ← 반출 심사 영업일 5일 역산
2026-09-18  접수 마감 17:00 · 완성 보고서
```

## 9-3. ⚠️ 현장 코딩 규칙 (2차 방문에서 시간 대부분을 오타로 소모)

인터넷 차단 = **붙여넣기 불가 = 전부 손타이핑.** 아래를 반입 코드 작성 원칙으로 삼는다.

| 금지 | 이유 | 대안 |
|---|---|---|
| 경로에 백슬래시 | `"D:\20260901_..."` 의 `\2`가 **8진 이스케이프**로 해석되어 경로가 깨짐 | **슬래시만** |
| 한글 경로 직접 타이핑 | 반복 오타 | `glob.glob("D:/2026*")[0].replace("\\","/")` |
| `%` 포맷 | 인자 개수·타입 불일치 에러 반복 | `print(a, b, c)` 콤마 나열 |
| `continue` | 아래 줄이 들여쓰기로 딸려가면 **에러도 출력도 없이** 죽음 | 긍정 조건 `if x:` |
| 3단계 이상 중첩 | 들여쓰기 어긋남 | 2단계 이내 |

**반입 형식은 PDF보다 xlsx.** PDF는 복사 시 들여쓰기·따옴표가 깨진다.
xlsx는 **한 행에 한 줄**, 셀 서식을 **텍스트**로 먼저 지정할 것(`=` 시작 셀의 수식 변환 방지).

---

# 10. 현재 평가

| 항목 | 점수 |
|---|:-:|
| 정책 활용성 | 9 |
| 주제 참신성 | 8 |
| 방법론 | 9 |
| 전달력 | 8 |
| 데이터 실현성 | 9 |
| 부산 특수성 | 8 |
| **종합** | **8.6 / 10** |

**최대 리스크는 하나**: 동백전 고령층 이용률(Q8-B). 5% 미만이면 AGE 축이 죽고 부산 특수성이 3점대로 떨어진다. 다만 그 경우에도 **결론 1·2층은 유지**되고 부산 특수성을 POP014·POP019 지역 인구 고령화로 대체할 수 있다.

**주제 전체가 무너지는 시나리오는 없다.** 커버리지가 나쁘면 오히려 결론 3층이 강해진다.

## 10-1. 2026-09-01 갱신 — 리스크 재평가

| 리스크 | 8/28 | 9/1 | 변화 |
|---|:-:|:-:|---|
| 동백전 **기간 부족** | 높음 | **해소** | 3개월 → **21개월** 확보. `AGE_SLOPE` 산출 가능 |
| 동백전 **고령층 비중 값** | 높음 | **높음(유지)** | Q8-B 미실행. 여전히 최대 리스크 |
| **CRD022·023 생존편향** | 미인지 | **높음(신규)** | 속성이 202501~. 폐업자 속성 누락 → **폐업 주제의 근간** |
| 동백전 × 카드 **시계열 결합** | 가능 전제 | **불가(확정)** | 겹침 4개월. 횡단면으로 축소 |
| 데이터 실현성 | 9 | **8** | 57GB·게이트 미실행·재요청 1건 잔존 |
| 공간 시각화 | 불가 전제 | **가능** | REL020 SHP 확인 (실체 검증은 §5-13) |

**신규 최대 리스크는 CRD022·023이다.** 폐업을 다루는 주제인데 폐업 사업자의 속성이 없다.
재요청이 반영되지 않으면 분석 구간을 2025/01~로 잘라야 하고, 그 경우 시차 설계(k=6)가 무너진다.
**Q8-B보다 이쪽이 더 급하다 — Q8-B는 값이 나쁘면 대체 축이 있지만, 이건 대체가 없다.**

---

# 11. 이전 세션에서 있었던 오류 (같은 실수 방지)

| 오류 | 교훈 |
|---|---|
| SPN019를 데이터명만 보고 "법정동 매핑"으로 단정 | **컬럼 목록을 반드시 확인**한다. 이름은 근거가 아니다 |
| CRD023에 사업자번호가 있다고 계획서에 기재 | 정의서 원문 대조 없이 쓰지 않는다 |
| 쿼리에 `/* 위 정의 */` 플레이스홀더를 남김 | 반입 환경은 **복붙 실행**이다. 자기완결형이어야 한다 |
| 커버리지 문제를 "제목부터 못 쓴다"로 과장 | 과장도 오류다. **정정을 명시**한다 |
| MySQL 5.x 대체안이 CTE를 참조 | 대체안이 대체가 되는지 확인한다 |
| 같은 이름 파일을 읽지 않고 덮어씀 | 쓰기 전에 존재 여부 확인 |
| **REL020을 반출등급만 보고 "공간데이터 없음"으로 단정** | **형식(확장자)을 확인한다.** 등급표는 형식의 근거가 아니다 |
| **DB를 전제로 쿼리세트 v3를 전부 작성** | 실제는 CSV였다. **제공 형식을 먼저 확인**한다 |
| **반입 환경을 "복붙 실행"으로 가정** | 인터넷 차단 = **붙여넣기 불가 = 전부 손타이핑.** 코드는 짧고 오타에 강해야 한다 (§9-3) |
| **2차 방문 전체를 파일 구조 파악으로 소진** | 구조 확인용 코드를 **미리 xlsx로 반입**했어야 했다. 현장에서 짜면 그것만으로 하루가 간다 |
